@DATABASE "gg:doc/NDK/Guide/diskfont"
@MASTER   "gg:doc/NDK/AutoDocs/diskfont.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:20:13
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "diskfont.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                              @{b}diskfont.library@{ub}

@{"AvailFonts()" LINK "AvailFonts"}       @{"DisposeFontContents()" LINK "DisposeFontContents"}  @{"GetDiskFontCtrl()" LINK "GetDiskFontCtrl"}
@{"NewFontContents()" LINK "NewFontContents"}  @{"NewScaledDiskFont()" LINK "NewScaledDiskFont"}    @{"OpenDiskFont()" LINK "OpenDiskFont"}
@{"SetDiskFontCtrl()" LINK "SetDiskFontCtrlA"}  @{"SetDiskFontCtrlA()" LINK "SetDiskFontCtrlA"}     

@ENDNODE
@NODE "AvailFonts" "diskfont.library/AvailFonts()"
@{b}

   NAME@{ub}
        @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} -- Inquire available memory & disk fonts.@{b}

   SYNOPSIS@{ub}
        error = @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30}(buffer, bufBytes, flags);
                           A0      D0        D1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30}( @{"struct AvailFontsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 106} *buffer, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} bufBytes
                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} flags );@{b}

   FUNCTION@{ub}
        @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} fills a user supplied buffer with the structure,
        described below, that contains information about all the
        fonts available in memory and/or on disk.  Those fonts
        available on disk need to be loaded into memory and opened
        via @{"OpenDiskFont" LINK "OpenDiskFont"}, those already in memory are accessed via
        @{"OpenFont" LINK "gg:doc/NDK/Guide/graphics/OpenFont"}.  The TextAttr structure required by the open calls
        is part of the information @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} supplies.

        When @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} fails, it returns the number of extra bytes
        it needed to complete the command.  Add this number to your
        current buffer size, allocate a new buffer, and try again.@{b}

   INPUTS@{ub}
        buffer - memory to be filled with @{"struct AvailFontsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 106}
                followed by an array of @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} elements, which
                contains entries for the available fonts and their
                names.

        bufBytes - the number of bytes in the buffer
        flags - @{"AFF_MEMORY" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 85} is set to search memory for fonts to fill
                the structure, @{"AFF_DISK" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 87} is set to search the disk for
                fonts to fill the structure.  @{"AFF_SCALED" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 89} is set to
                not filter out memory fonts that are not designed.
                @{"AFF_BITMAP" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 91} is set to filter out fonts that are not
                stored in Amiga font format, i.e. to filter out
                outline fonts.  Any combination may be specified.
                @{"AFF_TAGGED" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 94} is set to fill the buffer with TAvailFonts
                elements instead of @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} elements.@{b}

   RESULTS@{ub}
        buffer - filled with @{"struct AvailFontsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 106} followed by the
                [T]@{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} elements, There will be duplicate entries
                for fonts found both in memory and on disk, differing
                only by type.  The existance of a disk font in the
                buffer indicates that it exists as an entry in a font
                contents file -- the underlying font file has not been
                checked for validity, thus an @{"OpenDiskFont" LINK "OpenDiskFont"} of it may
                fail.
        error - if non-zero, this indicates the number of bytes needed
                for @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} in addition to those supplied.  Thus
                structure elements were not returned because of
                insufficient bufBytes.@{b}

   EXAMPLE@{ub}
        int afShortage, afSize;
        @{"struct AvailFontsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 106} *afh;

        ...

        afSize = 400;
        do {
            afh = (@{"struct AvailFontsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 106} *) @{"AllocMem" LINK "gg:doc/NDK/Guide/exec/AllocMem"}(afSize, 0);
            if (afh) {
                afShortage = @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30}(afh, afSize, @{"AFF_MEMORY" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 85}|@{"AFF_DISK" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 87});
                if (afShortage) {
                    @{"FreeMem" LINK "gg:doc/NDK/Guide/exec/FreeMem"}(afh, afSize);
                    afSize += afShortage;
                }
            }
            else {
                fail("@{"AllocMem" LINK "gg:doc/NDK/Guide/exec/AllocMem"} of @{"AvailFonts" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 30} buffer afh failed\\n");
                break;
            }
        }
            while (afShortage);

        /*
         * if (afh) non-zero here, then:
         * 1. it points to a valid AvailFontsHeader
         * 2. it must have @{"FreeMem" LINK "gg:doc/NDK/Guide/exec/FreeMem"}(afh, afSize) called for it after use
         */

@ENDNODE
@NODE "DisposeFontContents" "diskfont.library/DisposeFontContents()"
@{b}

   NAME@{ub}
        @{"DisposeFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 38} -- Free the result from @{"NewFontContents" LINK "NewFontContents"}. (V34)@{b}

   SYNOPSIS@{ub}
        @{"DisposeFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 38}(fontContentsHeader)
                            A1

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"DisposeFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 38}( @{"struct FontContentsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 53} * );@{b}

   FUNCTION@{ub}
        This function frees the array of FontContents entries
        returned by @{"NewFontContents" LINK "NewFontContents"}.@{b}

   INPUTS@{ub}
        fontContentsHeader - a @{"struct FontContentsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 53} pointer
            returned by @{"NewFontContents" LINK "NewFontContents"}.@{b}

   EXCEPTIONS@{ub}
        This command was first made available as of version 34.

        A fontContentsHeader other than one acquired by a call
        @{"NewFontContents" LINK "NewFontContents"} will crash.@{b}

   SEE ALSO@{ub}
        @{"NewFontContents()" LINK "NewFontContents"} to get structure freed here.

@ENDNODE
@NODE "NewFontContents" "diskfont.library/NewFontContents()"
@{b}

   NAME@{ub}
        @{"NewFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 34} -- Create a FontContents image for a font. (V34)@{b}

   SYNOPSIS@{ub}
        fontContentsHeader = @{"NewFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 34}(fontsLock,fontName)
       D0                                   A0        A1

        @{"struct FontContentsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 53} *@{"NewFontContents" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 34}( @{"BPTR" LINK "gg:doc/NDK/Guide/Include/dos/dos.h/File" 130}, char * );@{b}

   FUNCTION@{ub}
        This function creates a new array of FontContents entries
        that describe all the fonts associated with the fontName,
        specifically, all those in the font directory whose name
        is that of the font sans the ".font" suffix.@{b}

   INPUTS@{ub}
        fontsLock - a DOS lock on the FONTS: directory (or other
            directory where the font contents file and associated
            font directory resides).
        fontName - the font name, with the ".font" suffix, which
            is also the name of the font contents file.@{b}

   RESULT@{ub}
        fontContentsHeader - a @{"struct FontContentsHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 53} pointer.@{b}

   EXCEPTIONS@{ub}
        This command was first made available as of version 34.

        D0 is zero if the fontName is does not have a ".font" suffix,
        if the fontName is too long, if a DOS error occurred, or if
        memory could not be allocated for the fontContentsHeader.@{b}

   SEE ALSO@{ub}
        @{"DisposeFontContents()" LINK "DisposeFontContents"} to free the structure acquired here.

@ENDNODE
@NODE "NewScaledDiskFont" "diskfont.library/NewScaledDiskFont()"
@{b}

   NAME@{ub}
        @{"NewScaledDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 42} -- Create a DiskFont scaled from another. (V36)@{b}

   SYNOPSIS@{ub}
        header = @{"NewScaledDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 42}(srcFont, destTextAttr)
        D0                         A0       A1

        @{"struct DiskFontHeader" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfont.h/File" 63} *@{"NewScaledDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 42}( @{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *,
                @{"struct TTextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 72} * );@{b}

   INPUTS@{ub}
        srcFont - the font from which the scaled font is to be
            constructed.
        destTextAttr - the desired attributes for the new scaled
            font.  This may be a structure of type TextAttr or
            TTextAttr.@{b}

   RESULT@{ub}
        header - a pointer to a DiskFontHeader structure.  This is not
                being managed by the diskfont.library, however.@{b}

   NOTES@{ub}
        o   This function may use the blitter.
        o   Fonts containing characters that render wholly outside
            the character advance cell are currently not scalable.
        o   The font, and memory allocated for the scaled font can
            can be freed by calling @{"StripFont()" LINK "gg:doc/NDK/Guide/graphics/StripFont"} on the font,
            and then calling @{"UnLoadSeg()" LINK "gg:doc/NDK/Guide/dos/UnLoadSeg"} on the segment created
            by this function.

            Both the TextFont structure, and segment pointer are contained
            within the DiskFontHeader struct.  The DiskFontHeader structure
            will also be freed as part of the @{"UnLoadSeg()" LINK "gg:doc/NDK/Guide/dos/UnLoadSeg"} call.
            @{"StripFont()" LINK "gg:doc/NDK/Guide/graphics/StripFont"} is a new @{"graphics.library" LINK "gg:doc/NDK/Guide/graphics/MAIN"} call as of V36.

@ENDNODE
@NODE "OpenDiskFont" "diskfont.library/OpenDiskFont()"
@{b}

   NAME@{ub}
       @{"OpenDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 26} - load and get a pointer to a disk font.@{b}

   SYNOPSIS@{ub}
       font = @{"OpenDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 26}(textAttr)
       D0                  A0@{b}

   FUNCTION@{ub}
       This function finds the font with the specified textAttr on
       disk, loads it into memory, and returns a pointer to the font
       that can be used in subsequent @{"SetFont" LINK "gg:doc/NDK/Guide/graphics/SetFont"} and @{"CloseFont" LINK "gg:doc/NDK/Guide/graphics/CloseFont"} calls.
       It is important to match this call with a corresponding
       @{"CloseFont" LINK "gg:doc/NDK/Guide/graphics/CloseFont"} call for effective management of font memory.

       If the font is already in memory, the copy in memory is used.
       The disk copy is not reloaded.@{b}

   INPUTS@{ub}
       textAttr - a TextAttr structure that describes the text font
               attributes desired.@{b}

   RESULTS@{ub}
       D0 is zero if the desired font cannot be found.@{b}

   NOTES@{ub}
       As of V36, @{"OpenDiskFont" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 26}() will automatically attempt to
       construct a font for you if:

               You have requested a font size which does not exist
               as a designed font, and

               You have not set the DESIGNED bit in the ta_Flags
               field of the TextAttr, or TTextAttr struct.

       Constructed fonts are created by scaling a designed font.
       A designed font is one which typically resides on disk,
       or in ROM (e.g., a font which has been designed by hand
       using a drawing tool).  Designed fonts generally look better
       than fonts constructed by the font scaler, but designed
       fonts also require disk space for each font size.

       Always set the DESIGNED bit if you do not want constructed fonts,
       or use @{"AvailFonts()" LINK "AvailFonts"} to find out which font sizes already exist.

       As of V37 the diskfont.library supported built-in outline
       fonts.  Then in V38 the outline font engine was moved to
       a new library, "@{"bullet.library" LINK "gg:doc/NDK/Guide/bullet/MAIN"}."@{b}

   BUGS@{ub}
       This routine will not work well with font names whose file
       name components are longer than the maximum allowed
       (30 characters).

@ENDNODE
@NODE "GetDiskFontCtrl" "diskfont.library/GetDiskFontCtrl()"
@{b}
    NAME@{ub}
        @{"GetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 46} -- Inquire diskfont global settings. (V45)@{b}
    SYNOPSIS@{ub}
        value = @{"GetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 46}( tagid );
                                  D0

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"GetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 46}( @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} tagitem );@{b}


    FUNCTION@{ub}
        @{"GetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 46} reads global settings of the diskfont
        library, as the setting of the base DPI X and Y values,
        the cache enable flag and the @{"AvailFonts" LINK "AvailFonts"} sorting values.
        The TagItem passed in identifies the type of data item
        to read.@{b}

    INPUTS@{ub}
        tagid - a tag ID as documented in @{"diskfont/diskfonttag.h" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File"}
                that identifies the kind of data item to inquiry.

                The following tag values are currently supported:
                (see @{"diskfont/diskfonttag.h" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File"})

                Font generator DPI settings:

                @{"DFCTRL_XDPI" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 308}
                @{"DFCTRL_YDPI" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 309}     X and Y dpi device resolution

                @{"DFCTRL_XDOTP" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 316}
                @{"DFCTRL_YDOTP" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 317}    X and Y dpi dot sizes.

                @{"DFCTRL_SYMSET" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 323}   default symbol set (unused)

                @{"DFCTRL_CACHE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 329}    @{"AvailFonts" LINK "AvailFonts"} cache enable (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})

                @{"DFCTRL_SORTMODE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 334} @{"AvailFonts" LINK "AvailFonts"} font sorting (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})

                currently defined sort orders are:

                        @{"DFCTRL_SORT_OFF" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 337} don't sort
                        @{"DFCTRL_SORT_ASC" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 340} localized ascending
                        @{"DFCTRL_SORT_DES" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 343} localized descending@{b}

    RESULTS@{ub}
        value  - The current diskfont default setting for the
                 selected tag item.
                 The result code is undocumented if an unknown
                 tag value is passed in.@{b}

    EXAMPLE@{ub}

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} cache;

        cache = @{"GetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 46}(@{"DFCTRL_CACHE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 329});

        /* read the current cache enable flag. */@{b}


    NOTES@{ub}
        This call is not semaphore protected. This means that
        several calls to this function and @{"SetDiskFontCtrl()" LINK "SetDiskFontCtrlA"}
        might cause inconsistent results. The function will not
        fail or crash, but the result might be near to useless
        in a multitasking system.
        This function should never be called by the average
        user. Its sole purpose is to provide the font
        preferences editor with data about the current diskfont
        settings. It should not be called for other purposes.@{b}

    BUGS@{ub}@{b}

    SEE ALSO@{ub}
        @{"diskfont/diskfonttag.h" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File"}

@ENDNODE
@NODE "SetDiskFontCtrlA" "diskfont.library/SetDiskFontCtrlA()"
@{b}
    NAME@{ub}
        @{"SetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 55} -- Adjust disk font global settings (V45)@{b}

    SYNOPSIS@{ub}
        @{"SetDiskFontCtrlA" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 50}( tags );
                           A0

        void @{"SetDiskFontCtrlA" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 50}( @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} * );

        void @{"SetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 55}( Tag, ... );@{b}


    FUNCTION@{ub}
        @{"SetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 55} adjusts the global settings passed in
        in the form of a tag list and installs them into the
        the diskfont internal database. This includes the
        base DPI X and Y values, the cache enable flag and the
        @{"AvailFonts" LINK "AvailFonts"} font sorting order.@{b}

    INPUTS@{ub}
        tags -  a tag list defining the global settings that are
                to be adjusted.

                The following tag values are currently supported:
                (see @{"diskfont/diskfonttag.h" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File"})

                Font generator DPI settings:

                @{"DFCTRL_XDPI" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 308}
                @{"DFCTRL_YDPI" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 309}     X and Y dpi device resolution

                @{"DFCTRL_XDOTP" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 316}
                @{"DFCTRL_YDOTP" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 317}    X and Y dpi dot sizes.

                @{"DFCTRL_SYMSET" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 323}   default symbol set (unused)

                @{"DFCTRL_CACHE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 329}    @{"AvailFonts" LINK "AvailFonts"} cache enable (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})

                @{"DFCTRL_SORTMODE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 334} @{"AvailFonts" LINK "AvailFonts"} font sorting (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})

                currently defined sort orders are:

                        @{"DFCTRL_SORT_OFF" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 337} don't sort
                        @{"DFCTRL_SORT_ASC" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 340} localized ascending
                        @{"DFCTRL_SORT_DES" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 343} localized descending@{b}

    RESULTS@{ub}@{b}


    EXAMPLE@{ub}

        @{"SetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 55}(@{"DFCTRL_CACHE" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File" 329},@{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95},@{"TAG_DONE" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 39});

        /* enable the @{"AvailFonts" LINK "AvailFonts"} cache. */@{b}


    NOTES@{ub}
        The @{"SetDiskFontCtrlA" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 50}() function is the assembly language
        interface which takes the tag list pointer in A0.
        @{"SetDiskFontCtrl" LINK "gg:doc/NDK/Guide/Include/inline/diskfont.h/File" 55}() is the stack based wrapper for
        convenient C language calls.

        This call is not semaphore protected. This means that
        several calls to this function and @{"GetDiskFontCtrl()" LINK "GetDiskFontCtrl"}
        might cause inconsistent results. The function will not
        fail or crash, but the result might be near to useless
        in a multitasking system.
        This function should never be called by the average
        user. Its sole purpose is to allow the font
        preferences editor to adjust the diskfont internal
        data in a user friendly way. It should not be called
        for other purposes, especially, applications *MUST @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500}*
        call this to enable or disable the cache setting. This
        should be left to the user by selecting the preferences.@{b}

    BUGS@{ub}@{b}

    SEE ALSO@{ub}
        @{"diskfont/diskfonttag.h" LINK "gg:doc/NDK/Guide/Include/diskfont/diskfonttag.h/File"}, @{"utility/tagitem.h" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File"}

@ENDNODE
