@DATABASE "gg:doc/NDK/Guide/graphics"
@MASTER   "gg:doc/NDK/AutoDocs/graphics.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:21:35
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "graphics.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                              @{b}graphics.library@{ub}

@{"AddAnimOb()" LINK "AddAnimOb"}           @{"AddBob()" LINK "AddBob"}               @{"AddFont()" LINK "AddFont"}
@{"AddVSprite()" LINK "AddVSprite"}          @{"AllocBitMap()" LINK "AllocBitMap"}          @{"AllocDBufInfo()" LINK "AllocDBufInfo"}
@{"AllocRaster()" LINK "AllocRaster"}         @{"AllocSpriteData()" LINK "AllocSpriteDataA"}      @{"AllocSpriteDataA()" LINK "AllocSpriteDataA"}
@{"AndRectRegion()" LINK "AndRectRegion"}       @{"AndRegionRegion()" LINK "AndRegionRegion"}      @{"AndregionRegion()" LINK "AndRegionRegion"}
@{"Animate()" LINK "Animate"}             @{"AreaCircle()" LINK "AreaCircle"}           @{"AreaDraw()" LINK "AreaDraw"}
@{"AreaEllipse()" LINK "AreaEllipse"}         @{"AreaEnd()" LINK "AreaEnd"}              @{"AreaMove()" LINK "AreaMove"}
@{"AskFont()" LINK "AskFont"}             @{"AskSoftStyle()" LINK "AskSoftStyle"}         @{"AttachPalExtra()" LINK "AttachPalExtra"}
@{"AttempLockLayerRom()" LINK "AttemptLockLayerRom"}  @{"AttemptLockLayerRom()" LINK "AttemptLockLayerRom"}  @{"BestModeID()" LINK "BestModeIDA"}
@{"BestModeIDA()" LINK "BestModeIDA"}         @{"BitMapScale()" LINK "BitMapScale"}          @{"BltBitMap()" LINK "BltBitMap"}
@{"BltBitMapRastPort()" LINK "BltBitMapRastPort"}   @{"BltClear()" LINK "BltClear"}             @{"BltMaskBitMapRastPort()" LINK "BltMaskBitMapRastPort"}
@{"BltPattern()" LINK "BltPattern"}          @{"BltTemplate()" LINK "BltTemplate"}          @{"CalcIVG()" LINK "CalcIVG"}
@{"CBump()" LINK "CBump"}               @{"CEND()" LINK "CEND"}                 @{"ChangeExtSprite()" LINK "ChangeExtSpriteA"}
@{"ChangeExtSpriteA()" LINK "ChangeExtSpriteA"}    @{"ChangeSprite()" LINK "ChangeSprite"}         @{"ChangeVPBitMap()" LINK "ChangeVPBitMap"}
@{"CINIT()" LINK "CINIT"}               @{"ClearEOL()" LINK "ClearEOL"}             @{"ClearRectRegion()" LINK "ClearRectRegion"}
@{"ClearRegion()" LINK "ClearRegion"}         @{"ClearScreen()" LINK "ClearScreen"}          @{"ClipBlit()" LINK "ClipBlit"}
@{"CloseFont()" LINK "CloseFont"}           @{"CloseMonitor()" LINK "CloseMonitor"}         @{"CMOVE()" LINK "CMOVE"}
@{"CMove()" LINK "CMOVE"}               @{"CoerceMode()" LINK "CoerceMode"}           @{"CopySBitMap()" LINK "CopySBitMap"}
@{"CWAIT()" LINK "CWAIT"}               @{"CWait()" LINK "CWAIT"}                @{"DisownBlitter()" LINK "DisownBlitter"}
@{"DisposeRegion()" LINK "DisposeRegion"}       @{"DoCollision()" LINK "DoCollision"}          @{"Draw()" LINK "Draw"}
@{"DrawEllipse()" LINK "DrawEllipse"}         @{"DrawGList()" LINK "DrawGList"}            @{"EraseRect()" LINK "EraseRect"}
@{"ExtendFont()" LINK "ExtendFont"}          @{"ExtendFontTags()" LINK "ExtendFont"}       @{"FindColor()" LINK "FindColor"}
@{"FindDisplayInfo()" LINK "FindDisplayInfo"}     @{"Flood()" LINK "Flood"}                @{"FontExtent()" LINK "FontExtent"}
@{"FreeBitMap()" LINK "FreeBitMap"}          @{"FreeColorMap()" LINK "FreeColorMap"}         @{"FreeCopList()" LINK "FreeCopList"}
@{"FreeCprList()" LINK "FreeCprList"}         @{"FreeDBufInfo()" LINK "FreeDBufInfo"}         @{"FreeGBuffers()" LINK "FreeGBuffers"}
@{"FreeRaster()" LINK "FreeRaster"}          @{"FreeSprite()" LINK "FreeSprite"}           @{"FreeSpriteData()" LINK "FreeSpriteData"}
@{"FreeVPortCopLists()" LINK "FreeVPortCopLists"}   @{"GetAPen()" LINK "GetAPen"}              @{"GetBitMapAttr()" LINK "GetBitMapAttr"}
@{"GetBPen()" LINK "GetBPen"}             @{"GetColorMap()" LINK "GetColorMap"}          @{"GetDisplayInfoData()" LINK "GetDisplayInfoData"}
@{"GetDrMd()" LINK "GetDrMd"}             @{"GetExtSprite()" LINK "GetExtSpriteA"}         @{"GetExtSpriteA()" LINK "GetExtSpriteA"}
@{"GetGBuffers()" LINK "GetGBuffers"}         @{"GetOPen()" LINK "GetOPen"}              @{"GetRGB32()" LINK "GetRGB32"}
@{"GetRGB4()" LINK "GetRGB4"}             @{"GetRPAttrA()" LINK "GetRPAttrA"}           @{"GetRPAttrs()" LINK "GetRPAttrA"}
@{"GetSprite()" LINK "GetSprite"}           @{"GetVPModeID()" LINK "GetVPModeID"}          @{"GfxAssociate()" LINK "GfxAssociate"}
@{"GfxFree()" LINK "GfxFree"}             @{"GfxLookUP()" LINK "GfxLookUP"}            @{"GfxLookUp()" LINK "GfxLookUP"}
@{"GfxNew()" LINK "GfxNew"}              @{"InitArea()" LINK "InitArea"}             @{"InitBitMap()" LINK "InitBitMap"}
@{"InitGels()" LINK "InitGels"}            @{"InitGMasks()" LINK "InitGMasks"}           @{"InitMasks()" LINK "InitMasks"}
@{"InitRastPort()" LINK "InitRastPort"}        @{"InitTmpRas()" LINK "InitTmpRas"}           @{"InitView()" LINK "InitView"}
@{"InitViewPort()" LINK "InitVPort"}        @{"InitVPort()" LINK "InitVPort"}            @{"LoadRGB32()" LINK "LoadRGB32"}
@{"LoadRGB4()" LINK "LoadRGB4"}            @{"LoadView()" LINK "LoadView"}             @{"LockLayerRom()" LINK "LockLayerRom"}
@{"MakeVPort()" LINK "MakeVPort"}           @{"ModeNotAvailable()" LINK "ModeNotAvailable"}     @{"Move()" LINK "Move"}
@{"MoveSprite()" LINK "MoveSprite"}          @{"MrgCop()" LINK "MrgCop"}               @{"NewRegion()" LINK "NewRegion"}
@{"NextDisplayInfo()" LINK "NextDisplayInfo"}     @{"ObtainBestPen()" LINK "ObtainBestPenA"}        @{"ObtainBestPenA()" LINK "ObtainBestPenA"}
@{"ObtainPen()" LINK "ObtainPen"}           @{"OpenFont()" LINK "OpenFont"}             @{"OpenMonitor()" LINK "OpenMonitor"}
@{"OrRectRegion()" LINK "OrRectRegion"}        @{"OrRegionRegion()" LINK "OrRegionRegion"}       @{"OwnBlitter()" LINK "OwnBlitter"}
@{"PolyDraw()" LINK "PolyDraw"}            @{"QBlit()" LINK "QBlit"}                @{"QBSBlit()" LINK "QBSBlit"}
@{"ReadPixel()" LINK "ReadPixel"}           @{"ReadPixelArray8()" LINK "ReadPixelArray8"}      @{"ReadPixelLine8()" LINK "ReadPixelLine8"}
@{"RectFill()" LINK "RectFill"}            @{"ReleasePen()" LINK "ReleasePen"}           @{"RemBob()" LINK "RemBob"}
@{"RemFont()" LINK "RemFont"}             @{"RemIBob()" LINK "RemIBob"}              @{"RemVSprite()" LINK "RemVSprite"}
@{"ScalerDiv()" LINK "ScalerDiv"}           @{"ScrollRaster()" LINK "ScrollRaster"}         @{"ScrollRasterBF()" LINK "ScrollRasterBF"}
@{"ScrollVPort()" LINK "ScrollVPort"}         @{"SetABPenDrMd()" LINK "SetABPenDrMd"}         @{"SetAPen()" LINK "SetAPen"}
@{"SetBPen()" LINK "SetBPen"}             @{"SetChipRev()" LINK "SetChipRev"}           @{"SetCollision()" LINK "SetCollision"}
@{"SetDrMd()" LINK "SetDrMd"}             @{"SetFont()" LINK "SetFont"}              @{"SetMaxPen()" LINK "SetMaxPen"}
@{"SetOPen()" LINK "SetOPen"}             @{"SetOutlinePen()" LINK "SetOutlinePen"}        @{"SetRast()" LINK "SetRast"}
@{"SetRGB32()" LINK "SetRGB32"}            @{"SetRGB32CM()" LINK "SetRGB32CM"}           @{"SetRGB4()" LINK "SetRGB4"}
@{"SetRGB4CM()" LINK "SetRGB4CM"}           @{"SetRPAttrA()" LINK "SetRPAttrA"}           @{"SetRPAttrs()" LINK "SetRPAttrA"}
@{"SetSoftStyle()" LINK "SetSoftStyle"}        @{"SetWriteMask()" LINK "SetWriteMask"}         @{"SortGList()" LINK "SortGList"}
@{"StripFont()" LINK "StripFont"}           @{"SyncSBitMap()" LINK "SyncSBitMap"}          @{"Text()" LINK "Text"}
@{"TextExtent()" LINK "TextExtent"}          @{"textExtent()" LINK "TextExtent"}           @{"TextFit()" LINK "TextFit"}
@{"TextLength()" LINK "TextLength"}          @{"UCopperListInit()" LINK "CINIT"}      @{"UnlockLayerRom()" LINK "UnlockLayerRom"}
@{"VBeamPos()" LINK "VBeamPos"}            @{"VideoControl()" LINK "VideoControl"}         @{"VideoControlTags()" LINK "VideoControl"}
@{"WaitBlit()" LINK "WaitBlit"}            @{"WaitBOVP()" LINK "WaitBOVP"}             @{"WaitTOF()" LINK "WaitTOF"}
@{"WriteChunkyPixels()" LINK "WriteChunkyPixels"}   @{"WritePixel()" LINK "WritePixel"}           @{"WritePixelArray8()" LINK "WritePixelArray8"}
@{"WritePixelLine8()" LINK "WritePixelLine8"}     @{"XorRectRegion()" LINK "XorRectRegion"}        @{"XorRegionRegion()" LINK "XorRegionRegion"}

@ENDNODE
@NODE "AddAnimOb" "graphics.library/AddAnimOb()"
@{b}

   NAME@{ub}
        @{"AddAnimOb" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 137}  --  Add an AnimOb to the linked list of AnimObs.@{b}

   SYNOPSIS@{ub}
        @{"AddAnimOb" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 137}(anOb, anKey, rp)
                  A0    A1     A2

        void @{"AddAnimOb" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 137}(@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} *,@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} **, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Links this AnimOb into the current list pointed to by animKey.
        Initializes all the Timers of the AnimOb's components.
        Calls @{"AddBob" LINK "AddBob"} with each component's Bob.
        rp->GelsInfo must point to an initialized GelsInfo structure.@{b}

   INPUTS@{ub}
        anOb  = pointer to the AnimOb structure to be added to the list
        anKey = address of a pointer to the first AnimOb in the list
                (anKey = @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if there are no AnimObs in the list so far)
        rp    = pointer to a valid RastPort@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"Animate()" LINK "Animate"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"} @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "AddBob" "graphics.library/AddBob()"
@{b}

   NAME@{ub}
        @{"AddBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 97} -- Adds a Bob to current gel list.@{b}

   SYNOPSIS@{ub}
        @{"AddBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 97}(Bob, rp)
               A0   A1

        void @{"AddBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 97}(@{"struct Bob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 139} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Sets up the system Bob flags, then links this gel into the list
        via @{"AddVSprite" LINK "AddVSprite"}.@{b}

   INPUTS@{ub}
        Bob = pointer to the Bob structure to be added to the gel list
        rp  = pointer to a RastPort structure@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"AddVSprite()" LINK "AddVSprite"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AddFont" "graphics.library/AddFont()"
@{b}

   NAME@{ub}
        @{"AddFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 353} -- add a font to the system list@{b}

   SYNOPSIS@{ub}
        @{"AddFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 353}(textFont)
                A1

        void @{"AddFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 353}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *);@{b}

   FUNCTION@{ub}
        This function adds the text font to the system, making it
        available for use by any application.  The font added must be
        in public memory, and remain until successfully removed.@{b}

   INPUTS@{ub}
        textFont - a TextFont structure in public ram.@{b}

   RESULT@{ub}@{b}

   NOTES@{ub}
        This function will set the tf_Accessors to 0.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetFont()" LINK "SetFont"}  @{"RemFont()" LINK "RemFont"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "AddVSprite" "graphics.library/AddVSprite()"
@{b}

   NAME@{ub}
        @{"AddVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 101} -- Add a VSprite to the current gel list.@{b}

   SYNOPSIS@{ub}
        @{"AddVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 101}(vs, rp)
                   A0  A1

        void @{"AddVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 101}(@{"struct VSprite" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 72} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Sets up the system VSprite flags
        Links this VSprite into the current gel list using its Y,X@{b}

   INPUTS@{ub}
        vs = pointer to the VSprite structure to be added to the gel list
        rp = pointer to a RastPort structure@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "AllocBitMap" "graphics.library/AllocBitMap()"
@{b}

   NAME@{ub}
        @{"AllocBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 624} -- Allocate a bitmap and attach bitplanes to it. (V39)@{b}


   SYNOPSIS@{ub}
        bitmap=@{"AllocBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 624}(sizex,sizey,depth, flags, friend_bitmap)
                           d0    d1    d2     d3       a0

        @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *@{"AllocBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 624}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *);@{b}

   FUNCTION@{ub}
        Allocates and initializes a bitmap structure. Allocates and initializes
        bitplane data, and sets the bitmap's planes to point to it.@{b}

   INPUTS@{ub}
        sizex = the width (in pixels) desired for the bitmap data.

        sizey = the height (in pixels) desired.

        depth = the number of bitplanes deep for the allocation.
                Pixels with AT LEAST this many bits will be allocated.

        flags = @{"BMF_CLEAR" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 69} to specify that the allocated raster should be
                filled with color 0.

                @{"BMF_DISPLAYABLE" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 70} to specify that this bitmap data should
                be allocated in such a manner that it can be displayed.
                Displayable data has more severe alignment restrictions
                than non-displayable data in some systems.

                @{"BMF_INTERLEAVED" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 71} tells graphics that you would like your
                bitmap to be allocated with one large chunk of display
                memory for all bitplanes. This minimizes color flashing
                on deep displays. If there is not enough contiguous RAM
                for an interleaved bitmap, graphics.library will fall
                back to a non-interleaved one.

                @{"BMF_MINPLANES" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 73} causes graphics to only allocate enough space
                in the bitmap structure for "depth" plane pointers. This
                is for system use and should not be used by applications use
                as it is inefficient, and may waste memory.

        friend_bitmap = pointer to another bitmap, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}. If this pointer
                is passed, then the bitmap data will be allocated in
                the most efficient form for blitting to friend_bitmap.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        When allocating using a friend bitmap, it is not safe to assume
        anything about the structure of the bitmap data if that friend
        BitMap might not be a standard amiga bitmap (for instance, if
        the workbench is running on a non-amiga display device, its
        Screen->RastPort->BitMap won't be in standard amiga format.
        The only safe operations to perform on a non-standard BitMap are:

                - blitting it to another bitmap, which must be either a standard
                  Amiga bitmap, or a friend of this bitmap.

                - blitting from this bitmap to a friend bitmap or to a standard
                  Amiga bitmap.

                - attaching it to a rastport and making rendering calls.

        Good arguments to pass for the friend_bitmap are your window's
        RPort->BitMap, and your screen's RastPort->BitMap.
        Do @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} pass &(screenptr->BitMap)!

        BitMaps not allocated with @{"BMF_DISPLAYABLE" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 70} may not be used as
        Intuition Custom BitMaps or as RasInfo->BitMaps.  They may be blitted
        to a @{"BMF_DISPLAYABLE" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 70} BitMap, using one of the @{"BltBitMap()" LINK "BltBitMap"} family of
        functions.@{b}

   SEE ALSO@{ub}
        @{"FreeBitMap()" LINK "FreeBitMap"}

@ENDNODE
@NODE "AllocDBufInfo" "graphics.library/AllocDBufInfo()"
@{b}

   NAME@{ub}
       @{"AllocDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 661} -- Allocate structure for multi-buffered animation (V39)@{b}

   SYNOPSIS@{ub}
       @{"AllocDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 661}(vp)
                      a0

        @{"struct DBufInfo" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 279} * @{"AllocDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 661}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *)@{b}

   FUNCTION@{ub}
        Allocates a structure which is used by the @{"ChangeVPBitMap()" LINK "ChangeVPBitMap"}
        routine.@{b}

   INPUTS@{ub}
       vp  =  A pointer to a ViewPort structure.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        Returns 0 if there is no memory available or if the display mode
        of the viewport does not support double-buffering.

        The only fields of the DBufInfo structure which can be used by application
        programs are the dbi_SafeMessage, dbi_DispMessage, dbi_UserData1 and
        dbi_UserData2 fields.

        dbi_SafeMessage and dbi_DispMessage are standard exec message structures
        which may be used for synchronizing your animation with the screen update.

        dbi_SafeMessage is a message which is replied to when it is safe to write to
        the old BitMap (the one which was installed when you called @{"ChangeVPBitMap" LINK "ChangeVPBitMap"}).

        dbi_DispMessage is replied to when it is safe to call @{"ChangeVPBitMap" LINK "ChangeVPBitMap"} again
        and be certain that the new frame has been seen at least once.

        The dbi_UserData1 and dbi_UserData2 fields, which are stored after each
        message, are for your application to stuff any data into that it may need
        to examine when looking at the reply coming into the ReplyPort for either
        of the embedded Message structures.

        DBufInfo structures MUST be allocated with this function. The size of
        the structure will grow in future releases.

        The following fragment shows proper double buffering synchronization:

        int SafeToChange=@{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, SafeToWrite=@{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, CurBuffer=1;
        @{"struct MsgPort" LINK "gg:doc/NDK/Guide/Include/exec/ports.h/File" 28} *ports[2];    /* reply ports for DispMessage and SafeMessage */
        @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *BmPtrs[2];
        @{"struct DBufInfo" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 279} *myDBI;

        ... allocate bitmap pointers, DBufInfo, set up viewports, etc.

        myDBI->dbi_SafeMessage.mn_ReplyPort=ports[0];
        myDBI->dbi_DispMessage.mn_ReplyPort=ports[1];
        while (! done)
        {
            if (! SafeToWrite)
                while(! @{"GetMsg" LINK "gg:doc/NDK/Guide/exec/GetMsg"}(ports[0])) @{"Wait" LINK "gg:doc/NDK/Guide/exec/Wait"}(1l<<(ports[0]->mp_SigBit));
            SafeToWrite=@{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95};

            ... render to bitmap # CurBuffer.

            if (! SafeToChange)
                while(! @{"GetMsg" LINK "gg:doc/NDK/Guide/exec/GetMsg"}(ports[1])) @{"Wait" LINK "gg:doc/NDK/Guide/exec/Wait"}(1l<<(ports[1]->mp_SigBit));
            SafeToChange=@{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95};
            @{"WaitBlit()" LINK "WaitBlit"};         /* be sure rendering has finished */
            @{"ChangeVPBitMap" LINK "ChangeVPBitMap"}(vp,BmPtrs[CurBuffer],myDBI);
            SafeToChange=@{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98};
            SafeToWrite=@{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98};
            CurBuffer ^=1;      /* toggle current buffer */
        }
       if (! SafeToChange)      /* cleanup pending messages */
            while(! @{"GetMsg" LINK "gg:doc/NDK/Guide/exec/GetMsg"}(ports[1])) @{"Wait" LINK "gg:doc/NDK/Guide/exec/Wait"}(1l<<(ports[1]->mp_SigBit));
       if (! SafeToWrite)       /* cleanup */
            while(! @{"GetMsg" LINK "gg:doc/NDK/Guide/exec/GetMsg"}(ports[0])) @{"Wait" LINK "gg:doc/NDK/Guide/exec/Wait"}(1l<<(ports[0]->mp_SigBit));@{b}

   SEE ALSO@{ub}
        @{"FreeDBufInfo()" LINK "FreeDBufInfo"} @{"ChangeVPBitMap()" LINK "ChangeVPBitMap"}

@ENDNODE
@NODE "AllocRaster" "graphics.library/AllocRaster()"
@{b}

   NAME@{ub}
        @{"AllocRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 361} -- Allocate space for a bitplane.@{b}

   SYNOPSIS@{ub}
        planeptr = @{"AllocRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 361}( width, height )
           d0                    d0     d1

        @{"PLANEPTR" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 44} @{"AllocRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 361}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        This function calls the memory allocation routines
        to allocate memory space for a bitplane "width" bits
        wide and "height" bits high.@{b}

   INPUTS@{ub}
        width   - number of columns in bitplane
        height  - number of rows in bitplane@{b}

   RESULT@{ub}
        planeptr - pointer to first word in bitplane, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if
                   it was not possible to allocate the desired
                   amount of memory.@{b}

   NOTES@{ub}
        In order to assure proper alignment of display memory, the
        @{"AllocBitMap()" LINK "AllocBitMap"} function should be used instead of @{"AllocRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 361}
        when you wish to allocate display memory (rasters which are
        attached to a ViewPort or Screen).@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FreeRaster()" LINK "FreeRaster"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "AllocSpriteDataA" "graphics.library/AllocSpriteDataA()"
@{b}

   NAME@{ub}
        @{"AllocSpriteDataA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 693} -- allocate sprite data and convert from a bitmap. (V39)
        @{"AllocSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 698} -- varargs stub for @{"AllocSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 698}(). (V39)@{b}

   SYNOPSIS@{ub}
        SpritePtr | 0 = @{"AllocSpriteDataA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 693}(bitmap,taglist)
         d0                              a2      a1

        @{"struct ExtSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 25} *@{"AllocSpriteDataA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 693}( @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} * );

        extsprite=@{"AllocSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 698}(bitmap,tags,...@{"TAG_END" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 40})@{b}

    FUNCTION@{ub}
        Allocate memory to hold a sprite image, and convert the passed-in
        bitmap data to the appropriate format. The tags allow specification
        of width, scaling, and other options.@{b}

   INPUTS@{ub}
        bitmap - ptr to a bitmap. This bitmap provides the source data for the
                sprite image.

        tags -
                @{"SPRITEA_Width" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 35} specifies how many pixels wide you desire
                the sprite to be. Specifying a width wider than the hardware
                can handle will cause the function to return failure. If the
                bitmap passed in is narrower than the width asked for, then
                it will be padded on the right with transparent pixels.
                Defaults to 16.

                @{"SPRITEA_XReplication" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 36} controls the horizontal pixel replication factor
                used when converting the bitmap data. Valid values are:
                        0 - perform a 1 to 1 conversion
                        1 - each pixel from the source is replicated twice
                            in the output.
                        2 - each pixel is replicated 4 times.
                       -1 - skip every other pixel in the source bitmap
                       -2 - only include every fourth pixel from the source.

                        This tag is useful for converting data from one resolution
                to another. For instance, hi-res bitmap data can be correctly
                converted for a lo-res sprite by using an x replication factor
                of -1. Defaults to 0.

                @{"SPRITEA_YReplication" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 37} controls the vertical pixel replication factor
                in the same manner as @{"SPRITEA_XReplication" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 36} controls the horizontal.

                @{"SPRITEA_OutputHeight" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 38} specifies how tall the resulting sprite
                should be. Defaults to the bitmap height. The bitmap MUST be at
                least as tall as the output height.

                @{"SPRITEA_Attached" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 39} tells the function that you wish to convert
                the data for the second sprite in an attached sprite pair.
                This will cause @{"AllocSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 698}() to take its data from the
                3rd and 4th bitplanes of the passed in bitmap.


        Bitplane data is not required to be in chip ram for this function.@{b}


   RESULTS@{ub}
        SpritePtr = a pointer to a ExtSprite structure, or 0 if there is
        a failure. You should pass this pointer to @{"FreeSpriteData()" LINK "FreeSpriteData"} when finished
        with the sprite.@{b}

   BUGS@{ub}
                Under V39, the appropriate attach bits would not be set in the sprite
        data.
                The work-around is to set the bits manually. Bit 7 of the second
        word should be set. On a 32 bit sprite, bit 7 of the 3rd word should
        also be set. For a 64 bit sprite, bit 7 of the 5th word should also be
        set. This should @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} be done under V40, as the bug is fixed.@{b}

   SEE ALSO@{ub}
        @{"FreeSpriteData()" LINK "FreeSpriteData"} @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"GetExtSpriteA()" LINK "GetExtSpriteA"}
        @{"AllocBitMap()" LINK "AllocBitMap"} @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "AndRectRegion" "graphics.library/AndRectRegion()"
@{b}

   NAME@{ub}
        @{"AndRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 369} -- Perform 2d AND operation of rectangle
                         with region, leaving result in region.@{b}

   SYNOPSIS@{ub}
        @{"AndRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 369}(region,rectangle)
                        a0      a1

        void @{"AndRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 369}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Rectangle" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Clip away any portion of the region that exists outside
        of the rectangle. Leave the result in region.@{b}

   INPUTS@{ub}
        region - pointer to Region structure
        rectangle - pointer to Rectangle structure@{b}

   NOTES@{ub}
        Unlike the other rect-region primitives, @{"AndRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 369}() cannot
        fail.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AndRegionRegion()" LINK "AndRegionRegion"} @{"OrRectRegion()" LINK "OrRectRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "AndRegionRegion" "graphics.library/AndRegionRegion()"
@{b}

   NAME@{ub}
       @{"AndRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 449} -- Perform 2d AND operation of one region
                       with second region, leaving result in second region.@{b}

   SYNOPSIS@{ub}
       status = @{"AndRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 449}(region1,region2)
          d0                       a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} AndregionRegion(@{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} * );@{b}

   FUNCTION@{ub}
       Remove any portion of region2 that is not in region1.@{b}

   INPUTS@{ub}
       region1 - pointer to Region structure
       region2 - pointer to Region structure to use and for result@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"OrRegionRegion()" LINK "OrRegionRegion"} @{"AndRectRegion()" LINK "AndRectRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "Animate" "graphics.library/Animate()"
@{b}

   NAME@{ub}
        Animate  --  Processes every AnimOb in the current animation list.@{b}

   SYNOPSIS@{ub}
        Animate(anKey, rp)
                A0     A1

        void Animate(@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} **, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        For every AnimOb in the list
            - update its location and velocities
            - call the AnimOb's special routine if one is supplied
            - for each component of the AnimOb
                - if this sequence times out, switch to the new one
                - call this component's special routine if one is supplied
                - set the sequence's VSprite's y,x coordinates based
                  on whatever these routines cause@{b}

   INPUTS@{ub}
        ankey = address of the variable that points to the head AnimOb
        rp    = pointer to the RastPort structure@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AddAnimOb()" LINK "AddAnimOb"} @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AreaCircle" "graphics.library/AreaCircle()"
@{b}

    NAME@{ub}
        @{"AreaCircle" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 69} -- add a circle to areainfo list for areafill.@{b}


    SYNOPSIS@{ub}
        error = (int) @{"AreaCircle" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 69}( rp,  cx,  cy, radius)
        D0                        A1   D0   D1  D2

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"AreaCircle" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 69}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55});@{b}

    FUNCTION@{ub}
        Add circle to the vector buffer. It will be drawn to the rastport when
        @{"AreaEnd" LINK "AreaEnd"} is executed.@{b}

    INPUTS@{ub}
        rp       - pointer to a RastPort structure

        cx, cy   - the coordinates of the center of the desired circle.

        radius   - is the radius of the circle to draw around the centerpoint.@{b}

    RESULTS@{ub}
        0 if no error
        -1 if no space left in vector list@{b}

    NOTES@{ub}
        This function is actually a macro which calls
            @{"AreaEllipse" LINK "AreaEllipse"}(rp,cx,cy,radius,radius).@{b}

    SEE ALSO@{ub}
        @{"AreaMove()" LINK "AreaMove"} @{"AreaDraw()" LINK "AreaDraw"} @{"AreaCircle" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 69}() @{"InitArea()" LINK "InitArea"} @{"AreaEnd()" LINK "AreaEnd"}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"} @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"}

@ENDNODE
@NODE "AreaDraw" "graphics.library/AreaDraw()"
@{b}

   NAME@{ub}
        @{"AreaDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 205} -- Add a point to a list of end points for areafill.@{b}


   SYNOPSIS@{ub}
        error = @{"AreaDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 205}( rp,  x,     y)
          d0              A1 D0:16 D1:16

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"AreaDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 205}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
        Add point to the vector buffer.@{b}


   INPUTS@{ub}
        rp      - points to a RastPort structure.
        x,y     - are coordinates of a point in the raster.@{b}

   RESULT@{ub}
        error   - zero for success, else -1 if no there was no space
                  left in the vector list.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AreaMove()" LINK "AreaMove"} @{"InitArea()" LINK "InitArea"} @{"AreaEnd()" LINK "AreaEnd"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AreaEllipse" "graphics.library/AreaEllipse()"
@{b}

    NAME@{ub}
        @{"AreaEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 157} -- add a ellipse to areainfo list for areafill.@{b}


    SYNOPSIS@{ub}
        error = @{"AreaEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 157}( rp, cx,   cy,   a,    b    )
        d0                   a1  d0:16 d1:16 d2:16 d3:16

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"AreaEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 157}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81})@{b}

    FUNCTION@{ub}
        Add an ellipse to the vector buffer. It will be draw when @{"AreaEnd()" LINK "AreaEnd"} is
        called.@{b}

    INPUTS@{ub}
        rp - pointer to a RastPort structure
        cx - x coordinate of the centerpoint relative to the rastport.
        cy - y coordinate of the centerpoint relative to the rastport.
        a  - the horizontal radius of the ellipse (note: a must be > 0)
        b  - the vertical radius of the ellipse (note: b must be > 0)@{b}

    RESULT@{ub}
        error - zero for success, or -1 if there is no space left in the
                vector list@{b}

    SEE ALSO@{ub}
        @{"AreaMove()" LINK "AreaMove"} @{"AreaDraw()" LINK "AreaDraw"} @{"AreaCircle()" LINK "AreaCircle"} @{"InitArea()" LINK "InitArea"} @{"AreaEnd()" LINK "AreaEnd"}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AreaEnd" "graphics.library/AreaEnd()"
@{b}

   NAME@{ub}
        @{"AreaEnd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 209} -- Process table of vectors and ellipses and produce areafill.@{b}


   SYNOPSIS@{ub}
        error = @{"AreaEnd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 209}(rp)
          d0            A1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"AreaEnd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 209}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} * );@{b}

   FUNCTION@{ub}
        Trigger the filling operation.
        Process the vector buffer and generate required
        fill into the raster planes. After the fill is complete, reinitialize
        for the next @{"AreaMove" LINK "AreaMove"} or @{"AreaEllipse" LINK "AreaEllipse"}. Use the raster set up by
        @{"InitTmpRas" LINK "InitTmpRas"} when generating an areafill mask.@{b}

   RESULT@{ub}
        error - zero for success, or -1 if an error occurred anywhere.@{b}

   INPUTS@{ub}
        rp - pointer to a RastPort structure which specifies where the filled
             regions will be rendered to.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitArea()" LINK "InitArea"} @{"AreaMove()" LINK "AreaMove"} @{"AreaDraw()" LINK "AreaDraw"} @{"AreaEllipse()" LINK "AreaEllipse"}  @{"InitTmpRas()" LINK "InitTmpRas"}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AreaMove" "graphics.library/AreaMove()"
@{b}

   NAME@{ub}
        @{"AreaMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 201} -- Define a new starting point for a new
                    shape in the vector list.@{b}


   SYNOPSIS@{ub}
        error =  @{"AreaMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 201}( rp,   x,     y)
         d0                a1  d0:16  d1:16

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"AreaMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 201}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81} );@{b}

   FUNCTION@{ub}
        Close  the last polygon and start another polygon
        at  (x,y). Add the necessary  points  to  vector
        buffer. Closing a polygon may result in the generation
        of another @{"AreaDraw()" LINK "AreaDraw"} to close previous polygon.
        Remember to have an initialized AreaInfo structure attached
        to the RastPort.@{b}

   INPUTS@{ub}
        rp  - points to a RastPort structure
        x,y - positions in the raster@{b}

   RETURNS@{ub}
        error - zero for success, or -1 if there is no space left in the
        vector list@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitArea()" LINK "InitArea"} @{"AreaDraw()" LINK "AreaDraw"} @{"AreaEllipse()" LINK "AreaEllipse"} @{"AreaEnd()" LINK "AreaEnd"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "AskFont" "graphics.library/AskFont()"
@{b}

   NAME@{ub}
        @{"AskFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 349} -- get the text attributes of the current font@{b}

   SYNOPSIS@{ub}
        @{"AskFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 349}(rp, textAttr)
                A1  A0

        void @{"AskFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 349}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *);@{b}

   FUNCTION@{ub}
        This function fills the text attributes structure with the
        attributes of the current font in the RastPort.@{b}

   INPUTS@{ub}
        rp       - the RastPort from which the text attributes are
                   extracted
        textAttr - the TextAttr structure to be filled.  Note that
                   there is no support for a TTextAttr.@{b}

   RESULT@{ub}
        The textAttr structure is filled with the RastPort's text
        attributes.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "AskSoftStyle" "graphics.library/AskSoftStyle()"
@{b}

   NAME@{ub}
        @{"AskSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 89} -- Get the soft style bits of the current font.@{b}

   SYNOPSIS@{ub}
        enable = @{"AskSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 89}(rp)
        D0                    A1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"AskSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 89}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        This function returns those style bits of the current font
        that are not intrinsic in the font itself, but
        algorithmically generated.  These are the bits that are
        valid to set in the enable mask for @{"SetSoftStyle()" LINK "SetSoftStyle"}.@{b}

   INPUTS@{ub}
        rp - the RastPort from which the font and style are extracted.@{b}

   RESULTS@{ub}
        enable - those bits in the style algorithmically generated.
                 Style bits that are not defined are also set.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetSoftStyle()" LINK "SetSoftStyle"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "AttachPalExtra" "graphics.library/AttachPalExtra()"
@{b}

   NAME@{ub}
       @{"AttachPalExtra" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 575} -- Allocate and attach a palette sharing structure to a

                          colormap. (V39)@{b}


   SYNOPSIS@{ub}
       status=@{"AttachPalExtra" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 575}( cm, vp)
                               a0  a1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"AttachPalExtra" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 575}( Struct ColorMap *, @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
        Allocates and attaches a PalExtra structure to a ColorMap.
        This is necessary for color palette sharing to work. The
        PalExtra structure will be freed by @{"FreeColorMap()" LINK "FreeColorMap"}.
        The set of available colors will be determined by the mode
        and depth of the viewport.@{b}

   INPUTS@{ub}
        cm  =  A pointer to a color map created by @{"GetColorMap()" LINK "GetColorMap"}.

        vp   = A pointer to the viewport structure associated with
               the ColorMap.@{b}

   RESULTS@{ub}
        status - 0 if sucessful, else an error number. The only currently
                 defined error number is out of memory (1).@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        This function is for use with custom ViewPorts and custom ColorMaps,
        as Intuition attaches a PalExtra to all of its Screens.
        If there is already a PalExtra associated with the ColorMap, then
        this function will do nothing.@{b}

   SEE ALSO@{ub}
        @{"GetColorMap()" LINK "GetColorMap"} @{"FreeColorMap()" LINK "FreeColorMap"} @{"ObtainPen()" LINK "ObtainPen"} @{"ObtainBestPenA()" LINK "ObtainBestPenA"}

@ENDNODE
@NODE "AttemptLockLayerRom" "graphics.library/AttemptLockLayerRom()"

                           *@{b}
   NAME@{ub}
        @{"AttemptLockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 461} -- Attempt to Lock Layer structure
                                         by ROM(gfx lib) code@{b}

   SYNOPSIS@{ub}
        gotit = @{"AttemptLockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 461}( layer )
         d0                           a5

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} AttempLockLayerRom( @{"struct Layer" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Query the current state of the lock on this Layer. If it is
        already locked then return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, could not lock. If the
        Layer was not locked then lock it and return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}.
        This call does not destroy any registers.
        This call nests so that callers in this chain will not lock
        themselves out.@{b}

   INPUTS@{ub}
        layer - pointer to Layer structure@{b}

   RESULT@{ub}
        gotit - @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} or @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} depending on whether the Layer was
                successfully locked by the caller.@{b}

   SEE ALSO@{ub}
        @{"LockLayerRom()" LINK "LockLayerRom"} @{"UnlockLayerRom()" LINK "UnlockLayerRom"}

@ENDNODE
@NODE "BestModeIDA" "graphics.library/BestModeIDA()"
@{b}

   NAME@{ub}
        @{"BestModeIDA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 733} -- calculate the best ModeID with given parameters (V39)
        @{"BestModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 738}  -- varargs stub for @{"BestModeIDA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 733}()@{b}

   SYNOPSIS@{ub}
        ID = @{"BestModeIDA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 733}(TagItems)
        d0               a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"BestModeIDA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 733}(@{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        ID = @{"BestModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 738}(Tag1, ...)

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"BestModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 738}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, ...);@{b}

   FUNCTION@{ub}
        To determine the best ModeID to fit the parameters set in the TagList.@{b}

   INPUTS@{ub}
        TagItems - A pointer to an array of TagItems.@{b}

   TAGS@{ub}
        @{"BIDTAG_DIPFMustHave" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 333} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Mask of DIPF_ flags
               (from DisplayInfo->PropertyFlags) that the returned ModeID
               must have.
               Default - @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}

        @{"BIDTAG_DIPFMustNotHave" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 335} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Mask of DIPF_ flags that the
               returned ModeID must not have.
               Default - @{"SPECIAL_FLAGS" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 331}

        @{"BIDTAG_ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 337} (@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *) - ViewPort for which a best-fit
               ModeID is sought.
               Default - @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}

        @{"BIDTAG_NominalWidth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 339} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}),
        @{"BIDTAG_NominalHeight" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 340} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - together make the aspect ratio.
               These values override the vp->DWidth and vp->DHeight values
               in the given ViewPort.
               Default - SourceID NominalDimensionInfo if @{"BIDTAG_SourceID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 352} is
               passed, or vp->DWidth and vp->DHeight if @{"BIDTAG_ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 337} is
               passed, or 640 x 200.

        @{"BIDTAG_DesiredWidth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 345} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}),
        @{"BIDTAG_DesiredHeight" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 346} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - Used to distinguish between two
               mode IDs with identical aspect ratios.
               Default - same values as NominalWidth and NominalHeight.

        @{"BIDTAG_Depth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 348} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - minimum the returned ModeID must support.
               Default - vp->RasInfo->BitMap->Depth if @{"BIDTAG_ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 337} is
               passed, else 1.

        @{"BIDTAG_MonitorID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 350} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - returned ModeID must use this monitor.
               Default - will not restrict the search to any particular monitor

        @{"BIDTAG_SourceID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 352} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Use this ModeID instead of a ViewPort.
               If specified, the DIPFMustHave mask is made up of the
               ((DisplayInfo->PropertyFlags of this ID & @{"SPECIAL_FLAGS" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 331}) |
                DIPFMustHave flags).
               Default - VPModeID(vp) if @{"BIDTAG_ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 337} was passed, else the
               DIPFMustHave and DIPFMustNotHave masks are left unchanged.

        @{"BIDTAG_RedBits" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 357} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}),
        @{"BIDTAG_BlueBits" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 358} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}),
        BIDTAG_Greenits (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Minimum bits per gun the resultant
               ModeID must support.
               Default - 4 bits per gun.@{b}

   RESULTS@{ub}
        ID      - ID of the best mode to use, or @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} if a match could
                  not be found.@{b}

   NOTES@{ub}
        This function takes into account the Compatability of the Monitor
        being matched to, and the source ViewPort or ModeID.
        Incompatibilitys will cause a result of @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17}.

        @{"BIDTAG_NominalWidth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 339}, @{"BIDTAG_NominalHeight" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 340},
        @{"BIDTAG_DesiredWidth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 345}, @{"BIDTAG_DesiredHeight" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 346}, must all be non-0.

        The comparisons are made against the DimensionInfo->Nominal values.
        ie, this will not return a best fit against overscan dimensions.@{b}

   EXAMPLE@{ub}
        IFF Display Program with a @{"HAM" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 130} image, to be displayed in the same
        monitor type as the Workbench ViewPort.

        ID = @{"BestModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 738}(@{"BIDTAG_NominalWidth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 339}, IFFImage->Width,
                        @{"BIDTAG_NominalHeight" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 340}, IFFImage->Height,
                        @{"BIDTAG_Depth" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 348}, IFFImage->Depth,
                        @{"BIDTAG_DIPFMustHave" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 333}, @{"DIPF_IS_HAM" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 82},
                        @{"BIDTAG_MonitorID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 350}, (@{"GetVPModeID" LINK "GetVPModeID"}(WbVP) & @{"MONITOR_ID_MASK" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 33}),
                        @{"TAG_END" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 40});

        To make an interlace version of a ViewPort:

        ID = @{"BestModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 738}(@{"BIDTAG_ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 337}, ThisViewPort,
                        BIDTAG_MustHave, DIFP_IS_LACE,
                        @{"TAG_END" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 40});@{b}

   SEE ALSO@{ub}
        <@{"graphics/modeid.h" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File"}> <@{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}>

@ENDNODE
@NODE "BitMapScale" "graphics.library/BitMapScale()"
@{b}

   NAME@{ub}
        @{"BitMapScale" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 477} -- Perform raster scaling on a bit map. (V36)@{b}

   SYNOPSIS@{ub}
        @{"BitMapScale" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 477}(bitScaleArgs)
                    A0

        void @{"BitMapScale" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 477}(@{"struct BitScaleArgs" LINK "gg:doc/NDK/Guide/Include/graphics/scale.h/File" 17} *);@{b}

   FUNCTION@{ub}
        Scale a source bit map to a non-overlapping destination
        bit map.@{b}

   INPUTS@{ub}
        bitScaleArgs - structure of parameters describing scale:
            bsa_SrcX, bsa_SrcY - origin of the source bits.
            bsa_SrcWidth, bsa_SrcHeight - number of bits to scale from in x
            and y.
            bsa_DestX, bsa_DestY - origin of the destination.
            bsa_DestWidth, bsa_DestHeight - resulting number of bits in x
            and y.  NOTE: these values are set by this function.
            bsa_XSrcFactor:bsa_XDestFactor - equivalent to the ratio
                srcWidth:destWidth, but not necessarily the same
                numbers.  Each must be in the range 1..16383.
            bsa_YSrcFactor:bsa_YDestFactor - equivalent to the ratio
                srcHeight:destHeight, but not necessarily the same
                numbers.  Each must be in the range 1..16383.
            bsa_SrcBitMap - source of the bits to scale.
            bsa_DestBitMap - destination for the bits to scale.  This had
                better be big enough!
            bsa_Flags - future scaling options.  Set it to zero!
            bsa_XDDA, bsa_YDDA - for future use.  Need not be set by user.
            bsa_Reserved1, bsa_Reserved2 - for future use.  Need not be set.@{b}

   RESULT@{ub}
        The destWidth, destHeight fields are set by this function as
        described above.@{b}

   NOTES@{ub}
        o   This function may use the blitter.
        o   Overlapping source and destination bit maps are not
            supported.
        o   No check is made to ensure destBitMap is big enough: use
            @{"ScalerDiv" LINK "ScalerDiv"} to calculate a destination dimension.@{b}

   BUGS@{ub}
        o   This function does not use the HighRes Agnus 'Big Blit'
            facility. You should not use XSrcFactor == XDestFactor,
            where SrcWidth or DestWidth > 1024.

        o   Also, the blitter is used when expanding in the Y direction.
            You should not expand in the Y direction if
            ((DestX & 0xf) + DestWidth) >= 1024 pixels. (Up to 1008 pixels
            is always safe).@{b}

   SEE ALSO@{ub}
        @{"ScalerDiv()" LINK "ScalerDiv"}  @{"graphics/scale.h" LINK "gg:doc/NDK/Guide/Include/graphics/scale.h/File"}

@ENDNODE
@NODE "BltBitMap" "graphics.library/BltBitMap()"
@{b}

   NAME@{ub}
        @{"BltBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 53} -- Move a rectangular region of bits in a BitMap.@{b}

   SYNOPSIS@{ub}
        planecnt = @{"BltBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 53}(SrcBitMap, SrcX, SrcY, DstBitMap,
        D0                   A0         D0:16 D1:16 A1
            DstX, DstY, SizeX, SizeY, Minterm, Mask [, TempA])
            D2:16 D3:16 D4:16  D5:16  D6:8     D7:8   [A2]

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"BltBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 53}(@{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *,
            @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} *);@{b}

   FUNCTION@{ub}
        Perform non-destructive blits to move a rectangle from one
        area in a BitMap to another area, which can be on a different
        BitMap.
        This blit is assumed to be friendly: no error conditions (e.g.
        a rectangle outside the BitMap bounds) are tested or reported.@{b}

   INPUTS@{ub}
        SrcBitMap, DstBitMap - the BitMap(s) containing the
              rectangles
            - the planes copied from the source to the destination are
              only those whose plane numbers are identical and less
              than the minimum Depth of either BitMap and whose Mask
              bit for that plane is non-zero.
            - as a special case, if a plane pointer in the SrcBitMap
              is zero, it acts as a pointer to a plane of all zeros, and
              if the plane pointer is 0xffffffff, it acts as a pointer
              to a plane of all ones.  (Note: new for V36)
            - SrcBitMap and DstBitMap can be identical if they point
              to actual planes.
        SrcX, SrcY - the x and y coordinates of the upper left corner
            of the source rectangle.  Valid range is positive
            signed integer such that the raster word's offset
            0..(32767-Size)
        DstX, DstY - the x and y coordinates of the upper left
            corner of the destination for the rectangle.  Valid
            range is as for Src.
        SizeX, SizeY - the size of the rectangle to be moved.  Valid
            range is (X: 1..976; Y: 1..1023 such that final raster
            word's offset is 0..32767)
        Minterm - the logic function to apply to the rectangle when
            A is non-zero (i.e. within the rectangle).  B is the
            source rectangle and C, D is the destination for the
            rectangle.
            - $0C0 is a vanilla copy
            - $030 inverts the source before the copy
            - $050 ignores the source and inverts the destination
            - see the hardware reference manual for other combinations
        Mask - the write mask to apply to this operation.  Bits set
            indicate the corresponding planes (if not greater than
            the minimum plane count) are to participate in the
            operation.  Typically this is set to 0xff.
        TempA - If the copy overlaps exactly to the left or right
            (i.e. the scan line addresses overlap), and TempA is
            non-zero, it points to enough chip accessible memory
            to hold a line of A source for the blit (ie CHIP RAM).
            @{"BltBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 53} will allocate (and free) the needed TempA if
            none is provided and one is needed.  Blit overlap is
            determined from the relation of the first non-masked
            planes in the source and destination bit maps.@{b}

   RESULTS@{ub}
        planecnt - the number of planes actually involved in the blit.@{b}

   NOTES@{ub}
        o   This function may use the blitter.@{b}

   SEE ALSO@{ub}
        @{"ClipBlit()" LINK "ClipBlit"}  @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}  @{"hardware/blit.h" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File"}

@ENDNODE
@NODE "BltBitMapRastPort" "graphics.library/BltBitMapRastPort()"
@{b}

   NAME@{ub}
        @{"BltBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 437} -- Blit from source bitmap to destination rastport.@{b}

   SYNOPSIS@{ub}
        error = @{"BltBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 437}
                (srcbm, srcx, srcy, destrp, destX, destY, sizeX, sizeY, minterm)
         D0      A0     D0    D1    A1      D2     D3     D4     D5     D6

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"BltBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 437}
             (@{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54},
              @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62});@{b}

   FUNCTION@{ub}
        Blits from source bitmap to position specified in destination rastport
        using minterm.@{b}

   INPUTS@{ub}
        srcbm   - a pointer to the source bitmap
        srcx    - x offset into source bitmap
        srcy    - y offset into source bitmap
        destrp  - a pointer to the destination rastport
        destX   - x offset into dest rastport
        destY   - y offset into dest rastport
        sizeX   - width of blit in pixels
        sizeY   - height of blit in rows
        minterm - minterm to use for this blit@{b}

   RESULT@{ub}
        @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"BltMaskBitMapRastPort()" LINK "BltMaskBitMapRastPort"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "BltClear" "graphics.library/BltClear()"
@{b}

   NAME@{ub}

        @{"BltClear" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 233} - Clear a block of memory words to zero.@{b}

   SYNOPSIS@{ub}
        @{"BltClear" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 233}( memBlock, bytecount, flags )
                    a1         d0       d1

        void @{"BltClear" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 233}( void *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} );@{b}

   FUNCTION@{ub}
        For memory that is local and blitter accessible, the most
        efficient way to clear a range of memory locations is
       to use the system's most efficient data mover, the blitter.
       This command accepts the starting location and count and clears
       that block to zeros.@{b}

   INPUTS@{ub}
        memBloc - pointer to local memory to be cleared
                  memBlock is assumed to be even.
        flags   - set bit 0 to force function to wait until
                  the blit is done.
                  set bit 1 to use row/bytesperrow.

        bytecount - if (flags & 2) == 0 then
                                even number of bytes to clear.
                        else
                                low 16 bits is taken as number of bytes
                                per row and upper 16 bits taken as
                                number of rows.

        This function is somewhat hardware dependent. In the rows/bytesperrow
        mode (with the pre-ECS blitter) rows must be <- 1024. In bytecount mode
        multiple runs of the blitter may be used to clear all the memory.

        Set bit 2 to use the upper 16 bits of the Flags as the data to fill
        memory with instead of 0 (V36).@{b}

   RESULT@{ub}
        The block of memory is initialized.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}

@ENDNODE
@NODE "BltMaskBitMapRastPort" "graphics.library/BltMaskBitMapRastPort()"
@{b}

   NAME@{ub}
        @{"BltMaskBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 457} -- blit from source bitmap to destination rastport
        with masking of source image.@{b}

   SYNOPSIS@{ub}
        @{"BltMaskBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 457}
            (srcbm, srcx, srcy, destrp, destX, destY, sizeX, sizeY,
             A0     D0    D1    A1      D2     D3     D4     D5
             minterm, bltmask)
             D6       A2

        void @{"BltMaskBitMapRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 457}
             (@{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54},
              @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49});@{b}

   FUNCTION@{ub}
        Blits from source bitmap to position specified in destination rastport
        using bltmask to determine where source overlays destination, and
        minterm to determine whether to copy the source image "as is" or
        to "invert" the sense of the source image when copying. In either
        case, blit only occurs where the mask is non-zero.@{b}

   INPUTS@{ub}
        srcbm   - a pointer to the source bitmap
        srcx    - x offset into source bitmap
        srcy    - y offset into source bitmap
        destrp  - a pointer to the destination rastport
        destX   - x offset into dest rastport
        destY   - y offset into dest rastport
        sizeX   - width of blit in pixels
        sizeY   - height of blit in rows
        minterm - either (@{"ABC" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 30}|@{"ABNC" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 31}|@{"ANBC" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 32}) if copy source and blit thru mask
                  or     (@{"ANBC" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 32})          if invert source and blit thru mask
        bltmask - pointer to the single bit-plane mask, which must be the
                  same size and dimensions as the planes of the
                  source bitmap.@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"BltBitMapRastPort()" LINK "BltBitMapRastPort"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "BltPattern" "graphics.library/BltPattern()"
@{b}

   NAME@{ub}
        @{"BltPattern" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 241} --  Using standard drawing rules for areafill,
                                         blit through a mask.@{b}

   SYNOPSIS@{ub}
       @{"BltPattern" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 241}(rp, mask, xl, yl, maxx, maxy, bytecnt)
                  a1,  a0   d0  d1   d2   d3     d4

        void @{"BltPattern" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 241}
           (@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, void *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
       Blit using drawmode,areafill pattern, and mask
       at position rectangle (xl,yl) (maxx,maxy).@{b}

   INPUTS@{ub}
       rp    -  points to the destination RastPort for the blit.
       mask  -  points to 2 dimensional mask if needed
                if mask == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} then use a rectangle.
       xl,yl -  coordinates of upper left of rectangular region in RastPort
       maxx,maxy - points to lower right of rectangular region in RastPort
       bytecnt - BytesPerRow for mask@{b}

   RESULT@{ub}@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"}

@ENDNODE
@NODE "BltTemplate" "graphics.library/BltTemplate()"
@{b}

   NAME@{ub}
        @{"BltTemplate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 57} -- Cookie cut a shape in a rectangle to the RastPort.@{b}

   SYNOPSIS@{ub}
        @{"BltTemplate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 57}(SrcTemplate, SrcX, SrcMod, rp,
                    A0           D0:16  D1:16  A1
            DstX,  DstY, SizeX, SizeY)
            D2:16  D3:16 D4:16  D5:16

        void @{"BltTemplate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 57}(@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *,
             @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        This function draws the image in the template into the
        RastPort in the current color and drawing mode at the
        specified position.  The template is assumed not to overlap
        the destination.
        If the template falls outside the RastPort boundary, it is
        truncated to that boundary.

        Note: the SrcTemplate pointer should point to the "nearest" word
           (rounded down) of the template mask. Fine alignment of the mask
           is achieved by setting the SrcX bit offseet within the range
           of 0 to 15 decimal.@{b}

   INPUTS@{ub}
        SrcTemplate  - pointer to the first (nearest) word of the template mask.
        SrcX         - x bit offset into the template mask (range 0..15).
        SrcMod       - number of bytes per row in template mask.
        rp           - pointer to destination RastPort.
        DstX, DstY   - x and y coordinates of the upper left
                       corner of the destination for the blit.
        SizeX, SizeY - size of the rectangle to be used as the
                       template.@{b}

   NOTES@{ub}
        o   This function may use the blitter.@{b}

   SEE ALSO@{ub}
        @{"BltBitMap()" LINK "BltBitMap"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "CalcIVG" "graphics.library/CalcIVG()"
@{b}

   NAME@{ub}
        @{"CalcIVG" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 571} -- Calculate the number of blank lines above a ViewPort (V39)@{b}

   SYNOPSIS@{ub}
        count = @{"CalcIVG" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 571}(View, ViewPort)
         d0.w           a0    a1

        @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} @{"CalcIVG" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 571}(@{"struct View" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 64} *, @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
        To calculate the maximum number of blank lines above a viewport needed to
        load all the copper instructions, after accounting for the viewport
        bandwidth and size.@{b}

   INPUTS@{ub}
        View       - pointer to the View
        ViewPort   - pointer to the ViewPort you are interested in.@{b}

   RESULT@{ub}
        count      - the number of ViewPort resolution scan lines needed to
                     execute all the copper instructions for ViewPort,
                     or 0 if any error.@{b}

   NOTES@{ub}
        The number of copper instructions comes from the vp->vp_DspIns list.
        Although there may be other copper instructions in the final list (from
        UCopIns, SprIns and ClrIns) they are currently ignored for this
        function. This also means that if the ViewPort has never been made
        (for example, the ViewPort of an intuition screen was opened behind)
        then vp->vp_DspIns is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.

        Although @{"CalcIVG" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 571}() returns the true number of lines needed by the
        copper, intuition still maintains an inter-screen gap of 3 non-laced
        lines (6 interlaced). Therefore, for intuition screens use:
        @{"MAX" LINK "gg:doc/NDK/Guide/Include/clib/macros.h/File" 13}(@{"CalcIVG" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 571}(v, vp), (islaced ? 6 : 3))@{b}


   SEE ALSO@{ub}
        @{"GfxNew()" LINK "GfxNew"}  @{"VideoControl()" LINK "VideoControl"}  @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "CBump" "graphics.library/CBump()"
@{b}
   NAME@{ub}
        @{"CBump" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 277} -  increment user copper list pointer (bump to next position in list).@{b}

   SYNOPSIS@{ub}
        @{"CBump" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 277}( c )
              a1

        void @{"CBump" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 277}( @{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89} * );@{b}

   FUNCTION@{ub}
        Increment pointer to space for next instruction in user copper list.@{b}

   INPUTS@{ub}
        c - pointer to UCopList structure@{b}

   RESULTS@{ub}
        User copper list pointer is incremented to next position.
        Pointer is repositioned to next user copperlist instruction block
        if the current block is full.

            Note: @{"CBump" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 277} is usually invoked for the programmer as part of the
                  macro definitions @{"CWAIT" LINK "CWAIT"} or @{"CMOVE" LINK "CMOVE"}.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"CINIT()" LINK "CINIT"} @{"CWAIT()" LINK "CWAIT"} @{"CMOVE()" LINK "CMOVE"} @{"CEND()" LINK "CEND"} @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "CEND" "graphics.library/CEND()"
@{b}
   NAME@{ub}
        @{"CEND" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 66} -- Terminate user copper list.@{b}

   SYNOPSIS@{ub}
        @{"CEND" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 66}( c )

        @{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89} *c;@{b}

   FUNCTION@{ub}
        Add instruction to terminate user copper list.@{b}

   INPUTS@{ub}
        c - pointer to UCopList structure@{b}

   RESULTS@{ub}
        This is actually a macro that calls the macro @{"CWAIT" LINK "CWAIT"}(c,10000,255)
        10000 is a magical number that the graphics.library uses.
        I hope display technology doesn't catch up too fast!@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"CINIT()" LINK "CINIT"} @{"CWAIT()" LINK "CWAIT"} @{"CMOVE()" LINK "CMOVE"} @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "ChangeExtSpriteA" "graphics.library/ChangeExtSpriteA()"
@{b}

   NAME@{ub}
       @{"ChangeExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 702} -- Change the sprite image pointer. (V39)@{b}

   SYNOPSIS@{ub}
       @{"ChangeExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 702}( vp, oldsprite, newsprite, tags)
                          a0  a1         a2         a3

        success=@{"ChangeExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 702}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"struct ExtSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 25} *,
                        @{"struct ExtSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 25} *, struct TagList *);

        success=@{"ChangeExtSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 707}(vp,old_sp,new_sp,tag,....);@{b}

   FUNCTION@{ub}
        Attempt to change which sprite is displayed for a given
        sprite engine.@{b}

   INPUTS@{ub}
       vp - pointer to ViewPort structure that this sprite is
                  relative to,  or 0 if relative only top of View
        oldsprite - pointer the old ExtSprite structure
        newsprite - pointer to the new ExtSprite structure.@{b}

   RESULTS@{ub}
        success - 0 if there was an error.@{b}
   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"AllocSpriteDataA()" LINK "AllocSpriteDataA"}
        @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "ChangeSprite" "graphics.library/ChangeSprite()"
@{b}

   NAME@{ub}
       @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313} -- Change the sprite image pointer.@{b}

   SYNOPSIS@{ub}
       @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313}( vp, s, newdata)
                     a0  a1   a2

        void @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"struct SimpleSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 17} *, void * )@{b}

   FUNCTION@{ub}
        The sprite image is changed to use the data starting at newdata@{b}

   INPUTS@{ub}
       vp - pointer to ViewPort structure that this sprite is
                  relative to,  or 0 if relative only top of View
        s - pointer to SimpleSprite structure
        newdata - pointer to data structure of the following form.
                struct spriteimage
                {
                    @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}    posctl[2]; /* used by simple sprite machine*/
                    @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}    data[height][2];   /* actual sprite image */
                    @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}    reserved[2];       /* initialized to */
                                                     /*  0x0,0x0 */
                };
        The programmer must initialize reserved[2].  Spriteimage must be
        in CHIP memory. The height subfield of the SimpleSprite structure
        must be set to reflect the height of the new spriteimage BEFORE
        calling @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313}(). The programmer may allocate two sprites to
        handle a single attached sprite.  After @{"GetSprite()" LINK "GetSprite"}, @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313}(),
        the programmer can set the @{"SPRITE_ATTACHED" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 15} bit in posctl[1] of the
        odd numbered sprite.
        If you need more than 8 sprites, look up VSprites in the
        graphics documentation.@{b}

   RESULTS@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 313}() @{"MoveSprite()" LINK "MoveSprite"} @{"AddVSprite()" LINK "AddVSprite"} @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "ChangeVPBitMap" "graphics.library/ChangeVPBitMap()"
@{b}

   NAME@{ub}
       @{"ChangeVPBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 645} -- change display memory address for multi-buffered
                          animation (V39)@{b}

   SYNOPSIS@{ub}
       @{"ChangeVPBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 645}(vp,bm,db)
                       a0 a1 a2

        void @{"ChangeVPBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 645}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"struct DBufInfo" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 279} *);@{b}

   FUNCTION@{ub}
        Changes the area of display memory which will be displayed in a
        viewport. This can be used to implement double (or triple)
        buffering, a method of achieving smooth animation.@{b}

   INPUTS@{ub}
        vp  =  a pointer to a viewport
       bm  = a pointer to a BitMap structure. This BitMap structure must be
              of the same layout as the one attached to the viewport (same
              depth, alignment, and BytesPerRow).
        db  =  A pointer to a DBufInfo.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        This will set the vp->RasInfo->BitMap field to the bm pointer which is
        passed.

        When using the synchronization features, you MUST carefully insure that
        all messages have been replied to before calling @{"FreeDBufInfo" LINK "FreeDBufInfo"} or
        calling @{"ChangeVPBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 645} with the same DBufInfo.@{b}


   SEE ALSO@{ub}
        @{"AllocDBufInfo()" LINK "AllocDBufInfo"} @{"AllocBitMap()" LINK "AllocBitMap"}

@ENDNODE
@NODE "CINIT" "graphics.library/CINIT()"
@{b}

   NAME@{ub}
        @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63} -- Initialize user copperlist to accept intermediate
                 user copper instructions.@{b}

   SYNOPSIS@{ub}
        cl = @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63}( ucl , n )

        cl = @{"UCopperListInit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 429}( ucl , n )
                              a0    d0

        @{"struct CopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 62} *@{"UCopperListInit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 429}( @{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} );@{b}

   FUNCTION@{ub}
        Allocates and/or initialize copperlist structures/buffers
        internal to a UCopList structure.

        This is a macro that calls UCopListInit. You must pass a
        (non-initialized) UCopList to @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63} (@{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63} will @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} allocate
        a new UCopList if ucl==0 ). If (ucl != 0) it will initialize the
        intermediate data buffers internal to a UCopList.

        The maximum number of intermediate copper list instructions
        that these internal CopList data buffers contain is specified
        as the parameter n.@{b}

   INPUTS@{ub}
        ucl - pointer to UCopList structure
        n - number of instructions buffer must be able to hold@{b}

   RESULTS@{ub}
        cl- a pointer to a buffer which will accept n intermediate copper
            instructions.

        NOTE: this is @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} a UCopList pointer, rather a pointer to the
              UCopList's->FirstCopList sub-structure.@{b}

   BUGS@{ub}
        @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63} will not actually allocate a new UCopList if ucl==0.
        Instead you must allocate a block @{"MEMF_PUBLIC" LINK "gg:doc/NDK/Guide/Include/exec/memory.h/File" 69}|@{"MEMF_CLEAR" LINK "gg:doc/NDK/Guide/Include/exec/memory.h/File" 76}, the
        sizeof(@{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89}) and pass it to this function.

        The system's @{"FreeVPortCopLists" LINK "FreeVPortCopLists"} function will take care of
        deallocating it if they are called.

        Prior to release V36 the  @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63} macro had { } braces surrounding
        the definition, preventing the proper return of the result value.
        These braces have been removed for the V36 include definitions.@{b}

   SEE ALSO@{ub}
        @{"CINIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 63}() @{"CMOVE()" LINK "CMOVE"} @{"CEND()" LINK "CEND"} @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "ClearEOL" "graphics.library/ClearEOL()"
@{b}

   NAME@{ub}
        @{"ClearEOL" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 61} -- Clear from current position to end of line.@{b}

   SYNOPSIS@{ub}
        @{"ClearEOL" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 61}(rp)
                 A1

        void @{"ClearEOL" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 61}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Clear a rectangular swath from the current position to the
        right edge of the rastPort.  The height of the swath is taken
        from that of the current text font, and the vertical
        positioning of the swath is adjusted by the text baseline,
        such that text output at this position would lie wholly on
        this newly cleared area.
        Clearing consists of setting the color of the swath to zero,
        or, if the DrawMode is 2, to the BgPen.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure@{b}

   RESULT@{ub}@{b}

   NOTES@{ub}
        o   This function may use the blitter.@{b}

   SEE ALSO@{ub}
        @{"Text()" LINK "Text"}  @{"ClearScreen()" LINK "ClearScreen"}  @{"SetRast()" LINK "SetRast"}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ClearRectRegion" "graphics.library/ClearRectRegion()"
@{b}

   NAME@{ub}
        @{"ClearRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 381} -- Perform 2d CLEAR operation of rectangle
                        with region, leaving result in region.@{b}

   SYNOPSIS@{ub}
        status = @{"ClearRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 381}(region,rectangle)
         d0                       a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"ClearRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 381}(@{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Rectangle" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Clip away any portion of the region that exists inside
        of the rectangle. Leave the result in region.@{b}

   INPUTS@{ub}
        region - pointer to Region structure
        rectangle - pointer to Rectangle structure@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AndRectRegion()" LINK "AndRectRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "ClearRegion" "graphics.library/ClearRegion()"
@{b}

   NAME@{ub}
       @{"ClearRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 385} -- Remove all rectangles from region.@{b}

   SYNOPSIS@{ub}
       @{"ClearRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 385}(region)
                     a0

        viod @{"ClearRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 385}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} * );@{b}

   FUNCTION@{ub}
       Clip away all rectangles in the region leaving nothing.@{b}

   INPUTS@{ub}
       region - pointer to Region structure@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"NewRegion()" LINK "NewRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "ClearScreen" "graphics.library/ClearScreen()"
@{b}

   NAME@{ub}
        @{"ClearScreen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 65} -- Clear from current position to end of RastPort.@{b}

   SYNOPSIS@{ub}
        @{"ClearScreen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 65}(rp)
                    A1

        void @{"ClearScreen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 65}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Clear a rectangular swath from the current position to the
        right edge of the rastPort with @{"ClearEOL" LINK "ClearEOL"}, then clear the rest
        of the screen from just beneath the swath to the bottom of
        the rastPort.
        Clearing consists of setting the color of the swath to zero,
        or, if the DrawMode is 2, to the BgPen.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure@{b}

   NOTES@{ub}
        o   This function may use the blitter.@{b}

   SEE ALSO@{ub}
        @{"ClearEOL()" LINK "ClearEOL"}  @{"Text()" LINK "Text"}  @{"SetRast()" LINK "SetRast"}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ClipBlit" "graphics.library/ClipBlit()"
@{b}

   NAME@{ub}
        @{"ClipBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 401}  --  Calls @{"BltBitMap()" LINK "BltBitMap"} after accounting for windows@{b}

   SYNOPSIS@{ub}
        @{"ClipBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 401}(Src, SrcX, SrcY, Dest, DestX, DestY, XSize, YSize, Minterm)
                 A0   D0    D1    A1    D2     D3     D4     D5     D6

        void @{"ClipBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 401}
             (@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54},
              @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62});@{b}

   FUNCTION@{ub}
        Performs the same function as @{"BltBitMap()" LINK "BltBitMap"}, except that it
        takes into account the Layers and ClipRects of the layer library,
        all of which are (and should be) transparent to you.  So, whereas
        @{"BltBitMap()" LINK "BltBitMap"} requires pointers to BitMaps, @{"ClipBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 401} requires pointers to
        the RastPorts that contain the Bitmaps, Layers, etcetera.

        If you are going to blit blocks of data around via the RastPort of your
        Intuition Window, you must call this routine (rather than @{"BltBitMap()" LINK "BltBitMap"}).

        Either the Src RastPort, the Dest RastPort, both, or neither, can have
        Layers. This routine takes care of all cases.

        See @{"BltBitMap()" LINK "BltBitMap"} for a thorough explanation.@{b}

   INPUTS@{ub}
        Src          = pointer to the RastPort of the source for your blit
        SrcX, SrcY   = the topleft offset into Src for your data
        Dest         = pointer to the RastPort to receive the blitted data
        DestX, DestY = the topleft offset into the destination RastPort
        XSize        = the width of the blit (must be ta least 1)
        YSize        = the height of the blit (must be at least 1)
        Minterm      = the boolean blitter function, where @{"SRCB" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 58} is associated
                       with the Src RastPort and @{"SRCC" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 57} goes to the Dest RastPort@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"BltBitMap()" LINK "BltBitMap"}

@ENDNODE
@NODE "CloseFont" "graphics.library/CloseFont()"
@{b}

   NAME@{ub}
        @{"CloseFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 85} -- Release a pointer to a system font.@{b}

   SYNOPSIS@{ub}
        @{"CloseFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 85}(font)
                  A1

        void @{"CloseFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 85}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *);@{b}

   FUNCTION@{ub}
        This function indicates that the font specified is no longer
        in use.  It is used to close a font opened by @{"OpenFont" LINK "OpenFont"}, so
        that fonts that are no longer in use do not consume system
        resources.@{b}

   INPUTS@{ub}
        font -  a font pointer as returned by @{"OpenFont()" LINK "OpenFont"} or @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"OpenFont()" LINK "OpenFont"}  @{"diskfont.library/OpenDiskFont" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "CloseMonitor" "graphics.library/CloseMonitor()"
@{b}

   NAME@{ub}
        @{"CloseMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 510} -- close a MonitorSpec (V36)@{b}

   SYNOPSIS@{ub}
        error = @{"CloseMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 510}( monitor_spec )
        d0                    a0

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"CloseMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 510}( @{"struct MonitorSpec" LINK "gg:doc/NDK/Guide/Include/graphics/monitor.h/File" 25} * );@{b}

   FUNCTION@{ub}
        Relinquish access to a MonitorSpec.@{b}

   INPUTS@{ub}
        monitor_spec - a pointer to a MonitorSpec opened via @{"OpenMonitor()" LINK "OpenMonitor"},
        or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.@{b}

   RESULTS@{ub}
        error - @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if MonitorSpec closed uneventfully.
                @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if MonitorSpec could not be closed.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"OpenMonitor()" LINK "OpenMonitor"}

@ENDNODE
@NODE "CMOVE" "graphics.library/CMOVE()"
@{b}

   NAME@{ub}
        @{"CMOVE" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 64} -- append copper move instruction to user copper list.@{b}

   SYNOPSIS@{ub}
        @{"CMOVE" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 64}( c , a , v )

        @{"CMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 281}( c , a , v )
              a1  d0  d1
        @{"CBump" LINK "CBump"}( c )
              a1

        void @{"CMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 281}( @{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89} *, void *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} );@{b}

   FUNCTION@{ub}
        Add instruction to move value v to hardware register a.@{b}

   INPUTS@{ub}
        c - pointer to UCopList structure
        a - hardware register
        v - 16 bit value to be written@{b}

   RESULTS@{ub}
        This is actually a macro that calls @{"CMove" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 281}(c,&a,v)
        and then calls @{"CBump" LINK "CBump"}(c) to bump the local pointer
        to the next instruction. Watch out for macro side affects.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"CINIT()" LINK "CINIT"} @{"CWAIT()" LINK "CWAIT"} @{"CEND()" LINK "CEND"} @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "CoerceMode" "graphics.library/CoerceMode()"
@{b}

   NAME@{ub}
        @{"CoerceMode" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 641} -- calculate ViewPort mode coercion (V39)@{b}

   SYNOPSIS@{ub}
        ID = @{"CoerceMode" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 641}(RealViewPort, MonitorID, Flags);
        d0              a0            d0         d1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"CoerceMode" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 641}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        To determine the best mode in the MonitorID to coerce RealViewPort to,
        given the restrictions set in Flags.@{b}

   INPUTS@{ub}
        RealViewPort - ViewPort to coerce
        MonitorID    - Montor number to coerce to (ie a mode masked with
                       @{"MONITOR_ID_MASK" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 33}).
        Flags        - @{"PRESERVE_COLORS" LINK "gg:doc/NDK/Guide/Include/graphics/coerce.h/File" 20} - keep the number of bitplanes
                       in the ViewPort.
                       @{"AVOID_FLICKER" LINK "gg:doc/NDK/Guide/Include/graphics/coerce.h/File" 23} - do not coerce to an interlace mode@{b}

   RESULTS@{ub}
        ID      - ID of the best mode to coerce to, or @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} if could not
                  coerce (see NOTES).@{b}

   NOTES@{ub}
        This function takes into account the compatibility of the Monitor
        being coerced to, and the ViewPort that is being coerced.
        Incompatibilities will cause a result of @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17}.@{b}

   EXAMPLE@{ub}
        newmode = @{"CoerceMode" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 641}(vp, @{"VGA_MONITOR_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 88}, @{"PRESERVE_COLORS" LINK "gg:doc/NDK/Guide/Include/graphics/coerce.h/File" 20});@{b}

   SEE ALSO@{ub}
        <@{"graphics/coerce.h" LINK "gg:doc/NDK/Guide/Include/graphics/coerce.h/File"}> <@{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}>

@ENDNODE
@NODE "CopySBitMap" "graphics.library/CopySBitMap()"
@{b}

   NAME@{ub}
       @{"CopySBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 333} --   Syncronize Layer window with contents of
                                                Super BitMap@{b}

   SYNOPSIS@{ub}
       @{"CopySBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 333}( layer )
                     a0

        void @{"CopySBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 333}(@{"struct Layer" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File" 27} *);@{b}

   FUNCTION@{ub}
        This is the inverse of @{"SyncSBitMap" LINK "SyncSBitMap"}.
       Copy all bits from SuperBitMap to Layer bounds.
        This is used for those functions that do not
        want to deal with the ClipRect structures but do want
        to be able to work with a SuperBitMap Layer.@{b}

   INPUTS@{ub}
        layer - pointer to a SuperBitMap Layer
            The Layer must already be locked by the caller.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"LockLayerRom()" LINK "LockLayerRom"} @{"SyncSBitMap()" LINK "SyncSBitMap"}

@ENDNODE
@NODE "CWAIT" "graphics.library/CWAIT()"
@{b}

   NAME@{ub}
        @{"CWAIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 65} -- Append copper wait instruction to user copper list.@{b}

   SYNOPSIS@{ub}
        @{"CWAIT" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 65}( c , v , h )

        @{"CWait" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 285}( c , v , h )
               a1  d0  d1
        @{"CBump" LINK "CBump"}( c )
              a1

        void @{"CWait" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 285}( @{"struct UCopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 89} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54})@{b}

   FUNCTION@{ub}
        Add instruction to wait for vertical beam position v and
        horizontal position h to this intermediate copper list.@{b}

   INPUTS@{ub}
        c - pointer to UCopList structure
        v - vertical beam position (relative to top of viewport)
        h - horizontal beam position@{b}

   RESULTS@{ub}
        this is actually a macro that calls @{"CWait" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 285}(c,v,h)
        and then calls @{"CBump" LINK "CBump"}(c) to bump the local pointer
        to the next instruction.@{b}

   BUGS@{ub}
        User waiting for horizontal values of greater than 222 decimal
        is illegal.@{b}

   SEE ALSO@{ub}
        @{"CINIT()" LINK "CINIT"} @{"CMOVE()" LINK "CMOVE"} @{"CEND()" LINK "CEND"} @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "DisownBlitter" "graphics.library/DisownBlitter()"
@{b}

   NAME@{ub}
       @{"DisownBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 341} -- return blitter to free state.@{b}


   SYNOPSIS@{ub}
       @{"DisownBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 341}()

        void @{"DisownBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 341}( void );@{b}

   FUNCTION@{ub}
        Free blitter up for use by other blitter users.@{b}

   INPUTS@{ub}@{b}

   RETURNS@{ub}@{b}

   SEE ALSO@{ub}
       @{"OwnBlitter()" LINK "OwnBlitter"}

@ENDNODE
@NODE "DisposeRegion" "graphics.library/DisposeRegion()"
@{b}

   NAME@{ub}
       @{"DisposeRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 389} -- Return all space for this region to free
                         memory pool.@{b}

   SYNOPSIS@{ub}
       @{"DisposeRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 389}(region)
                      a0

        void @{"DisposeRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 389}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} * );@{b}

   FUNCTION@{ub}
       Free all RegionRectangles for this Region then
        free the Region itself.@{b}

   INPUTS@{ub}
       region - pointer to Region structure@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"NewRegion()" LINK "NewRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "DoCollision" "graphics.library/DoCollision()"
@{b}

   NAME@{ub}
        @{"DoCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 105} -- Test every gel in gel list for collisions.@{b}

   SYNOPSIS@{ub}
        @{"DoCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 105}(rp)
                    A1

        void @{"DoCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 105}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Tests each gel in gel list for boundary and gel-to-gel collisions.
        On detecting one of these collisions, the appropriate collision-
        handling routine is called. See the documentation for a thorough
        description of which collision routine is called. This routine
        expects to find the gel list correctly sorted in Y,X order.
        The system routine @{"SortGList" LINK "SortGList"} performs this function for the user.@{b}

   INPUTS@{ub}
        rp = pointer to a RastPort@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"SortGList()" LINK "SortGList"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "Draw" "graphics.library/Draw()"
@{b}

   NAME@{ub}
       Draw -- Draw a line between the current pen position
                       and the new x,y position.@{b}

   SYNOPSIS@{ub}
       Draw( rp,   x,     y)
             a1  d0:16  d1:16

        void Draw( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
       Draw a line from the current pen position to (x,y).@{b}

   INPUTS@{ub}

        rp - pointer to the destination RastPort
        x,y - coordinates of where in the RastPort to end the line.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"Move()" LINK "Move"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "DrawEllipse" "graphics.library/DrawEllipse()"
@{b}

    NAME@{ub}
        @{"DrawEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 153} -- Draw an ellipse centered at cx,cy with vertical
           and horizontal radii of a,b respectively.@{b}

    SYNOPSIS@{ub}
        @{"DrawEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 153}( rp, cx, cy, a, b )
                     a1  d0  d1  d2 d3

        void @{"DrawEllipse" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 153}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

    FUNCTION@{ub}
       Creates an elliptical outline within the rectangular region
        specified by the parameters, using the current foreground pen color.@{b}

    INPUTS@{ub}
        rp - pointer to the RastPort into which the ellipse will be drawn.
        cx - x coordinate of the centerpoint relative to the rastport.
        cy - y coordinate of the centerpoint relative to the rastport.
        a - the horizontal radius of the ellipse (note: a must be > 0)
        b - the vertical radius of the ellipse (note: b must be > 0)@{b}

    BUGS@{ub}@{b}

    NOTES@{ub}
        this routine does not clip the ellipse to a non-layered rastport.@{b}

    SEE ALSO@{ub}
        @{"DrawCircle" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 68} macro
        @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "DrawGList" "graphics.library/DrawGList()"
@{b}

   NAME@{ub}
        @{"DrawGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 109} -- Process the gel list, queueing VSprites, drawing Bobs.@{b}

   SYNOPSIS@{ub}
        @{"DrawGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 109}(rp, vp)
                  A1  A0

        void @{"DrawGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 109}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
        Performs one pass of the current gel list.
           - If nextLine and lastColor are defined, these are
             initialized for each gel.
          - If it's a VSprite, build it into the copper list.
          - If it's a Bob, draw it into the current raster.
          - Copy the save values into the "old" variables,
             double-buffering if required.@{b}

   INPUTS@{ub}
        rp = pointer to the RastPort where Bobs will be drawn
        vp = pointer to the ViewPort for which VSprites will be created@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}
        @{"MUSTDRAW" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 23} isn't implemented yet.@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}  @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "EraseRect" "graphics.library/EraseRect()"
@{b}

   NAME@{ub}

       @{"EraseRect" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 554} -- Fill a defined rectangular area using the current
                        BackFill hook. (V36)@{b}

   SYNOPSIS@{ub}
        @{"EraseRect" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 554}( rp, xmin, ymin, xmax, ymax)
                  a1  d0:16 d1:16 d2:16 d3:16

        void @{"EraseRect" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 554}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
        Fill the rectangular region specified by the parameters with the
        BackFill hook. If non-layered, the rectangular region specified by
        the parameters is cleared. If layered the Layer->BackFill Hook is used.@{b}

   INPUTS@{ub}
        rp      - pointer to a RastPort structure
        xmin    - x coordinate of the upper left corner of the region to fill.
        ymin    - y coordinate of the upper left corner of the region to fill.
        xmax    - x coordinate of the lower right corner of the region to fill.
        ymax    - y coordinate of the lower right corner of the region to fill.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        The following relation MUST be true:
        (xmax >= xmin) and (ymax >= ymin)@{b}

   SEE ALSO@{ub}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"} @{"graphics/clip.h" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File"}

@ENDNODE
@NODE "ExtendFont" "graphics.library/ExtendFont()"
@{b}

   NAME@{ub}
        @{"ExtendFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 558} -- ensure @{"tf_Extension" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 113} has been built for a font (V36)@{b}

   SYNOPSIS@{ub}
        success = @{"ExtendFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 558}(font, fontTags)
        D0                   A0    A1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"ExtendFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 558}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        success = @{"ExtendFontTags" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 563}(font, Tag1, ...)  (V39)

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"ExtendFontTags" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 563}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, ...);@{b}

   FUNCTION@{ub}
        To extend a TextFont structure.@{b}

   INPUTS@{ub}
        font - The font to extend.
        fontTags - An optional taglist.  If @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, then a default is used.
                   Currently, the only tag defined is @{"TA_DeviceDPI" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 82}.@{b}

   RESULT@{ub}
        success - 1 if the TextFont was properly extended, else 0.@{b}

   NOTES@{ub}
        The varargs stub was missing from @{"amiga.lib" LINK "gg:doc/NDK/Guide/amiga_lib/MAIN"} until V39.@{b}

   SEE ALSO@{ub}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "FindColor" "graphics.library/FindColor()"
@{b}

   NAME@{ub}
        @{"FindColor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 689} -- Find the closest matching color in a colormap. (V39)@{b}

   SYNOPSIS@{ub}
        color = @{"FindColor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 689}(  cm,  R,   G,    B , maxpen)
                           a3   d1   d2    d3   d4

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"FindColor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 689}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51});@{b}

   INPUTS@{ub}
        cm = colormap
        R = red level   (32 bit left justified fraction)
        G = green level (32 bit left justified fraction)
        B = blue level  (32 bit left justified fraction)
        MaxPen = the maximum entry in the color table to search. A value of
                -1 will limt the search to only those pens which could be
                rendered in (for instance, it will not examine the sprite
                colors on a 4 color screen).@{b}


   RESULT@{ub}
        The system will attempt to find the color in the passed colormap
        which most closely matches the RGB values passed. No new pens will
        be allocated, and you should not @{"ReleasePen()" LINK "ReleasePen"} the returned pen.
        This function is not sensitive to palette sharing issues. Its
        intended use is for:

                (a) programs which pop up on public screens when those
                    screens are not using palette sharing. You might
                    use this function as a fallback when @{"ObtainBestPenA()" LINK "ObtainBestPenA"}
                    says that there are no sharable pens.

                (b) Internal color matching by an application which is
                    either running on a non-public screen, or which
                    wants to match colors to an internal color table
                    which may not be associated with any displayed screen.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        In order to use the MaxPen=-1 feature, you must have initialized
        palette sharing via @{"AttachPalExtra()" LINK "AttachPalExtra"} (all intuition screens do this).
        Otherwise, MaxPen=-1 will search all colors in the colormap.@{b}


   SEE ALSO@{ub}
        @{"ObtainBestPenA()" LINK "ObtainBestPenA"} @{"GetColorMap()" LINK "GetColorMap"} @{"ObtainPen()" LINK "ObtainPen"} @{"ReleasePen()" LINK "ReleasePen"}

@ENDNODE
@NODE "FindDisplayInfo" "graphics.library/FindDisplayInfo()"
@{b}

   NAME@{ub}
        @{"FindDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 514} -- search for a record identified by a specific key (V36)@{b}

   SYNOPSIS@{ub}
        handle = @{"FindDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 514}(ID)
        D0                       D0

        @{"DisplayInfoHandle" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 35} @{"FindDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 514}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Given a 32-bit Mode Key, return a handle to a valid DisplayInfoRecord
        found in the graphics database.  Using this handle, you can obtain
        information about this Mode, including its default dimensions,
        properties, and whether it is currently available for use.@{b}

   INPUTS@{ub}
        ID     - unsigned long identifier@{b}

   RESULT@{ub}
        handle - handle to a displayinfo Record with that key
                 or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if no match.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}

@ENDNODE
@NODE "Flood" "graphics.library/Flood()"
@{b}

   NAME@{ub}
        Flood -- Flood rastport like areafill.@{b}

   SYNOPSIS@{ub}
        error = Flood( rp, mode, x, y)
        d0            a1   d2  d0  d1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} Flood(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
        Search the BitMap starting at (x,y).
        Fill all adjacent pixels if they are:
            Mode 0: not the same color as AOLPen
            Mode 1: the same color as the pixel at (x,y)

        When actually doing the fill use the modes that apply to
        standard areafill routine such as drawmodes and patterns.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort
        (x,y) - coordinate in BitMap to start the flood fill at.
        mode -  0 fill all adjacent pixels searching for border.
                1 fill all adjacent pixels that have same pen number
                as the one at (x,y).@{b}

   NOTES@{ub}
        In order to use Flood, the destination RastPort must
        have a valid TmpRas raster whose size is as large as
        that of the RastPort.@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"} @{"InitTmpRas()" LINK "InitTmpRas"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "FontExtent" "graphics.library/FontExtent()"
@{b}

   NAME@{ub}
        @{"FontExtent" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 526} -- get the font attributes of the current font (V36)@{b}

   SYNOPSIS@{ub}
        @{"FontExtent" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 526}(font, fontExtent)
                   A0    A1

        void @{"FontExtent" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 526}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *, @{"struct TextExtent" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 164} *);@{b}

   FUNCTION@{ub}
        This function fills the text extent structure with a bounding
        (i.e. maximum) extent for the characters in the specified font.@{b}

   INPUTS@{ub}
        font       - the TextFont from which the font metrics are extracted.
        fontExtent - the @{"TextExtent" LINK "TextExtent"} structure to be filled.@{b}

   RESULT@{ub}
        fontExtent is filled.@{b}

   NOTES@{ub}
        The TextFont, not the RastPort, is specified -- unlike
        @{"TextExtent()" LINK "TextExtent"}, effect of algorithmic enhancements is not
        included, nor does te_Width include any effect of
        rp_TxSpacing.  The returned te_Width will be negative only
        when @{"FPF_REVPATH" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 47} is set in the tf_Flags of the font -- the
        effect of left-moving characters is ignored for the width of
        a normal font, and the effect of right-moving characters is
        ignored if a REVPATH font.  These characters will, however,
        be reflected in the bounding extent.@{b}

   SEE ALSO@{ub}
        @{"TextExtent()" LINK "TextExtent"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "FreeBitMap" "graphics.library/FreeBitMap()"
@{b}

   NAME@{ub}
        @{"FreeBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 628} -- free a bitmap created by @{"AllocBitMap" LINK "AllocBitMap"} (V39)@{b}

   SYNOPSIS@{ub}
        @{"FreeBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 628}(bm)
                   a0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"FreeBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 628}(@{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *)@{b}

   FUNCTION@{ub}
        Frees bitmap and all associated bitplanes@{b}

   INPUTS@{ub}
        bm  =  A pointer to a BitMap structure. Passing a @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}-pointer
               (meaning "do nothing") is OK.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        Be careful to insure that any rendering done to the bitmap has
        completed (by calling @{"WaitBlit()" LINK "WaitBlit"}) before you call this function.@{b}

   SEE ALSO@{ub}
        @{"AllocBitMap()" LINK "AllocBitMap"}

@ENDNODE
@NODE "FreeColorMap" "graphics.library/FreeColorMap()"
@{b}

   NAME@{ub}
       @{"FreeColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 417} -- Free the ColorMap structure and return memory
                                                to free memory pool.@{b}

   SYNOPSIS@{ub}
       @{"FreeColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 417}( colormap )
                       a0

        void @{"FreeColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 417}(@{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *);@{b}

   FUNCTION@{ub}
        Return the memory to the free memory pool that was allocated
        with @{"GetColorMap" LINK "GetColorMap"}.@{b}

   INPUTS@{ub}
        colormap - pointer to ColorMap allocated with @{"GetColorMap" LINK "GetColorMap"}.

                  Passing a @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} pointer (meaning "do nothing") is
                  acceptable (V39).@{b}

   RESULT@{ub}
        The space is made available for others to use.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"SetRGB4()" LINK "SetRGB4"} @{"GetColorMap()" LINK "GetColorMap"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "FreeCopList" "graphics.library/FreeCopList()"
@{b}

   NAME@{ub}
        @{"FreeCopList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 397} -- deallocate intermediate copper list@{b}

   SYNOPSIS@{ub}
       @{"FreeCopList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 397}(coplist)
                      a0

        void @{"FreeCopList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 397}( @{"struct CopList" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 62} *);@{b}

   FUNCTION@{ub}
        Deallocate all memory associated with this copper list.@{b}

   INPUTS@{ub}
       coplist  - pointer to structure CopList@{b}

   RESULTS@{ub}
        memory returned to memory manager@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "FreeCprList" "graphics.library/FreeCprList()"
@{b}

   NAME@{ub}
       @{"FreeCprList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 409} -- deallocate hardware copper list@{b}

   SYNOPSIS@{ub}
       @{"FreeCprList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 409}(cprlist)
                      a0

        void @{"FreeCprList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 409}(@{"struct cprlist" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File" 55} *);@{b}

   FUNCTION@{ub}
       return cprlist to free memory pool@{b}

   INPUTS@{ub}
       cprlist - pointer to cprlist structure@{b}

   RESULTS@{ub}
        memory returned and made available to other tasks@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/copper.h" LINK "gg:doc/NDK/Guide/Include/graphics/copper.h/File"}

@ENDNODE
@NODE "FreeDBufInfo" "graphics.library/FreeDBufInfo()"
@{b}

   NAME@{ub}
       @{"FreeDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 665} -- free information for multi-buffered animation (V39)@{b}

   SYNOPSIS@{ub}
       @{"FreeDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 665}(db)
                     a1

        void @{"FreeDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 665}(@{"struct DBufInfo" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 279} *)@{b}

   FUNCTION@{ub}
        Frees a structure obtained from @{"AllocDBufInfo" LINK "AllocDBufInfo"}@{b}

   INPUTS@{ub}
       db  =  A pointer to a DBufInfo.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        @{"FreeDBufInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 665}(@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}) is a no-op.@{b}

   SEE ALSO@{ub}
        @{"AllocDBufInfo()" LINK "AllocDBufInfo"} @{"ChangeVPBitMap()" LINK "ChangeVPBitMap"}

@ENDNODE
@NODE "FreeGBuffers" "graphics.library/FreeGBuffers()"
@{b}

   NAME@{ub}
        @{"FreeGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 433} -- Deallocate memory obtained by GetGBufers.@{b}

   SYNOPSIS@{ub}
        @{"FreeGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 433}(anOb, rp, db)
                     A0    A1  D0

        void @{"FreeGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 433}(@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91});@{b}

   FUNCTION@{ub}
        For each sequence of each component of the AnimOb,
        deallocate memory for:
            SaveBuffer
            BorderLine
            CollMask and ImageShadow (point to same buffer)
            if db is set (user had used double-buffering) deallocate:
                DBufPacket
                BufBuffer@{b}

   INPUTS@{ub}
        anOb = pointer to the AnimOb structure
        rp   = pointer to the current RastPort
        db   = double-buffer indicator (set @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} for double-buffering)@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"GetGBuffers()" LINK "GetGBuffers"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "FreeRaster" "graphics.library/FreeRaster()"
@{b}

   NAME@{ub}
       @{"FreeRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 365} -- Release an allocated area to the system free memory pool .@{b}


   SYNOPSIS@{ub}
       @{"FreeRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 365}( p, width, height)
                   a0   d0:16  d1:16

        void @{"FreeRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 365}( @{"PLANEPTR" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 44}, @{"USHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 82}, @{"USHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 82});@{b}

   FUNCTION@{ub}
        Return the memory associated with this @{"PLANEPTR" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 44} of size
        width and height to the @{"MEMF_CHIP" LINK "gg:doc/NDK/Guide/Include/exec/memory.h/File" 70} memory pool.@{b}

   INPUTS@{ub}
       p  =  a pointer to a memory space  returned  as  a
             result of a call to @{"AllocRaster" LINK "AllocRaster"}.

        width - the width in bits of the bitplane.
        height - number of rows in bitplane.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
       Width and height should be the same values with which you
       called @{"AllocRaster" LINK "AllocRaster"} in the first place.@{b}

   SEE ALSO@{ub}
        @{"AllocRaster()" LINK "AllocRaster"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "FreeSprite" "graphics.library/FreeSprite()"
@{b}

   NAME@{ub}
       @{"FreeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 309} -- Return sprite for use by others and virtual
                                          sprite machine.@{b}

   SYNOPSIS@{ub}
       @{"FreeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 309}( pick )
                    d0

        void @{"FreeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 309}( @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} );@{b}

   FUNCTION@{ub}
        Mark sprite as available for others to use.
       These sprite routines are provided to ease sharing of sprite
        hardware and to handle simple cases of sprite usage and
        movement.  It is assumed the programs that use these routines
        do want to be good citizens in their hearts. ie: they will
        not @{"FreeSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 309} unless they actually own the sprite.
        The Virtual Sprite machine may ignore the simple sprite machine.@{b}

   INPUTS@{ub}
       pick - number in range of 0-7@{b}

   RESULTS@{ub}
        sprite made available for subsequent callers of @{"GetSprite" LINK "GetSprite"}
        as well as use by Virtual Sprite Machine.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"GetSprite()" LINK "GetSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "FreeSpriteData" "graphics.library/FreeSpriteData()"
@{b}

   NAME@{ub}
        @{"FreeSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 711} -- free sprite data allocated by @{"AllocSpriteData()" LINK "AllocSpriteDataA"} (V39)@{b}

   SYNOPSIS@{ub}
        @{"FreeSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 711}(extsp)
                        a2

        void @{"FreeSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 711}(@{"struct ExtSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 25} *);@{b}


   FUNCTION@{ub}@{b}

   INPUTS@{ub}
        extsp - The extended sprite structure to be freed. Passing @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} is a
        NO-OP.@{b}

   SEE ALSO@{ub}
        @{"FreeSpriteData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 711}() @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"GetExtSprite()" LINK "GetExtSpriteA"}
        @{"AllocBitMap()" LINK "AllocBitMap"} @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "FreeVPortCopLists" "graphics.library/FreeVPortCopLists()"
@{b}

   NAME@{ub}
       @{"FreeVPortCopLists" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 393} -- deallocate all intermediate copper lists and
       their headers from a viewport@{b}

   SYNOPSIS@{ub}
       @{"FreeVPortCopLists" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 393}(vp)
                         a0

        void @{"FreeVPortCopLists" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 393}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
       Search display, color, sprite, and user copper
       lists and call @{"FreeMem()" LINK "gg:doc/NDK/Guide/exec/FreeMem"} to deallocate them from memory@{b}

   INPUTS@{ub}
       vp - pointer to ViewPort structure@{b}

   RESULTS@{ub}
       The memory allocated to the various copper lists will be returned
        to the system's free memory pool, and the following fields in
        the viewport structure will be set to @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}:

                DspIns, Sprins, ClrIns, UCopIns@{b}

   BUGS@{ub}
       none known@{b}

   SEE ALSO@{ub}
        @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "GetAPen" "graphics.library/GetAPen()"
@{b}

   NAME@{ub}
       @{"GetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 592} -- Get the A Pen value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       pen = @{"GetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 592}  ( rp )
         d0              a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 592}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *)@{b}

   FUNCTION@{ub}
        Return the current value of the A pen for the rastport. This function
        should be used instead of peeking the structure directly, because future
        graphics devices may store it differently, for instance, using more bits.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetAPen()" LINK "SetAPen"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "GetBitMapAttr" "graphics.library/GetBitMapAttr()"
@{b}

   NAME@{ub}
        @{"GetBitMapAttr" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 657} -- Returns information about a bitmap (V39)@{b}


   SYNOPSIS@{ub}
        value=@{"GetBitMapAttr" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 657}(bitmap,attribute_number);
          d0                     a0       d1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetBitMapAttr" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 657}(@{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *,@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Determines information about a bitmap. This function should be used
        instead of reading the bitmap structure fields directly. This will
        provide future compatibility.@{b}

   INPUTS@{ub}
        bm  =  A pointer to a BitMap structure.

        attribute_number = A number telling graphics which attribute
                           of the bitmap should be returned:

                        @{"BMA_HEIGHT" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 76} returns the height in pixels
                        @{"BMA_WIDTH" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 78}  returns the width in pixels.
                        @{"BMA_DEPTH" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 77}  returns the depth. This is the number of
                                bits which are required to store the information
                                for one pixel in the bitmap.
                        @{"BMA_FLAGS" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 79}  returns a longword bitfield describing
                                various attributes which the bitmap may have.
                                Currently defined flags are @{"BMF_DISPLAYABLE" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 70},
                                @{"BMF_INTERLEAVED" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 71} (see @{"AllocBitMap()" LINK "AllocBitMap"}). The flag
                                @{"BMF_STANDARD" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 72} returns will be set if the
                                bitmap is represented as planar data in Amiga
                                Chip RAM.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}

        Unknown attributes are reserved for future use, and return zero.

        @{"BMF_DISPLAYABLE" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 70} will only be set if the source bitmap meets all of the
        required alignment restrictions. A bitmap which does not meet these
        restrictions may still be displayable at some loss of efficiency.

        Size values returned by this function may not exactly match the values
        which were passed to @{"AllocBitMap()" LINK "AllocBitMap"}, due to alignment restrictions.@{b}

   SEE ALSO@{ub}
        @{"AllocBitMap()" LINK "AllocBitMap"}

@ENDNODE
@NODE "GetBPen" "graphics.library/GetBPen()"
@{b}

   NAME@{ub}
       @{"GetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 596} -- Get the B Pen value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       pen = @{"GetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 596}  ( rp )
        d0               a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 596}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *)@{b}

   FUNCTION@{ub}
        Return the current value of the B pen for the rastport. This function
        should be used instead of peeking the structure directly, because future
        graphics devices may store it differently, using more bits.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetBPen()" LINK "SetBPen"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "GetColorMap" "graphics.library/GetColorMap()"
@{b}

   NAME@{ub}
       @{"GetColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 413} -- allocate and initialize Colormap@{b}


   SYNOPSIS@{ub}
       cm = @{"GetColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 413}( entries )
       d0                   d0

        @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *@{"GetColorMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 413}( @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
       Allocates, initializes and returns a pointer to a ColorMap
       data structure, later enabling calls to @{"SetRGB4" LINK "SetRGB4"}/@{"SetRGB32" LINK "SetRGB32"}
       and @{"LoadRGB4" LINK "LoadRGB4"}/@{"LoadRGB32" LINK "LoadRGB32"} to load colors for a view port. The
       ColorTable pointer in the ColorMap structure points to a
       hardware specific colormap data structure. You should not
       count on it being anything you can understand. Use
       @{"GetRGB4()" LINK "GetRGB4"}/@{"GetRGB32()" LINK "GetRGB32"} to query it or @{"SetRGB4CM" LINK "SetRGB4CM"}/@{"SetRGB32CM" LINK "SetRGB32CM"}
       to set it directly.@{b}

   INPUTS@{ub}
        entries - number of entries for this colormap@{b}

    RESULT@{ub}
        The pointer value returned by this routine, if nonzero,
       may be stored into the ViewPort.ColorMap pointer.
       If a value of 0 is returned, the system was unable
       to allocate enough memory space for the required
       data structures.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"SetRGB4()" LINK "SetRGB4"} @{"FreeColorMap()" LINK "FreeColorMap"}

@ENDNODE
@NODE "GetDisplayInfoData" "graphics.library/GetDisplayInfoData()"
@{b}

   NAME@{ub}
        @{"GetDisplayInfoData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 522} -- query DisplayInfo Record parameters (V36)@{b}

   SYNOPSIS@{ub}
        result = @{"GetDisplayInfoData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 522}(handle, buf, size, tagID, [ID])
        D0                          A0      A1   D0    D1     [D2]

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetDisplayInfoData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 522}(@{"DisplayInfoHandle" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 35}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        @{"GetDisplayInfoData" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 522}() fills a buffer with data meaningful to the
        DisplayInfoRecord pointed at by your valid handle. The data type
        that you are interested in is indicated by a tagID for that chunk.
        The types of tagged information that may be available include:

        @{"DTAG_DISP" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 39}: (DisplayInfo)   - properties and availability information.
        @{"DTAG_DIMS" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 40}: (DimensionInfo) - default dimensions and overscan info.
        @{"DTAG_MNTR" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 41}: (MonitorInfo)   - type, position, scanrate, and compatibility
        @{"DTAG_NAME" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 42}: (NameInfo)      - a user friendly way to refer to this mode.@{b}

   INPUTS@{ub}
        handle - displayinfo handle
        buf    - pointer to destination buffer
        size   - buffer size in bytes
        tagID  - data chunk type
        ID     - displayinfo identifier, optionally used if handle is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}@{b}

   RESULT@{ub}
        result - if positive, number of bytes actually transferred
                 if zero, no information for ID was available@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FindDisplayInfo()" LINK "FindDisplayInfo"}, @{"NextDisplayInfo()" LINK "NextDisplayInfo"}
        @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}

@ENDNODE
@NODE "GetDrMd" "graphics.library/GetDrMd()"
@{b}

   NAME@{ub}
       @{"GetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 600} -- Get the draw mode value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       mode = @{"GetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 600}  ( rp )
        d0                a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 600}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *)@{b}

   FUNCTION@{ub}
        Return the current value of the draw mode for the rastport. This function
        should be used instead of peeking the structure directly, because future
        graphics devices may store it differently.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetDrMd()" LINK "SetDrMd"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "GetExtSpriteA" "graphics.library/GetExtSpriteA()"
@{b}

   NAME@{ub}
        @{"GetExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 632} -- Attempt to get a sprite for the extended sprite
                                         manager. (V39)
        @{"GetExtSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 637} -- varargs stub for @{"GetExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 632}. (V39)@{b}

   SYNOPSIS@{ub}
        Sprite_Number = @{"GetExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 632}( sprite, tags )  (V39)
            d0                          a2      a1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"GetExtSpriteA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 632}( @{"struct ExtSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 25} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} * );

        spritenum=@{"GetExtSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 637}(sprite,tags,...);@{b}

   FUNCTION@{ub}
        Attempt to allocate one of the eight sprites for private use
        with the extended sprite manager.@{b}

   INPUTS@{ub}
        sprite - ptr to programmer's ExtSprite (from @{"AllocSpriteData()" LINK "AllocSpriteDataA"}).
        tags - a standard tag list:

                @{"GSTAG_SPRITE_NUM" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 43}        specifies a specific sprite to get by number.

                @{"GSTAG_ATTACHED" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 44} specifies that you wish to get a sprite pair.
                        the tag data field points to a ExtSprite structure
                        for the second sprite. You must free both sprites.@{b}


   RESULTS@{ub}
        Sprite_number = a sprite number or -1 for an error.
                This call will fail if no sprites could be allocated, or
                if you try to allocate a sprite which would require
                a mode change when there are other sprites of incompatible
                modes in use.@{b}

   BUGS@{ub}

        @{"GSTAG_ATTACHED" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 44} does not work in version 39. When running under V39,
        you should attach the second sprite with a separate @{"GetExtSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 637} call.@{b}

   SEE ALSO@{ub}
        @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"GetSprite()" LINK "GetSprite"} @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "GetGBuffers" "graphics.library/GetGBuffers()"
@{b}

   NAME@{ub}
        @{"GetGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 145} -- Attempt to allocate ALL buffers of an entire AnimOb.@{b}

   SYNOPSIS@{ub}
        status = @{"GetGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 145}(anOb, rp, db)
        D0                   A0    A1  D0

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"GetGBuffers" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 145}(@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91});@{b}

   FUNCTION@{ub}
        For each sequence of each component of the AnimOb, allocate memory for:
            SaveBuffer
            BorderLine
            CollMask and ImageShadow (point to same buffer)
            if db is set @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} (user wants double-buffering) allocate:
                DBufPacket
                BufBuffer@{b}

   INPUTS@{ub}
        anOb = pointer to the AnimOb structure
        rp   = pointer to the current RastPort
        db   = double-buffer indicator (set @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} for double-buffering)@{b}

   RESULT@{ub}
        status = @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if the memory allocations were all successful, else @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}@{b}

   BUGS@{ub}
        If any of the memory allocations fail it does not free the partial
        allocations that did succeed.@{b}

   SEE ALSO@{ub}
        @{"FreeGBuffers()" LINK "FreeGBuffers"} @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "GetOPen" "graphics.library/GetOPen()"
@{b}

   NAME@{ub}
       GetOPen -- Get the O Pen value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       pen = GetOPen  ( rp )
        d0               a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} GetOPen(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *)@{b}

   FUNCTION@{ub}
        Return the current value of the O pen for the rastport. This function
        should be used instead of peeking the structure directly, because future
        graphics devices may store it differently, for instance, using more bits.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetOutlinePen()" LINK "SetOutlinePen"} @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "GetRGB32" "graphics.library/GetRGB32()"
@{b}

   NAME@{ub}
        @{"GetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 620} -- Set a series of color registers for this Viewport. (V39)@{b}

   SYNOPSIS@{ub}
        @{"GetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 620}(  cm,  firstcolor, ncolors, table )
                   a0   d0              d1    a1

       void @{"GetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 620}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} *);@{b}

   INPUTS@{ub}
        cm = colormap
        firstcolor = the first color register to get
        ncolors = the number of color registers to set.
        table=a pointer to a series of 32-bit RGB triplets.@{b}

   RESULT@{ub}
        The @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} data pointed to by 'table' will be filled with the 32 bit
        fractional RGB values from the colormap.@{b}
   BUGS@{ub}@{b}

   NOTES@{ub}
        'Table' should point to at least ncolors*3 longwords.@{b}

   SEE ALSO@{ub}
        @{"LoadRGB4()" LINK "LoadRGB4"} @{"GetColorMap()" LINK "GetColorMap"} @{"LoadRGB32()" LINK "LoadRGB32"} @{"SetRGB32CM()" LINK "SetRGB32CM"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "GetRGB4" "graphics.library/GetRGB4()"
@{b}

   NAME@{ub}
       @{"GetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 421} -- Inquire value of entry in ColorMap.@{b}

   SYNOPSIS@{ub}
       value = @{"GetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 421}( colormap, entry )
          d0              a0       d0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 421}(@{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51});@{b}

   FUNCTION@{ub}
        Read and format a value from the ColorMap.@{b}

   INPUTS@{ub}
        colormap - pointer to ColorMap structure
        entry - index into colormap@{b}

   RESULT@{ub}
        returns -1 if no valid entry
        return @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} RGB value 4 bits per gun right justified@{b}

   NOTE@{ub}
        Intuition's @{"DisplayBeep()" LINK "gg:doc/NDK/Guide/intuition/DisplayBeep"} changes color 0. Reading Color 0 during a
        @{"DisplayBeep()" LINK "gg:doc/NDK/Guide/intuition/DisplayBeep"} will lead to incorrect results.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"SetRGB4()" LINK "SetRGB4"} @{"LoadRGB4()" LINK "LoadRGB4"} @{"GetColorMap()" LINK "GetColorMap"} @{"FreeColorMap()" LINK "FreeColorMap"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "GetRPAttrA" "graphics.library/GetRPAttrA()"
@{b}

   NAME@{ub}
       GetRPAttrA -- examine rastport settings via a tag list
        @{"GetRPAttrs" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 729}  -- varargs stub for GetRPAttrA@{b}

   SYNOPSIS@{ub}
       GetRPAttrA(rp,tags)
                  a0   a1

        void GetRPAttrA(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        @{"GetRPAttrs" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 729}(rp,attr1,&result1,...);@{b}

   FUNCTION@{ub}
       Read the settings of a rastport into variables. The
        ti_Tag field of the TagItem specifies which attribute
        should be read, and the ti_Data field points at the
        location where the result hsould be stored. All current
        tags store the return data as LONGs (32 bits).

        currently available tags are:

                @{"RPTAG_Font" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 11}              Font for @{"Text()" LINK "Text"}
                RPTAG_SoftStyle         style for text (see @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"})
                @{"RPTAG_APen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 12}              Primary rendering pen
                @{"RPTAG_BPen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 13}              Secondary rendering pen
                @{"RPTAG_DrMd" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 14}              Drawing mode (see @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"})
                @{"RPTAG_OutLinePen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 15}        Area Outline pen
                @{"RPTAG_WriteMask" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 17}         Bit Mask for writing.
                @{"RPTAG_MaxPen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 18}            Maximum pen to render (see @{"SetMaxPen()" LINK "SetMaxPen"})
                @{"RPTAG_DrawBounds" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 20}        Determine the area that will be rendered
                                        into by rendering commands. Can be used
                                        to optimize window refresh. Pass a pointer
                                        to a rectangle in the tag data. On return,
                                        the rectangle's MinX will be greater than
                                        its MaxX if there are no active cliprects.@{b}

   INPUTS@{ub}
        rp - pointer to the RastPort to examine.
        tags - a standard tag list specifying the attributes to be read,
                and where to store their values.@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"GetAPen()" LINK "GetAPen"} @{"GetBPen()" LINK "GetBPen"} @{"GetDrMd()" LINK "GetDrMd"} GetOutLinePen()
        @{"SetRPAttrA()" LINK "SetRPAttrA"} @{"graphics/rpattr.h" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File"}

@ENDNODE
@NODE "GetSprite" "graphics.library/GetSprite()"
@{b}

   NAME@{ub}
        @{"GetSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 305} -- Attempt to get a sprite for the simple sprite manager.@{b}

   SYNOPSIS@{ub}
        Sprite_Number = @{"GetSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 305}( sprite, pick )
            d0                      a0      d0

        @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} @{"GetSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 305}( @{"struct SimpleSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 17} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} );@{b}

   FUNCTION@{ub}
        Attempt to allocate one of the eight sprites for private use
        with the simple sprite manager. This must be done before using
        further calls to the simple sprite machine. If the programmer
        wants to use 15 color sprites, they must allocate both sprites
        and set the '@{"SPRITE_ATTACHED" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 15}' bit in the odd sprite's posctldata
        array.@{b}

   INPUTS@{ub}
        sprite - ptr to programmers SimpleSprite structure.
        pick - number in the range of 0-7 or
          -1 if programmer just wants the next one.@{b}

   RESULTS@{ub}
        If pick is 0-7 attempt to allocate the sprite. If the sprite
        is already allocated then return -1.
        If pick -1 allocate the next sprite starting search at 0.
        If no sprites are available return -1 and fill -1 in num entry
        of SimpleSprite structure.
        If the sprite is available for allocation, mark it allocated
        and fill in the 'num' entry of the SimpleSprite structure.
        If successful return the sprite number.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FreeSprite()" LINK "FreeSprite"} @{"ChangeSprite()" LINK "ChangeSprite"} @{"MoveSprite()" LINK "MoveSprite"} @{"GetSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 305}() @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "GetVPModeID" "graphics.library/GetVPModeID()"
@{b}

   NAME@{ub}
        @{"GetVPModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 546} -- get the 32 bit DisplayID from a ViewPort. (V36)@{b}

   SYNOPSIS@{ub}
        modeID =  @{"GetVPModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 546}( vp )
        d0                     a0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"GetVPModeID" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 546}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
        returns the normal display modeID, if one is currently  associated
        with this ViewPort.@{b}

   INPUTS@{ub}
        vp -- pointer to a ViewPort structure.@{b}

   RESULT@{ub}

        modeID -- a 32 bit DisplayInfoRecord identifier associated with
                  this ViewPort, or @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17}.@{b}

   NOTES@{ub}
        Test the return value of this function against @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17}, not @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.
        (@{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} is defined in @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}).@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}, @{"ModeNotAvailable()" LINK "ModeNotAvailable"}

@ENDNODE
@NODE "GfxAssociate" "graphics.library/GfxAssociate()"
@{b}

   NAME@{ub}
        @{"GfxAssociate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 473} -- associate a graphics extended node with a given pointer
                        (V36)@{b}

   SYNOPSIS@{ub}
       @{"GfxAssociate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 473}(pointer, node);
                    A0       A1

        void @{"GfxAssociate" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 473}(@{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} *, @{"struct ExtendedNode" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File" 17} *);@{b}

   FUNCTION@{ub}
        Associate a special graphics extended data structure (each of which
        begins with an ExtendedNode structure)  with another structure via
        the other structure's pointer. Later, when you call @{"GfxLookUp()" LINK "GfxLookUP"}
        with the other structure's pointer you may retrieve a pointer
        to this special graphics extended data structure, if it is
        available.@{b}

   INPUTS@{ub}
        pointer = a pointer to a data structure.
        node = an ExtendedNode structure to associate with the pointer@{b}

   RESULT@{ub}
        an association is created between the pointer and the node such
        that given the pointer the node can be retrieved via @{"GfxLookUp()" LINK "GfxLookUP"}.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gfxnodes.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File"} @{"GfxNew()" LINK "GfxNew"} @{"GfxFree()" LINK "GfxFree"} @{"GfxLookUp()" LINK "GfxLookUP"}

@ENDNODE
@NODE "GfxFree" "graphics.library/GfxFree()"
@{b}

   NAME@{ub}
       @{"GfxFree" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 469} -- free a graphics extended data structure (V36)@{b}

   SYNOPSIS@{ub}
       @{"GfxFree" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 469}( node );
              a0

        void @{"GfxFree" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 469}(@{"struct ExtendedNode" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File" 17} *);@{b}

   FUNCTION@{ub}
        Free a special graphics extended data structure (each of which
        begins with an ExtendedNode structure).@{b}

   INPUTS@{ub}
        node = pointer to a graphics extended data structure obtained via
               @{"GfxNew()" LINK "GfxNew"}.@{b}

   RESULT@{ub}
        the node is deallocated from memory. graphics will disassociate
        this special graphics extended node from any associated data
        structures, if necessary, before freeing it (see @{"GfxAssociate()" LINK "GfxAssociate"}).@{b}

   BUGS@{ub}
        an @{"Alert()" LINK "gg:doc/NDK/Guide/exec/Alert"} will be called if you attempt to free any structure
        other than a graphics extended data structure obtained via @{"GfxFree" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 469}().@{b}

   SEE ALSO@{ub}
        @{"graphics/gfxnodes.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File"} @{"GfxNew()" LINK "GfxNew"} @{"GfxAssociate()" LINK "GfxAssociate"} @{"GfxLookUp()" LINK "GfxLookUP"}

@ENDNODE
@NODE "GfxLookUP" "graphics.library/GfxLookUP()"
@{b}

   NAME@{ub}
        @{"GfxLookUp" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 493} -- find a graphics extended node associated with a
                     given pointer (V36)@{b}

   SYNOPSIS@{ub}
       result = @{"GfxLookUp" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 493}( pointer );
       d0                   a0

        @{"struct ExtendedNode" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File" 17} *@{"GfxLookUp" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 493}( void *);@{b}

   FUNCTION@{ub}
        Finds a special graphics extended data structure (if any) associated
        with the pointer to a data structure (eg: ViewExtra associated with
        a View structure).@{b}

   INPUTS@{ub}
        pointer = a pointer to a data structure which may have an
                  ExtendedNode associated with it (typically a View ).@{b}

   RESULT@{ub}
        result = a pointer to the ExtendedNode that has previously been
                 associated with the pointer.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gfxnodes.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File"} @{"GfxNew()" LINK "GfxNew"} @{"GfxFree()" LINK "GfxFree"} @{"GfxAssociate()" LINK "GfxAssociate"}

@ENDNODE
@NODE "GfxNew" "graphics.library/GfxNew()"
@{b}

   NAME@{ub}
       @{"GfxNew" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 465} -- allocate a graphics extended data structure (V36)@{b}

   SYNOPSIS@{ub}
        result = @{"GfxNew" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 465}( node_type );
        d0               d0

        @{"struct ExtendedNode" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File" 17} *@{"GfxNew" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 465}( @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Allocate a special graphics extended data structure (each of which
        begins with an ExtendedNode structure).  The type of structure to
        be allocated is specified by the node_type identifier.@{b}

   INPUTS@{ub}
        node_type = which type of graphics extended data structure to allocate.
                    (see gfxnodes.h for identifier definitions.)@{b}

   RESULT@{ub}
        result = a pointer to the allocated graphics node or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if the
                 allocation failed.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gfxnodes.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxnodes.h/File"} @{"GfxFree()" LINK "GfxFree"} @{"GfxAssociate()" LINK "GfxAssociate"} @{"GfxLookUp()" LINK "GfxLookUP"}

@ENDNODE
@NODE "InitArea" "graphics.library/InitArea()"
@{b}

   NAME@{ub}

        @{"InitArea" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 221} -- Initialize vector collection matrix@{b}

   SYNOPSIS@{ub}

        @{"InitArea" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 221}( areainfo, buffer, maxvectors )
                    a0          a1      d0

        void @{"InitArea" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 221}(@{"struct AreaInfo" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 19} *, void *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81});@{b}

   FUNCTION@{ub}
        This function provides initialization for the vector collection matrix
        such that it has a size of (max vectors ).  The size of the region
        pointed to by buffer (short pointer) should be five (5) times as large
        as maxvectors. This size is in bytes.  Areafills done by using @{"AreaMove" LINK "AreaMove"},
        @{"AreaDraw" LINK "AreaDraw"}, and @{"AreaEnd" LINK "AreaEnd"} must have enough space allocated in this table to
        store all the points of the largest fill. @{"AreaEllipse" LINK "AreaEllipse"} takes up two
        vectors for every call. If @{"AreaMove" LINK "AreaMove"}/Draw/Ellipse detect too many
        vectors going into the buffer they will return -1.@{b}

   INPUTS@{ub}
        areainfo - pointer to AreaInfo structure
        buffer - pointer to chunk of memory to collect vertices
        maxvectors - max number of vectors this buffer can hold@{b}

   RESULT@{ub}
        Pointers are set up to begin storage of vectors done by
        @{"AreaMove" LINK "AreaMove"}, @{"AreaDraw" LINK "AreaDraw"}, and @{"AreaEllipse" LINK "AreaEllipse"}.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"} @{"AreaMove()" LINK "AreaMove"} @{"AreaDraw()" LINK "AreaDraw"} @{"AreaEllipse()" LINK "AreaEllipse"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "InitBitMap" "graphics.library/InitBitMap()"
@{b}

   NAME@{ub}

        @{"InitBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 293} -- Initialize bit map structure with input values.@{b}

   SYNOPSIS@{ub}
        @{"InitBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 293}( bm, depth, width, height )
                    a0   d0     d1      d2

        void @{"InitBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 293}( @{"struct BitMap" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 46} *, @{"BYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 58}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} );@{b}

   FUNCTION@{ub}
        Initialize various elements in the BitMap structure to
        correctly reflect depth, width, and height.
        Must be used before use of BitMap in other graphics calls.
        The Planes[8] are not initialized and need to be set up
        by the caller.  The Planes table was put at the end of the
        structure so that it may be truncated to conserve space,
        as well as extended. All routines that use BitMap should
        only depend on existence of depth number of bitplanes.
        The Flagsh and pad fields are reserved for future use and
        should not be used by application programs.@{b}

   INPUTS@{ub}
        bm - pointer to a BitMap structure (gfx.h)
        depth - number of bitplanes that this bitmap will have
        width - number of bits (columns) wide for this BitMap
        height- number of bits (rows) tall for this BitMap@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gfx.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File"}

@ENDNODE
@NODE "InitGels" "graphics.library/InitGels()"
@{b}

   NAME@{ub}
        @{"InitGels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 113} -- initialize a gel list; must be called before using gels.@{b}

   SYNOPSIS@{ub}
        @{"InitGels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 113}(head, tail, GInfo)
                 A0    A1    A2

        void @{"InitGels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 113}(@{"struct VSprite" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 72} *, @{"struct VSprite" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 72} *, @{"struct GelsInfo" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 37} *);@{b}

   FUNCTION@{ub}
        Assigns the VSprites as the head and tail of the gel list in GfxBase.
        Links these two gels together as the keystones of the list.
        If the collHandler vector points to some memory array, sets
        the @{"BORDERHIT" LINK "gg:doc/NDK/Guide/Include/graphics/collide.h/File" 21} vector to @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.@{b}

   INPUTS@{ub}
        head  = pointer to the VSprite structure to be used as the gel list head
        tail  = pointer to the VSprite structure to be used as the gel list tail
        GInfo = pointer to the GelsInfo structure to be initialized@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "InitGMasks" "graphics.library/InitGMasks()"
@{b}

   NAME@{ub}
        @{"InitGMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 149} -- Initialize all of the masks of an AnimOb.@{b}

   SYNOPSIS@{ub}
        @{"InitGMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 149}(anOb)
                   A0

        void @{"InitGMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 149}(@{"struct AnimOb" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 204} *);@{b}

   FUNCTION@{ub}
        For every sequence of every component call @{"InitMasks" LINK "InitMasks"}.@{b}

   INPUTS@{ub}
        anOb = pointer to the AnimOb@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitMasks()" LINK "InitMasks"} @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "InitMasks" "graphics.library/InitMasks()"
@{b}

   NAME@{ub}
        @{"InitMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 117} -- Initialize the BorderLine and CollMask masks of a VSprite.@{b}

   SYNOPSIS@{ub}
        @{"InitMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 117}(vs)
                  A0

        void @{"InitMasks" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 117}(@{"struct VSprite" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 72} *);@{b}

   FUNCTION@{ub}
        Creates the appropriate BorderLine and CollMask masks of the VSprite.
        Correctly detects if the VSprite is actually a Bob definition, handles
        the image data accordingly.@{b}

   INPUTS@{ub}
        vs = pointer to the VSprite structure@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "InitRastPort" "graphics.library/InitRastPort()"
@{b}

   NAME@{ub}
        @{"InitRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 165} -- Initialize raster port structure@{b}

   SYNOPSIS@{ub}
        @{"InitRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 165}( rp )
                      a1

        void @{"InitRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 165}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *rp);@{b}

   FUNCTION@{ub}
       Initialize a RastPort structure to standard values.@{b}

   INPUTS@{ub}
        rp      = pointer to a RastPort structure.@{b}

   RESULT@{ub}
        all entries in RastPort get zeroed out, with the following exceptions:

            Mask, FgPen, AOLPen, and LinePtrn are set to -1.
            The DrawMode is set to @{"JAM2" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 91}
            The font is set to the standard system font@{b}

   NOTES@{ub}
        The struct Rastport describes a control structure
       for a write-able raster. The RastPort structure
       describes how a complete single playfield display
       will be written into. A RastPort structure is
       referenced whenever any drawing or filling
       operations are to be performed on a section of
       memory.

       The section of memory which is being used in this
       way may or may not be presently a part of the
       current actual onscreen display memory. The name
       of the actual memory section which is linked to
       the RastPort is referred to here as a "raster" or
       as a bitmap.

       NOTE: Calling the routine @{"InitRastPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 165} only
       establishes various defaults. It does @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500}
       establish where, in memory, the rasters are
       located. To do graphics with this RastPort the user
        must set up the BitMap pointer in the RastPort.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "InitTmpRas" "graphics.library/InitTmpRas()"
@{b}

   NAME@{ub}
        @{"InitTmpRas" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 345} -- Initialize area of local memory for usage by
                        areafill, floodfill, text.@{b}

   SYNOPSIS@{ub}
        @{"InitTmpRas" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 345}(tmpras, buffer, size)
                    a0       a1     d0

        void @{"InitTmpRas" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 345}( @{"struct TmpRas" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 30} *, void *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} );@{b}

   FUNCTION@{ub}
        The area of memory pointed to by buffer is set up to be used
        by RastPort routines that may need to get some memory for
        intermediate operations in preparation to putting the graphics
        into the final BitMap.
        Tmpras is used to control the usage of buffer.@{b}

   INPUTS@{ub}
        tmpras - pointer to a TmpRas structure to be linked into
                a RastPort
        buffer - pointer to a contiguous piece of chip memory.
        size - size in bytes of buffer@{b}

   RESULT@{ub}
        makes buffer available for users of RastPort@{b}

   BUGS@{ub}
        Would be nice if RastPorts could share one TmpRas.@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"} @{"Flood()" LINK "Flood"} @{"Text()" LINK "Text"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "InitView" "graphics.library/InitView()"
@{b}

   NAME@{ub}
        @{"InitView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 273} - Initialize View structure.@{b}

   SYNOPSIS@{ub}
        @{"InitView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 273}( view )
                   a1

        void @{"InitView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 273}( @{"struct View" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 64} * );@{b}

   FUNCTION@{ub}
        Initialize View structure to default values.@{b}

   INPUTS@{ub}
        view - pointer to a View structure@{b}

   RESULT@{ub}
        View structure set to all 0's. (1.0,1.1.1.2)
        Then values are put in DxOffset,DyOffset to properly position
        default display about .5 inches from top and left on monitor.
        @{"InitView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 273} pays no attention to previous contents of view.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"MakeVPort" LINK "MakeVPort"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "InitVPort" "graphics.library/InitVPort()"
@{b}

   NAME@{ub}
        @{"InitVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 169} - Initialize ViewPort structure.@{b}

   SYNOPSIS@{ub}
        @{"InitVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 169}( vp )
                   a0

        void InitViewPort( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} * );@{b}

   FUNCTION@{ub}
        Initialize ViewPort structure to default values.@{b}

   INPUTS@{ub}
        vp - pointer to a ViewPort structure@{b}

   RESULT@{ub}
        ViewPort structure set to all 0's. (1.0,1.1)
       New field added SpritePriorities, initialized to 0x24 (1.2)@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"MakeVPort()" LINK "MakeVPort"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "LoadRGB32" "graphics.library/LoadRGB32()"
@{b}

   NAME@{ub}
        @{"LoadRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 608} -- Set a series of color registers for this Viewport. (V39)@{b}

   SYNOPSIS@{ub}
        @{"LoadRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 608}(  vp,  table )
                    a0   a1

        void @{"LoadRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 608}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} *);@{b}

   INPUTS@{ub}
        vp = viewport
        table = a pointer to a series of records which describe which colors to
                modify.@{b}
   RESULT@{ub}
        The selected color registers are changed to match your specs.@{b}
   BUGS@{ub}@{b}

   NOTES@{ub}

        Passing a @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} "table" is ignored.
        The format of the table passed to this function is a series of records,
        each with the following format:

                1 Word with the number of colors to load
                1 Word with the first color to be loaded.
                3 longwords representing a left justified 32 bit rgb triplet.
                The list is terminated by a count value of 0.

           examples:
                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} table[]={1l<<16+0,0xffffffff,0,0,0} loads color register
                        0 with 100% red.
                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} table[]={256l<<16+0,r1,g1,b1,r2,g2,b2,.....0} can be used
                        to load an entire 256 color palette.

        Lower order bits of the palette specification will be discarded,
        depending on the color palette resolution of the target graphics
        device. Use 0xffffffff for the full value, 0x7fffffff for 50%,
        etc. You can find out the palette range for your screen by
        querying the graphics data base.

        @{"LoadRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 608} is faster than @{"SetRGB32" LINK "SetRGB32"}, even for one color.@{b}

   SEE ALSO@{ub}
        @{"LoadRGB4()" LINK "LoadRGB4"} @{"GetColorMap()" LINK "GetColorMap"} @{"GetRGB32()" LINK "GetRGB32"} @{"SetRGB32CM()" LINK "SetRGB32CM"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "LoadRGB4" "graphics.library/LoadRGB4()"
@{b}

   NAME@{ub}
        @{"LoadRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 161} -- Load RGB color values from table.@{b}

   SYNOPSIS@{ub}
        @{"LoadRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 161}( vp, colors , count )
                 a0     a1     d0:16

        void @{"LoadRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 161}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        load the count words of the colormap from table starting at
        entry 0.@{b}

   INPUTS@{ub}
        vp - pointer to ViewPort, whose colors you wish to change
        colors - pointer to table of RGB values set up as an array
                 of USHORTS
                        background--  0x0RGB
                        color1    --  0x0RGB
                        color2    --  0x0RGB
                         etc.         @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} per value.
                The colors are interpreted as 15 = maximum intensity.
                                              0 = minimum intensity.
        count   = number of UWORDs in the table to load into the
          colormap starting at color 0(background) and proceeding
          to the next higher color number@{b}

   RESULTS@{ub}
        The ViewPort should have a pointer to a valid ColorMap to store
        the colors in.
        Updates the hardware copperlist to reflect the new colors.
        Updates the intermediate copperlist with the new colors.@{b}

   BUGS@{ub}

        NOTE: Under V36 and up, it is not safe to call this function
        from an interrupt, due to semaphore protection of graphics
        copper lists.@{b}

   SEE ALSO@{ub}
        @{"SetRGB4()" LINK "SetRGB4"} @{"GetRGB4()" LINK "GetRGB4"} @{"GetColorMap()" LINK "GetColorMap"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "LoadView" "graphics.library/LoadView()"
@{b}

   NAME@{ub}
       @{"LoadView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 181} -- Use a (possibly freshly created) coprocessor instruction
                   list to create the current display.@{b}

   SYNOPSIS@{ub}
       @{"LoadView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 181}( View )
                  A1

        void @{"LoadView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 181}( @{"struct View" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 64} * );@{b}

   FUNCTION@{ub}
        Install a new view to be displayed during the next display
        refresh pass.
       Coprocessor instruction list has been created by
       @{"InitVPort()" LINK "InitVPort"}, @{"MakeVPort()" LINK "MakeVPort"}, and @{"MrgCop()" LINK "MrgCop"}.@{b}

   INPUTS@{ub}
       View - a pointer to the View structure which contains the
       pointer to the constructed coprocessor instructions list, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.@{b}

   RESULT@{ub}
        If the View pointer is non-@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the new View is displayed,
        according to your instructions.  The vertical blank routine
        will pick this pointer up and direct the copper to start
        displaying this View.

        If the View pointer is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, no View is displayed.@{b}

   NOTE@{ub}
        Even though a @{"LoadView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 181}(@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}) is performed, display DMA will still be
        active.  Sprites will continue to be displayed after a @{"LoadView" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 181}(@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101})
        unless an @{"OFF_SPRITE" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 42} is subsequently performed.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"InitVPort()" LINK "InitVPort"} @{"MakeVPort()" LINK "MakeVPort"} @{"MrgCop()" LINK "MrgCop"} intuition/@{"RethinkDisplay()" LINK "gg:doc/NDK/Guide/intuition/RethinkDisplay"}
        @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "LockLayerRom" "graphics.library/LockLayerRom()"
@{b}

   NAME@{ub}
        @{"LockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 321} -- Lock Layer structure by ROM(gfx lib) code.@{b}

   SYNOPSIS@{ub}
        @{"LockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 321}( layer )
                       a5

        void @{"LockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 321}( @{"struct Layer" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Return when the layer is locked and no other task may
        alter the ClipRect structure in the Layer structure.
        This call does not destroy any registers.
        This call nests so that callers in this chain will not lock
        themselves out.
        Do not have the Layer locked during a call to intuition.
        There is a potential deadlock problem here, if intuition
        needs to get other locks as well.
        Having the layer locked prevents other tasks from using the
        layer library functions, most notably intuition itself. So
        be brief.
        @{"layers.library" LINK "gg:doc/NDK/Guide/layers/MAIN"}'s @{"LockLayer" LINK "gg:doc/NDK/Guide/layers/LockLayer"} is identical to @{"LockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 321}.@{b}

   INPUTS@{ub}
        layer - pointer to Layer structure@{b}

   RESULTS@{ub}
        The layer is locked and the task can render assuming the
        ClipRects will not change out from underneath it until
        an @{"UnlockLayerRom" LINK "UnlockLayerRom"} is called.@{b}

   SEE ALSO@{ub}
        @{"UnlockLayerRom()" LINK "UnlockLayerRom"} @{"layers.library/LockLayer()" LINK "gg:doc/NDK/Guide/layers/LockLayer"} @{"graphics/clip.h" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File"}

@ENDNODE
@NODE "MakeVPort" "graphics.library/MakeVPort()"
@{b}

   NAME@{ub}
        @{"MakeVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 177} -- generate display copper list for a viewport.@{b}

   SYNOPSIS@{ub}
        error =  @{"MakeVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 177}( view, viewport )
         d0                   a0     a1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"MakeVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 177}( @{"struct View" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 64} *, @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} * );@{b}

   FUNCTION@{ub}
        Uses information in the View, ViewPort, ViewPort->RasInfo to
        construct and intermediate copper list for this ViewPort.@{b}

   INPUTS@{ub}
        view - pointer to a View structure
        viewport - pointer to a ViewPort structure
                 The viewport must have valid pointer to a RasInfo.@{b}

   RESULTS@{ub}
        constructs intermediate copper list and puts pointers in
        viewport.DspIns
        If the ColorMap ptr in ViewPort is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} then it uses colors
        from the default color table.
        If @{"DUALPF" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 129} in Modes then there must be a second RasInfo pointed
        to by the first RasInfo

        From V39, @{"MakeVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 177} can return a @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} error value (previous versions
        returned void), to indicate that either not enough memory could be
        allocated for @{"MakeVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 177}'s use, or that the ViewPort mode
        and bitplane alignments are incorrect for the bitplane's depth.

        You should check for these error values - they are defined in
        <@{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}>.@{b}


   BUGS@{ub}
        In V37 and earlier, narrow Viewports (whose righthand edge is
        less than 3/4 of the way across the display) do not work properly.@{b}

   SEE ALSO@{ub}
        @{"InitVPort()" LINK "InitVPort"} @{"MrgCop()" LINK "MrgCop"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"} @{"intuition.library/MakeScreen()" LINK "gg:doc/NDK/Guide/intuition/MakeScreen"}
        @{"intuition.library/RemakeDisplay()" LINK "gg:doc/NDK/Guide/intuition/RemakeDisplay"} @{"intuition.library/RethinkDisplay()" LINK "gg:doc/NDK/Guide/intuition/RethinkDisplay"}

@ENDNODE
@NODE "ModeNotAvailable" "graphics.library/ModeNotAvailable()"
@{b}

   NAME@{ub}
        @{"ModeNotAvailable" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 550} -- check to see if a DisplayID isn't available. (V36)@{b}

   SYNOPSIS@{ub}
        error =  @{"ModeNotAvailable" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 550}( modeID )
        d0                         d0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}   @{"ModeNotAvailable" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 550}( @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        returns an error code, indicating why this modeID is not available,
        or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if there is no reason known why this mode should not be there.@{b}

   INPUTS@{ub}
        modeID -- a 32 bit DisplayInfoRecord identifier.@{b}

   RESULT@{ub}
        error -- a general indication of why this modeID is not available,
                 or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if there is no reason why it shouldn't be available.@{b}

   NOTE@{ub}
        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} return values from this function are a proper superset of the
        DisplayInfo.NotAvailable field (defined in @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}).

        @{"ModeNotAvailable" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 550}() will return @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} when given a non-existant
        moe ID.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}, @{"GetVPModeID()" LINK "GetVPModeID"}

@ENDNODE
@NODE "Move" "graphics.library/Move()"
@{b}

   NAME@{ub}
        Move -- Move graphics pen position.@{b}

   SYNOPSIS@{ub}
        Move( rp,   x,    y)
              a1  d0:16 d1:16

        void Move( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81} );@{b}

   FUNCTION@{ub}
        Move graphics pen position to (x,y) relative to upper left (0,0)
        of RastPort. This sets the starting point for subsequent @{"Draw()" LINK "Draw"}
        and @{"Text()" LINK "Text"} calls.@{b}

   INPUTS@{ub}
        rp - pointer to a RastPort structure
        x,y - point in the RastPort@{b}

   RESULTS@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"Draw()" LINK "Draw"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "MoveSprite" "graphics.library/MoveSprite()"
@{b}

   NAME@{ub}
        @{"MoveSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 317} -- Move sprite to a point relative to top of viewport.@{b}

   SYNOPSIS@{ub}
        @{"MoveSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 317}(vp, sprite, x, y)
                   A0  A1      D0 D1

        void @{"MoveSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 317}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *,@{"struct SimpleSprite" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File" 17} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        Move sprite image to new place on display.@{b}

   INPUTS@{ub}
        vp - pointer to ViewPort structure
             if vp = 0, sprite is positioned relative to View.
        sprite - pointer to SimpleSprite structure
        (x,y)  - new position relative to top of viewport or view.@{b}

   RESULTS@{ub}
        Calculate the hardware information for the sprite and
        place it in the posctldata array. During next video display
        the sprite will appear in new position.@{b}

   BUGS@{ub}
        Sprites really appear one pixel to the left of the position you specify.
        This bug affects the apparent display position of the sprite on the
        screen, but does not affect the numeric position relative to the
        viewport or view. This behaviour only applies to SimpleSprites,
        not to ExtSprites.@{b}


   SEE ALSO@{ub}
        @{"FreeSprite()" LINK "FreeSprite"}  @{"ChangeSprite()" LINK "ChangeSprite"}  @{"GetSprite()" LINK "GetSprite"}  @{"graphics/sprite.h" LINK "gg:doc/NDK/Guide/Include/graphics/sprite.h/File"}

@ENDNODE
@NODE "MrgCop" "graphics.library/MrgCop()"
@{b}

   NAME@{ub}
       @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173} -- Merge together coprocessor instructions.@{b}

   SYNOPSIS@{ub}
       error = @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173}( View )
       d0              A1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173}( @{"struct View" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 64} * );@{b}

   FUNCTION@{ub}
       Merge together the display, color, sprite and user coprocessor
       instructions into a single coprocessor instruction stream.  This
       essentially creates a per-display-frame program for the coprocessor.
       This function @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173} is used, for example, by the graphics animation
       routines which effectively add information into an essentially
       static background display.  This changes some of the user
       or sprite instructions, but not those which have formed the
       basic display in the first place.  When all forms of coprocessor
       instructions are merged together, you will have a complete per-
       frame instruction list for the coprocessor.

       Restrictions:  Each of the coprocessor instruction lists MUST be
       internally sorted in min to max Y-X order.  The merge routines
       depend on this! Each list must be terminated using @{"CEND" LINK "CEND"}(copperlist).@{b}

   INPUTS@{ub}
       View - a pointer to the view structure whose coprocessor
              instructions are to be merged.@{b}

   RESULT@{ub}
       The view structure will now contain a complete, sorted/merged
       list of instructions for the coprocessor, ready to be used by
       the display processor.  The display processor is told to use
       this new instruction stream through the instruction @{"LoadView()" LINK "LoadView"}.

        From V39, @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173}() can return a @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} error value (previous versions
        returned void), to indicate that either there was insufficient memory
        to build the system copper lists, or that @{"MrgCop" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 173}() had no work to do
        if, for example, there were no ViewPorts in the list.

        You should check for these error values - they are defined in
        <@{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}>.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"InitVPort()" LINK "InitVPort"} @{"MakeVPort()" LINK "MakeVPort"} @{"LoadView()" LINK "LoadView"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}
        @{"intuition.library/RethinkDisplay()" LINK "gg:doc/NDK/Guide/intuition/RethinkDisplay"}

@ENDNODE
@NODE "NewRegion" "graphics.library/NewRegion()"
@{b}

   NAME@{ub}
       @{"NewRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 377} -- Get an empty region.@{b}

   SYNOPSIS@{ub}
       region = @{"NewRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 377}()
        d0

        @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *@{"NewRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 377}();@{b}

   FUNCTION@{ub}
        Create a Region structure, initialize it to empty, and return
        a pointer it.@{b}

   RESULTS@{ub}
        region - pointer to initialized region. If it could not allocate
                required memory region = @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.@{b}

   INPUTS@{ub}
        none@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "NextDisplayInfo" "graphics.library/NextDisplayInfo()"
@{b}

   NAME@{ub}
        @{"NextDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 518} -- iterate current displayinfo identifiers (V36)@{b}

   SYNOPSIS@{ub}
        next_ID = @{"NextDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 518}(last_ID)
        D0                        D0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"NextDisplayInfo" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 518}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        The basic iteration function with which to find all records in the
        graphics database.  Using each ID in succession, you can then call
        @{"FindDisplayInfo()" LINK "FindDisplayInfo"} to obtain the handle associated with each ID.
        Each ID is a 32-bit Key which uniquely identifies one record.
        The @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} is special, and indicates the end-of-list.@{b}

   INPUTS@{ub}
        last_ID - previous displayinfo identifier
                  or @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} if beginning iteration.@{b}

   RESULT@{ub}
        next_ID - subsequent displayinfo identifier
                  or @{"INVALID_ID" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 17} if no more records.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"FindDisplayInfo()" LINK "FindDisplayInfo"}, @{"GetDisplayInfoData()" LINK "GetDisplayInfoData"}
        @{"graphics/displayinfo.h" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File"}

@ENDNODE
@NODE "ObtainBestPenA" "graphics.library/ObtainBestPenA()"
@{b}

   NAME@{ub}
        @{"ObtainBestPenA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 579} --- Search for the closest color match, or allocate a
                           new one. (V39)
        @{"ObtainBestPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 584}  --- varargs stub for @{"ObtainBestPenA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 579}@{b}

   SYNOPSIS@{ub}
        color | -1 =@{"ObtainBestPenA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 579}(  cm,  R,   G,    B,    taglist)
                                     a0   d1   d2    d3       a1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"ObtainBestPenA" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 579}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},
                        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        color = @{"ObtainBestPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 584}(cm,r,g,b,tags....);@{b}

   INPUTS@{ub}
        cm = colormap
        R = red level   (32 bit left justified fraction)
        G = green level (32 bit left justified fraction)
        B = blue level  (32 bit left justified fraction)
        taglist = a pointer to a standard tag list specifying the color
                  matching settings desired:

                @{"OBP_Precision" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 245} - specifies the desired precision for the
                        match. Should be @{"PRECISION_GUI" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 241}, @{"PRECISION_ICON" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 240}, or
                        @{"PRECISION_IMAGE" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 239} or @{"PRECISION_EXACT" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 238}.
                        Defaults to @{"PRECISION_IMAGE" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 239}.

                @{"OBP_FailIfBad" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 246} - specifies that you want @{"ObtainBestPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 584} to return
                        a failure value if there is not a color within the
                        given tolerance, instead of returning the closest color.
                        With @{"OBP_FailIfBad" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 246}==@{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, @{"ObtainBestPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 584} will only fail
                        if the ViewPort contains no sharable colors.
                        Defaults to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.@{b}


   FUNCTION@{ub}
        This function can be used by applications to figure out
        what pen to use to represent a given color.

        The system will attempt to find the color in your viewport closest
        to the specified color. If there is no color within your tolerance,
        then a new one will be allocated, if available. If none is available,
        then the closest one found will be returned.@{b}

   RESULT@{ub}
        The correct pen value, or -1 if no sharable palette entries are available.@{b}


   BUGS@{ub}@{b}

   NOTES@{ub}
        If this call succceeds, then you must call @{"ReleasePen()" LINK "ReleasePen"} when you are
        done with the color.

        The error metric used for @{"ObtainBestPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 584}() is based on the magnitude
        squared between the two RGB values, scaled by the percentage of free
        entries.@{b}

   SEE ALSO@{ub}
        @{"GetColorMap()" LINK "GetColorMap"} @{"ObtainPen()" LINK "ObtainPen"} @{"ReleasePen()" LINK "ReleasePen"}

@ENDNODE
@NODE "ObtainPen" "graphics.library/ObtainPen()"
@{b}

   NAME@{ub}
        @{"ObtainPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 653} -- Obtain a free palette entry for use by your program. (V39)@{b}


   SYNOPSIS@{ub}
        n = @{"ObtainPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 653}( cm, n, r, g, b, flags)
        d0             a0 d0  d1 d2 d3  d4

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"ObtainPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 653}(@{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *,@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Attempt to allocate an entry in the colormap for use by the application.
        If successful, you should @{"ReleasePen()" LINK "ReleasePen"} this entry after you have finished
        with it.

        Applications needing exclusive use of a color register (say for color
        cycling) will typically call this function with n=-1. Applications needing
        only the shared use of a color will typically use @{"ObtainBestPenA()" LINK "ObtainBestPenA"} instead.
        Other uses of this function are rare.@{b}

   INPUTS@{ub}
        cm  =  A pointer to a color map created by @{"GetColorMap()" LINK "GetColorMap"}.
        n   =  The index of the desired entry, or -1 if any one is acceptable
        rgb =  The RGB values (32 bit left justified fractions) to set the new
               palette entry to.
        flags= @{"PEN_EXCLUSIVE" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 233} - tells the system that you want exclusive
               (non-shared) use of this pen value. Default is shared access.

               @{"PEN_NO_SETCOLOR" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 234} - tells the system to not change the rgb values
               for the selected pen. Really only makes sense for exclusive pens.@{b}


   RESULTS@{ub}

        n   =  The allocated pen. -1 will be returned if there is no pen available
               for you.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        When you allocate a palette entry in non-exclusive mode, you
        should not change it (via @{"SetRGB32" LINK "SetRGB32"}), because other programs on the
        same screen may be using it. With @{"PEN_EXCLUSIVE" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 233} mode, you can
        change the returned entry at will.

        To avoid visual artifacts, you should not free up a palette
        entry until you are sure that your application is not displaying
        any pixels in that color at the time you free it. Otherwise, another
        task could allocate and set that color index, thus changing the colors
        of your pixels.

        Generally, for shared access, you should use @{"ObtainBestPenA()" LINK "ObtainBestPenA"}
        instead, since it will not allocate a new color if there is one
        "close enough" to the one you want already.
        If there is no Palextra attached to the colormap, then this
        routine will always fail.@{b}

   SEE ALSO@{ub}
        @{"GetColorMap()" LINK "GetColorMap"} @{"ReleasePen()" LINK "ReleasePen"} @{"AttachPalExtra()" LINK "AttachPalExtra"} @{"ObtainBestPenA()" LINK "ObtainBestPenA"}

@ENDNODE
@NODE "OpenFont" "graphics.library/OpenFont()"
@{b}

   NAME@{ub}
        @{"OpenFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 81} -- Get a pointer to a system font.@{b}

   SYNOPSIS@{ub}
        font = @{"OpenFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 81}(textAttr)
        D0              A0

        @{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *@{"OpenFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 81}(@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *);@{b}

   FUNCTION@{ub}
        This function searches the system font space for the graphics
        text font that best matches the attributes specified.  The
        pointer to the font returned can be used in subsequent
        @{"SetFont" LINK "SetFont"} and @{"CloseFont" LINK "CloseFont"} calls.  It is important to match this
        call with a corresponding @{"CloseFont" LINK "CloseFont"} call for effective
        management of ram fonts.@{b}

   INPUTS@{ub}
        textAttr - a TextAttr or TTextAttr structure that describes the
                   text font attributes desired.@{b}

   RESULT@{ub}
        font is zero if the desired font cannot be found.  If the named
        font is found, but the size and style specified are not
        available, a font with the nearest attributes is returned.@{b}

   BUGS@{ub}
        Prior to V39 this function would return a TextFont pointer
        for any font which matched exactly in Y size, regardless of
        differences in DPI, or DotSize.

        As part of fixing this bug it is REQUIRED that you use pass the
        same TextAttr (or TTextAttr) to this function that was used when
        @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"} was called.

        @{"OpenFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 81}(), and @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"} use WeighTAMatch() to measure
        how well two fonts match.  WeightTAMatch() was a public function
        in graphics.library V36-V37; it is now a system PRIVATE function
        as of V39.@{b}

   SEE ALSO@{ub}
        @{"CloseFont()" LINK "CloseFont"}  @{"SetFont()" LINK "SetFont"}
        @{"diskfont.library/OpenDiskFont" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}
        @{"intuition/intuition.h" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File"}

@ENDNODE
@NODE "OpenMonitor" "graphics.library/OpenMonitor()"
@{b}

   NAME@{ub}
       @{"OpenMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 506} -- open a named MonitorSpec (V36)@{b}

   SYNOPSIS@{ub}
       mspc = @{"OpenMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 506}( monitor_name , display_id)
       d0                  a1              d0

       @{"struct MonitorSpec" LINK "gg:doc/NDK/Guide/Include/graphics/monitor.h/File" 25} *@{"OpenMonitor" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 506}( char *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} );@{b}

   FUNCTION@{ub}
       Locate and open a named MonitorSpec.@{b}

   INPUTS@{ub}
       monitor_name - a pointer to a null terminated string.
       display_id - an optional 32 bit monitor/mode identifier@{b}

   RESULTS@{ub}
       mspc - a pointer to an open MonitorSpec structure.
              @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if MonitorSpec could not  be opened.@{b}

   NOTE@{ub}
        if monitor_name is non-@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the monitor will be opened by name.
        if monitor_name is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} the monitor will be opened by optional ID.
        if both monitor_name and display_id are @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} returns default monitor.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"CloseMonitor()" LINK "CloseMonitor"} @{"graphics/monitor.h" LINK "gg:doc/NDK/Guide/Include/graphics/monitor.h/File"}

@ENDNODE
@NODE "OrRectRegion" "graphics.library/OrRectRegion()"
@{b}

   NAME@{ub}
       @{"OrRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 373} -- Perform 2d OR operation of rectangle
                       with region, leaving result in region.@{b}

   SYNOPSIS@{ub}
       status = @{"OrRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 373}(region,rectangle)
         d0                    a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"OrRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 373}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Rectangle" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 27} * );@{b}

   FUNCTION@{ub}
       If any portion of rectangle is not in the region then add
       that portion to the region.@{b}

   INPUTS@{ub}
       region - pointer to Region structure
       rectangle - pointer to Rectangle structure@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AndRectRegion()" LINK "AndRectRegion"} @{"OrRegionRegion()" LINK "OrRegionRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "OrRegionRegion" "graphics.library/OrRegionRegion()"
@{b}

   NAME@{ub}
       @{"OrRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 441} -- Perform 2d OR operation of one region
                       with second region, leaving result in second region@{b}

   SYNOPSIS@{ub}
       status = @{"OrRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 441}(region1,region2)
         d0                       a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"OrRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 441}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} * );@{b}

   FUNCTION@{ub}
       If any portion of region1  is not in the region then add
       that portion to the region2@{b}

   INPUTS@{ub}
       region1 - pointer to Region structure
       region2 - pointer to Region structure@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"OrRectRegion()" LINK "OrRectRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "OwnBlitter" "graphics.library/OwnBlitter()"
@{b}

   NAME@{ub}
       @{"OwnBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 337} -- get the blitter for private usage@{b}

   SYNOPSIS@{ub}
       @{"OwnBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 337}()

        void @{"OwnBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 337}( void );@{b}

   FUNCTION@{ub}
        If blitter is available return immediately with the blitter
        locked for your exclusive use. If the blitter is not available
        put task to sleep. It will be awakened as soon as the blitter
        is available. When the task first owns the blitter the blitter
        may still be finishing up a blit for the previous owner. You
        must do a @{"WaitBlit" LINK "WaitBlit"} before actually using the blitter registers.

        Calls to @{"OwnBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 337}() do not nest. If a task that owns the
        blitter calls @{"OwnBlitter" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 337}() again, a lockup will result.
        (Same situation if the task calls a system function
        that tries to own the blitter).@{b}

   INPUTS@{ub}
        NONE@{b}

   RETURNS@{ub}
        NONE@{b}

   SEE ALSO@{ub}
        @{"DisownBlitter()" LINK "DisownBlitter"} @{"WaitBlit()" LINK "WaitBlit"}

@ENDNODE
@NODE "PolyDraw" "graphics.library/PolyDraw()"
@{b}

   NAME@{ub}
        @{"PolyDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 257} -- Draw lines from table of (x,y) values.@{b}

   SYNOPSIS@{ub}
        @{"PolyDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 257}( rp, count , array )
                  a1   d0      a0

        void @{"PolyDraw" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 257}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} * );@{b}

   FUNCTION@{ub}
        starting with the first pair in the array, draw connected lines to
        it and every successive pair.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure
        count -  number of (x,y) pairs in the array
        array - pointer to first (x,y) pair@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"Draw()" LINK "Draw"} @{"Move()" LINK "Move"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "QBlit" "graphics.library/QBlit()"
@{b}

   NAME@{ub}

        @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217} -- Queue up a request for blitter usage@{b}

   SYNOPSIS@{ub}
        @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217}( bp )
               a1

        void @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217}( @{"struct bltnode" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 88} * );@{b}

   FUNCTION@{ub}
        Link a request for the use of the blitter to the end of the
       current blitter queue.  The pointer bp points to a blit structure
       containing, among other things, the link information, and the
       address of your routine which is to be called when the blitter
       queue finally gets around to this specific request.  When your
       routine is called, you are in control of the blitter ... it is
       not busy with anyone else's requests.  This means that you can
       directly specify the register contents and start the blitter.
       See the description of the blit structure and the uses of @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217}
       in the section titled Graphics Support in the OS Kernel Manual.
       Your code must be written to run either in supervisor or user
       mode on the 68000.@{b}

   INPUTS@{ub}
        bp - pointer to a blit structure@{b}

   RESULT@{ub}
        Your routine is called when the blitter is ready for you.
        In general requests for blitter usage through this channel are
        put in front of those who use the blitter via @{"OwnBlitter" LINK "OwnBlitter"} and
        @{"DisownBlitter" LINK "DisownBlitter"}. However for small blits there is more overhead
        using the queuer than Own/Disown Blitter.@{b}

   NOTES@{ub}
        Code which uses @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217}(), or @{"QBSBlit()" LINK "QBSBlit"} should make use of
        the pointer to a cleanup routine in the bltnode structure.
        The cleanup routine may be called on the context of an
        interrupt, therefore the routine may set a flag, and signal
        a task, but it may not call @{"FreeMem()" LINK "gg:doc/NDK/Guide/exec/FreeMem"} directly.  Use of
        the cleanup routine is the only safe way to signal that
        your bltnode has completed.@{b}

   BUGS@{ub}
        @{"QBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 217}(), and @{"QBSBlit()" LINK "QBSBlit"} have been rewritten for V39 due to
        various long standing bugs in earlier versions of this code.@{b}

   SEE ALSO@{ub}
        @{"QBSBlit()" LINK "QBSBlit"} @{"hardware/blit.h" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File"}

@ENDNODE
@NODE "QBSBlit" "graphics.library/QBSBlit()"
@{b}

   NAME@{ub}

        @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229} -- Synchronize the blitter request with the video beam.@{b}

   SYNOPSIS@{ub}

        @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229}( bsp )
                 a1

        void @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229}( @{"struct bltnode" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 88} * );@{b}

   FUNCTION@{ub}
        Call a user routine for use of the blitter, enqueued separately from
       the @{"QBlit" LINK "QBlit"} queue.  Calls the user routine contained in the blit
       structure when the video beam is located at a specified position
       onscreen.   Useful when you are trying to blit into a visible part
       of the screen and wish to perform the data move while the beam is
       not trying to display that same area.  (prevents showing part of
       an old display and part of a new display simultaneously).  Blitter
       requests on the @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229} queue take precedence over those on the
       regular blitter queue. The beam position is specified in the blitnode.@{b}

   INPUTS@{ub}
        bsp - pointer to a blit structure.  See description in the
             Graphics Support section of the manual for more info.@{b}

   RESULT@{ub}
       User routine is called when the @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229} queue reaches this
       request AND the video beam is in the specified position.
       If there are lots of blits going on and the video beam
       has wrapped around back to the top it will call all the
       remaining bltnodes as fast as it can to try and catch up.@{b}

   NOTES@{ub}
       @{"QBlit()" LINK "QBlit"}, and @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229}() have been rewritten for V39.  Queued
       blits are now handled in FIFO order.  Tasks trying to
       @{"OwnBlitter()" LINK "OwnBlitter"} are now given a fair share of the total
       blitter time available.  @{"QBSBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 229}() is no longer queued
       separately from nodes added by @{"QBlit()" LINK "QBlit"}.  This fixes the
       ordering dependencies listed under BUGS in prior autodoc
       notes.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"QBlit()" LINK "QBlit"} @{"hardware/blit.h" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File"}

@ENDNODE
@NODE "ReadPixel" "graphics.library/ReadPixel()"
@{b}

   NAME@{ub}
       @{"ReadPixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 245} -- read the pen number value of the pixel at a
                    specified x,y location within a certain RastPort.@{b}

   SYNOPSIS@{ub}
       penno = @{"ReadPixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 245}( rp,    x,    y )
         d0               a1  d0:16 d1:16

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"ReadPixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 245}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81} );@{b}

   FUNCTION@{ub}
       Combine the bits from each of the bit-planes used to describe
       a particular RastPort into the pen number selector which that
       bit combination normally forms for the system hardware selection
       of pixel color.@{b}

   INPUTS@{ub}
       rp -  pointer to a RastPort structure
       (x,y) a point in the RastPort@{b}

   RESULT@{ub}
       penno - the pen number of the pixel at (x,y) is returned.
                -1 is returned if the pixel cannot be read for some reason.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"WritePixel()" LINK "WritePixel"}     @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ReadPixelArray8" "graphics.library/ReadPixelArray8()"
@{b}

   NAME@{ub}
        @{"ReadPixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 538} -- read the pen number value of a rectangular array
        of pixels starting at a specified x,y location and continuing
        through to another x,y location within a certain RastPort. (V36)@{b}

   SYNOPSIS@{ub}
        count = @{"ReadPixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 538}(rp,xstart,ystart,xstop,ystop,array,temprp)
        D0                      A0 D0:16  D1:16  D2:16 D3:16 A2    A1

     @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"ReadPixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 538}(@{"struct  RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55},
           @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        For each pixel in a rectangular region, combine the bits from each
        of the bit-planes used to describe a particular RastPort into the pen
        number selector which that bit combination normally forms for the
        system hardware selection of pixel color.@{b}

   INPUTS@{ub}
        rp    -  pointer to a RastPort structure
        (xstart,ystart) - starting point in the RastPort
        (xstop,ystop)   - stopping point in the RastPort
        array - pointer to an array of UBYTEs from which to fetch the pixel data
                allocate at least ((((width+15)>>4)<<4)*(ystop-ystart+1)) bytes.
        temprp - temporary rastport (copy of rp with Layer set == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101},
                 temporary memory allocated for
                 temprp->BitMap with Rows set == 1,
                 temprp->BytesPerRow == (((width+15)>>4)<<1),
                 and temporary memory allocated for
                 temprp->BitMap->Planes[])@{b}

   RESULT@{ub}
        For each pixel in the array:
            Pen - (0..255) number at that position is returned
        count   - the number of pixels read.@{b}

   NOTE@{ub}
        xstop must be >= xstart
        ystop must be >= ystart@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"ReadPixel()" LINK "ReadPixel"}  @{"ReadPixelLine8()" LINK "ReadPixelLine8"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ReadPixelLine8" "graphics.library/ReadPixelLine8()"
@{b}

   NAME@{ub}
        @{"ReadPixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 530} -- read the pen number value of a horizontal line
        of pixels starting at a specified x,y location and continuing
        right for count pixels. (V36)@{b}

   SYNOPSIS@{ub}
        count = @{"ReadPixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 530}(rp,xstart,ystart,width,array,temprp)
        D0                     A0 D0:16  D1:16  D2    A2    A1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"ReadPixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 530}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55},
             @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} * );@{b}

   FUNCTION@{ub}
        For each pixel in a rectangular region, combine the bits from each
        of the bit-planes used to describe a particular RastPort into the pen
        number selector which that bit combination normally forms for the
        system hardware selection of pixel color.@{b}

   INPUTS@{ub}
        rp    - pointer to a RastPort structure
        (x,y) - a point in the RastPort
        width - count of horizontal pixels to read
        array - pointer to an array of UBYTEs from which to fetch the pixel data
                allocate at least (((width+15)>>4)<<4) bytes.
        temprp - temporary rastport (copy of rp with Layer set == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101},
                 temporary memory allocated for
                 temprp->BitMap with Rows set == 1,
                 temprp->BytesPerRow == (((width+15)>>4)<<1),
                 and temporary memory allocated for
                 temprp->BitMap->Planes[])@{b}

   RESULT@{ub}
        For each pixel in the array:
            Pen - (0..255) number at that position is returned
        count   - the number of pixels read.@{b}

   NOTE@{ub}
        width must be non negative@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"ReadPixel()" LINK "ReadPixel"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "RectFill" "graphics.library/RectFill()"
@{b}

   NAME@{ub}
       @{"RectFill" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 237} -- Fill a rectangular region in a RastPort.@{b}

   SYNOPSIS@{ub}

        @{"RectFill" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 237}( rp, xmin, ymin, xmax, ymax)
                 a1  d0:16 d1:16 d2:16 d3:16

        void @{"RectFill" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 237}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81} );@{b}

   FUNCTION@{ub}
        Fills  the  rectangular  region  specified  by  the
        parameters  with the chosen pen  colors,  areafill
        pattern, and drawing mode. If no areafill pattern is
        specified, fill the rectangular region with the FgPen
        color, taking into account the drawing mode.@{b}

   INPUTS@{ub}
        rp - pointer to a RastPort structure
        (xmin,ymin) (xmax,ymax) are the coordinates of the upper
                left corner and the lower right corner, respectively, of the
                rectangle.@{b}
   NOTE@{ub}

        The following relation MUST be true:
                (xmax >= xmin) and (ymax >= ymin)@{b}

   BUGS@{ub}
        Complement mode with FgPen complements all bitplanes.@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ReleasePen" "graphics.library/ReleasePen()"
@{b}

   NAME@{ub}
        @{"ReleasePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 649} -- Release an allocated palette entry to the free pool. (V39)@{b}


   SYNOPSIS@{ub}
        @{"ReleasePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 649}( cm, n)
                    a0 d0

        void @{"ReleasePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 649}( Struct ColorMap *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Return the palette entry for use by other applications.
        If the reference count for this palette entry goes to zero,
        then it may be reset to another RGB value.@{b}

   INPUTS@{ub}
       cm  =  A pointer to a color map created by @{"GetColorMap()" LINK "GetColorMap"}.

        n   =  A palette index obtained via any of the palette allocation
               functions. Passing a -1 will result in this call doing
               nothing.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        This function works for both shared and exclusive palette entries.@{b}

   SEE ALSO@{ub}
        @{"GetColorMap()" LINK "GetColorMap"} @{"ObtainPen()" LINK "ObtainPen"} @{"ObtainBestPenA()" LINK "ObtainBestPenA"}

@ENDNODE
@NODE "RemBob" "graphics.library/RemBob()"
@{b}

   NAME@{ub}
        @{"RemBob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 253} -- Macro to remove a Bob from the gel list.@{b}

   SYNOPSIS@{ub}
        @{"RemBob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 253}(bob)

        @{"RemBob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 253}(@{"struct Bob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 139} *);@{b}

   FUNCTION@{ub}
        Marks a Bob as no-longer-required.  The gels internal code then
        removes the Bob from the list of active gels the next time
        @{"DrawGList" LINK "DrawGList"} is executed. This is implemented as a macro.
        If the user is double-buffering the Bob, it could take two
        calls to @{"DrawGList" LINK "DrawGList"} before the Bob actually disappears from
        the RastPort.@{b}

   INPUTS@{ub}
        Bob = pointer to the Bob to be removed@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"RemIBob()" LINK "RemIBob"}  @{"DrawGList()" LINK "DrawGList"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"}

@ENDNODE
@NODE "RemFont" "graphics.library/RemFont()"
@{b}

   NAME@{ub}
        @{"RemFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 357} -- Remove a font from the system list.@{b}

   SYNOPSIS@{ub}
        @{"RemFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 357}(textFont)
                A1

        void @{"RemFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 357}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *);@{b}

   FUNCTION@{ub}
        This function removes a font from the system, ensuring that
        access to it is restricted to those applications that
        currently have an active pointer to it: i.e. no new @{"SetFont" LINK "SetFont"}
        requests to this font are satisfied.@{b}

   INPUTS@{ub}
        textFont - the TextFont structure to remove.@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetFont()" LINK "SetFont"}  @{"AddFont()" LINK "AddFont"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "RemIBob" "graphics.library/RemIBob()"
@{b}

   NAME@{ub}
        @{"RemIBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 121} -- Immediately remove a Bob from the gel list and the RastPort.@{b}

   SYNOPSIS@{ub}
        @{"RemIBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 121}(bob, rp, vp)
                A0   A1  A2

        void @{"RemIBob" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 121}(@{"struct Bob" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 139} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *);@{b}

   FUNCTION@{ub}
        Removes a Bob immediately by uncoupling it from the gel list and
        erases it from the RastPort.@{b}

   INPUTS@{ub}
        bob = pointer to the Bob to be removed
        rp  = pointer to the RastPort if the Bob is to be erased
        vp  = pointer to the ViewPort for beam-synchronizing@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"RemVSprite()" LINK "RemVSprite"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "RemVSprite" "graphics.library/RemVSprite()"
@{b}

   NAME@{ub}
        @{"RemVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 125} -- Remove a VSprite from the current gel list.@{b}

   SYNOPSIS@{ub}
        @{"RemVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 125}(vs)
                   A0

        void @{"RemVSprite" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 125}(@{"struct VSprite" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File" 72} *);@{b}

   FUNCTION@{ub}
        Unlinks the VSprite from the current gel list.@{b}

   INPUTS@{ub}
        vs = pointer to the VSprite structure to be removed from the gel list@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"RemIBob()" LINK "RemIBob"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}

@ENDNODE
@NODE "ScalerDiv" "graphics.library/ScalerDiv()"
@{b}

   NAME@{ub}
        @{"ScalerDiv" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 481} -- Get the scaling result that @{"BitMapScale" LINK "BitMapScale"} would. (V36)@{b}

   SYNOPSIS@{ub}
        result = @{"ScalerDiv" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 481}(factor, numerator, denominator)
        D0                 D0      D1         D2

        @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} @{"ScalerDiv" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 481}(@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55});@{b}

   FUNCTION@{ub}
        Calculate the expression (factor*numerator/denominator) such
        that the result is the same as the width of the destination
        result of @{"BitMapScale" LINK "BitMapScale"} when the factor here is the width of
        the source, and the numerator and denominator are the
        XDestFactor and XSrcFactor for @{"BitMapScale" LINK "BitMapScale"}.@{b}

   INPUTS@{ub}
        factor                 - a number in the range 0..16383
        numerator, denominator - numbers in the range 1..16383@{b}

   RESULT@{ub}
        this returns factor*numerator/denominator

@ENDNODE
@NODE "ScrollRaster" "graphics.library/ScrollRaster()"
@{b}

   NAME@{ub}
        @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297} -- Push bits in rectangle in raster around by
                        dx,dy towards 0,0 inside rectangle.@{b}
   SYNOPSIS@{ub}
        @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297}(rp, dx, dy, xmin, ymin, xmax, ymax)
                     A1  D0  D1  D2    D3    D4    D5

        void @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297}
             (@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        Move the bits in the raster by (dx,dy) towards (0,0)
        The space vacated is RectFilled with BGPen.
        Limit the scroll operation to the rectangle defined
        by (xmin,ymin)(xmax,ymax). Bits outside will not be
        affected. If xmax,ymax is outside the rastport then use
        the lower right corner of the rastport.
        If you are dealing with a SimpleRefresh layered RastPort you
        should check rp->Layer->Flags & @{"LAYERREFRESH" LINK "gg:doc/NDK/Guide/Include/graphics/layers.h/File" 24} to see if
        there is any damage in the damage list.  If there is you should
        call the appropriate @{"BeginRefresh" LINK "gg:doc/NDK/Guide/intuition/BeginRefresh"}(Intuition) or @{"BeginUpdate" LINK "gg:doc/NDK/Guide/layers/BeginUpdate"}(graphics)
        routine sequence.@{b}

   INPUTS@{ub}
        rp - pointer to a RastPort structure
        dx,dy are integers that may be positive, zero, or negative
        xmin,ymin - upper left of bounding rectangle
        xmax,ymax - lower right of bounding rectangle@{b}

   EXAMPLE@{ub}
        @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297}(rp,0,1,minx,miny,maxx,maxy) /* shift raster up by one row */
        @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297}(rp,-1,-1,minx,miny,maxx,maxy)
                       /* shift raster down and to the right by 1 pixel@{b}

   BUGS@{ub}
        In 1.2/V1.3 if you @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297} a SUPERBITMAP exactly left or
        right, and there is no TmpRas attached to the RastPort, the system
        will allocate one for you, but will never free it or record its
        location. This bug has been fixed for V36.  The workaround for
        1.2/1.3 is to attach a valid TmpRas of size at least
        @{"MAXBYTESPERROW" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File" 23} to the RastPort before the call.

        Beginning with V36 @{"ScrollRaster" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 297} adds the shifted areas into the
        damage list for @{"SIMPLE_REFRESH" LINK "gg:doc/NDK/Guide/Include/intuition/iobsolete.h/File" 148} windows. Due to unacceptable
        system overhead, the decision was made @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} to propagate this
        shifted area damage for @{"SMART_REFRESH" LINK "gg:doc/NDK/Guide/Include/intuition/iobsolete.h/File" 147} windows.@{b}

   SEE ALSO@{ub}
        @{"ScrollRasterBF()" LINK "ScrollRasterBF"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ScrollRasterBF" "graphics.library/ScrollRasterBF()"
@{b}

   NAME@{ub}
        @{"ScrollRasterBF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 685} -- Push bits in rectangle in raster around by
                        dx,dy towards 0,0 inside rectangle. Newly empty areas
                     will be filled via @{"EraseRect()" LINK "EraseRect"}. (V39)@{b}

   SYNOPSIS@{ub}
        @{"ScrollRasterBF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 685}(rp, dx, dy, xmin, ymin, xmax, ymax)
                     A1  D0  D1  D2    D3    D4    D5

        void @{"ScrollRasterBF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 685}
             (@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        Move the bits in the raster by (dx,dy) towards (0,0)
        The space vacated is filled by calling @{"EraseRect()" LINK "EraseRect"}.
        Limit the scroll operation to the rectangle defined
        by (xmin,ymin)(xmax,ymax). Bits outside will not be
        affected. If xmax,ymax is outside the rastport then use
        the lower right corner of the rastport.
        If you are dealing with a SimpleRefresh layered RastPort you
        should check rp->Layer->Flags & @{"LAYERREFRESH" LINK "gg:doc/NDK/Guide/Include/graphics/layers.h/File" 24} to see if
        there is any damage in the damage list.  If there is you should
        call the appropriate @{"BeginRefresh" LINK "gg:doc/NDK/Guide/intuition/BeginRefresh"}(Intuition) or @{"BeginUpdate" LINK "gg:doc/NDK/Guide/layers/BeginUpdate"}(graphics)
        routine sequence.@{b}

   INPUTS@{ub}
        rp - pointer to a RastPort structure
        dx,dy are integers that may be positive, zero, or negative
        xmin,ymin - upper left of bounding rectangle
        xmax,ymax - lower right of bounding rectangle@{b}

   NOTES@{ub}

        This call is exactly the same as @{"ScrollRaster" LINK "ScrollRaster"}, except that it calls
        @{"EraseRect()" LINK "EraseRect"} instead of @{"RectFill()" LINK "RectFill"} when clearing the newly exposed
        area. This allows use of a custom layer backfill hook.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"ScrollRaster()" LINK "ScrollRaster"} @{"EraseRect()" LINK "EraseRect"} @{"intuition.library/ScrollWindowRaster()" LINK "gg:doc/NDK/Guide/intuition/ScrollWindowRaster"}
        @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "ScrollVPort" "graphics.library/ScrollVPort()"
@{b}

   NAME@{ub}
        @{"ScrollVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 425} -- Reinterpret RasInfo information in ViewPort to reflect
                        the current Offset values.@{b}

   SYNOPSIS@{ub}
        @{"ScrollVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 425}( vp )
                     a0

        void @{"ScrollVPort" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 425}(@{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *vp);@{b}

   FUNCTION@{ub}
        After the programmer has adjusted the Offset values in
        the RasInfo structures of ViewPort, change the
        the copper lists to reflect the the Scroll positions.
        Changing the BitMap ptr in RasInfo and not changing the
        the Offsets will effect a double buffering affect.@{b}

   INPUTS@{ub}
       vp - pointer to a ViewPort structure
             that is currently be displayed.@{b}
   RESULTS@{ub}
        modifies hardware and intermediate copperlists to reflect
        new RasInfo@{b}

   BUGS@{ub}
       pokes not fast enough to avoid some visible hashing of display (V37)
        This function was re-written in V39 and is ~10 times faster than
        before.@{b}

   SEE ALSO@{ub}
        @{"MakeVPort()" LINK "MakeVPort"} @{"MrgCop()" LINK "MrgCop"} @{"LoadView()" LINK "LoadView"}  @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "SetABPenDrMd" "graphics.library/SetABPenDrMd()"
@{b}

   NAME@{ub}
        @{"SetABPenDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 616} -- Set pen colors and draw mode for a RastPort. (V39)@{b}

   SYNOPSIS@{ub}
        @{"SetABPenDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 616}( rp, apen, bpen, mode )
                                  a1  d0     d1    d2

        void @{"SetABPenDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 616}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} );@{b}

   FUNCTION@{ub}
        Set the pen values and drawing mode for lines, fills and text.
        Get the bit definitions from rastport.h@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure.
        apen - primary pen value
        bpen - secondary pen value
        mode - 0-255, some combinations may not make much sense.@{b}

   RESULT@{ub}
        The mode set is dependent on the bits selected.
        Changes minterms to reflect new drawing mode and colors.
        Sets line drawer to restart pattern.

        NOTES
        This call is essentially the same as a sequence of
        @{"SetAPen()" LINK "SetAPen"}/@{"SetBPen()" LINK "SetBPen"}/SetDrMD() calls, except that it is
        significantly faster. The minterms will only be generated
        once, or not at all if nothing changed (warning to illegal
        RastPort pokers!).@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetAPen()" LINK "SetAPen"} @{"SetBPen()" LINK "SetBPen"} @{"SetDrMd()" LINK "SetDrMd"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetAPen" "graphics.library/SetAPen()"
@{b}

   NAME@{ub}
        @{"SetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 261} -- Set the primary pen for a RastPort.@{b}

   SYNOPSIS@{ub}
        @{"SetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 261}( rp, pen )
                 a1  d0

        void @{"SetAPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 261}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   FUNCTION@{ub}
        Set the primary drawing pen for lines, fills, and text.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure.
        pen - (0-255)@{b}

   RESULT@{ub}
        Changes the minterms in the RastPort to reflect new primary pen.
        Sets line drawer to restart pattern.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetBPen()" LINK "SetBPen"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetBPen" "graphics.library/SetBPen()"
@{b}

   NAME@{ub}
        @{"SetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 265} -- Set secondary pen for a RastPort@{b}

   SYNOPSIS@{ub}
        @{"SetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 265}( rp, pen )
                 a1  d0

        void @{"SetBPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 265}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   FUNCTION@{ub}
        Set the secondary drawing pen for lines, fills, and text.@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure.
        pen - (0-255)@{b}

   RESULT@{ub}
        Changes the minterms in the RastPort to reflect new secondary pen.
        Sets line drawer to restart pattern.@{b}

   BUGS@{ub}@{b}

    SEE ALSO@{ub}
        @{"SetAPen()" LINK "SetAPen"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetChipRev" "graphics.library/SetChipRev()"
@{b}

   NAME@{ub}
        @{"SetChipRev" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 612} -- turns on the features of a Chip Set  (V39)@{b}

   SYNOPSIS@{ub}
        chiprevbits = @{"SetChipRev" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 612}(ChipRev)
                                   d0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"SetChipRev" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 612}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        Enables the features of the requested Chip Set if available,
        and updates the graphics database accordingly.@{b}

   INPUTS@{ub}
        ChipRev - Chip Rev that you would like to be enabled.@{b}

   RESULT@{ub}
        chiprevbits - Actual bits set in GfxBase->ChipRevBits0.@{b}

   NOTES@{ub}
        This routine should only be called once. It will be called by the system
        in the startup-sequence, but is included in the autodocs for authors
        of bootblock-games that wish to take advantage of post-ECS features.@{b}

   SEE ALSO@{ub}
        <@{"graphics/gfxbase.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxbase.h/File"}>

@ENDNODE
@NODE "SetCollision" "graphics.library/SetCollision()"
@{b}

   NAME@{ub}
        @{"SetCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 129} -- Set a pointer to a user collision routine.@{b}

   SYNOPSIS@{ub}
        @{"SetCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 129}(num, routine, GInfo)
                     D0   A0       A1

        void @{"SetCollision" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 129}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} (*)(), @{"struct GelsInfo" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 37} *);@{b}

   FUNCTION@{ub}
        Sets a specified entry (num) in the user's collision vectors table
        equal to the address of the specified collision routine.@{b}

   INPUTS@{ub}
        num     = collision vector number
        routine = pointer to the user's collision routine
        GInfo   = pointer to a GelsInfo structure@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"graphics/gels.h" LINK "gg:doc/NDK/Guide/Include/graphics/gels.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetDrMd" "graphics.library/SetDrMd()"
@{b}

   NAME@{ub}
        @{"SetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 269} -- Set drawing mode for a RastPort@{b}

   SYNOPSIS@{ub}
        @{"SetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 269}( rp, mode )
                 a1  d0:8

        void @{"SetDrMd" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 269}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   FUNCTION@{ub}
        Set the drawing mode for lines, fills and text.
        Get the bit definitions from rastport.h@{b}

   INPUTS@{ub}
        rp - pointer to RastPort structure.
        mode - 0-255, some combinations may not make much sense.@{b}

   RESULT@{ub}
        The mode set is dependent on the bits selected.
        Changes minterms to reflect new drawing mode.
        Sets line drawer to restart pattern.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetAPen()" LINK "SetAPen"} @{"SetBPen()" LINK "SetBPen"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetFont" "graphics.library/SetFont()"
@{b}

   NAME@{ub}
        @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77} -- Set the text font and attributes in a RastPort.@{b}

   SYNOPSIS@{ub}
        @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77}(rp, font)
                A1   A0

        void @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *);@{b}

   FUNCTION@{ub}
        This function sets the font in the RastPort to that described
        by font, and updates the text attributes to reflect that
        change.  This function clears the effect of any previous
        soft styles.@{b}

   INPUTS@{ub}
        rp   - the RastPort in which the text attributes are to be changed
        font - pointer to a TextFont structure returned from @{"OpenFont()" LINK "OpenFont"}
               or @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}@{b}

   RESULT@{ub}@{b}

   NOTES@{ub}
        This function had previously been documented that it would
        accept a null font.  This practice is discouraged.
        o   Use of a RastPort with a null font with text routines has
            always been incorrect and risked the guru.
        o   Keeping an obsolete font pointer in the RastPort is no more
            dangerous than keeping a zero one there.
        o   @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77}(rp, 0) causes spurious low memory accesses under
            some system software releases.

        As of V36, the following Amiga font variants are no longer
        directly supported:
            fonts with @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharSpace and non-@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharKern.
            fonts with non-@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharSpace and @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharKern.
            fonts with @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharSpace and @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} tf_CharKern with
                a tf_CharLoc size component greater than tf_XSize.
        Attempts to @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77} these one of these font variants will
        cause the system to modify your font to make it acceptable.@{b}

   BUGS@{ub}
        Calling @{"SetFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 77}() on in-code TextFonts (ie fonts not
        @{"OpenFont()" LINK "OpenFont"}ed) will result in a loss of 24 bytes from
        the system as of V36.
        This can be resolved by calling @{"StripFont()" LINK "StripFont"}.@{b}

   SEE ALSO@{ub}
        @{"OpenFont()" LINK "OpenFont"}  @{"StripFont()" LINK "StripFont"}
        @{"diskfont.library/OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "SetMaxPen" "graphics.library/SetMaxPen()"
@{b}

   NAME@{ub}
       @{"SetMaxPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 677} -- set maximum pen value for a rastport (V39).@{b}


   SYNOPSIS@{ub}
       @{"SetMaxPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 677} ( rp, maxpen)
                    a0   d0

        void @{"SetMaxPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 677}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *,@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52})@{b}

   FUNCTION@{ub}
        This will instruct the graphics library that the owner of the rastport
        will not be rendering in any colors whose index is >maxpen. If there
        are any speed optimizations which the graphics device can make based
        on this fact (for instance, setting the pixel write mask), they will
        be done.

        Basically this call sets the rastport mask, if this would improve
        speed. On devices where masking would slow things down (like with
        chunky pixels), it will be a no-op.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.
        maxpen =  a longword pen value.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        The maximum pen value passed must take into account not only which
        colors you intend to render in the future, but what colors you will
        be rendering on top of.
        @{"SetMaxPen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 677}(rp,0) doesn't make much sense.@{b}

   SEE ALSO@{ub}
        @{"SetWriteMask()" LINK "SetWriteMask"}

@ENDNODE
@NODE "SetOPen" "graphics.library/SetOPen()"
@{b}

   NAME@{ub}
        @{"SetOPen" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 50} -- Change the Area OutLine pen and turn on Outline
                        mode for areafills.@{b}

   SYNOPSIS@{ub}
        @{"SetOPen" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 50}(rp, pen)

        void @{"SetOPen" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File" 50}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   FUNCTION@{ub}
        This is implemented as a c-macro.
        Pen is the pen number that will be used to draw a border
        around an areafill during @{"AreaEnd()" LINK "AreaEnd"}.@{b}

   INPUTS@{ub}
        rp = pointer to RastPort structure
        pen = number  between 0-255@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AreaEnd()" LINK "AreaEnd"} @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetOutlinePen" "graphics.library/SetOutlinePen()"
@{b}

   NAME@{ub}
       @{"SetOutlinePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 669} -- Set the Outline Pen value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       old_pen=@{"SetOutlinePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 669}  ( rp, pen )
          d0                     a0   d0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"SetOutlinePen" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 669}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *,@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52})@{b}

   FUNCTION@{ub}
        Set the current value of the O pen for the rastport and turn on area outline
        mode. This function should be used instead of poking the structure directly,
        because future graphics devices may store it differently, for instance,
        using more bits.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.
        pen =  a longword pen number

        returns the previous outline pen@{b}
   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"GetOPen()" LINK "GetOPen"} @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"}

@ENDNODE
@NODE "SetRast" "graphics.library/SetRast()"
@{b}

   NAME@{ub}
       @{"SetRast" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 189} - Set an entire drawing area to a specified color.@{b}

   SYNOPSIS@{ub}
       @{"SetRast" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 189}( rp, pen )
                a1  d0

        void @{"SetRast" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 189}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   FUNCTION@{ub}
       Set the entire contents of the specified RastPort to the
       specified pen.@{b}

   INPUTS@{ub}
       rp - pointer to RastPort structure
       pen - the pen number (0-255) to jam into bitmap@{b}

   RESULT@{ub}
       All pixels within the drawing area are set to the
        selected pen number.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"RectFill()" LINK "RectFill"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "SetRGB32" "graphics.library/SetRGB32()"
@{b}

   NAME@{ub}
        @{"SetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 588} -- Set one color register for this Viewport. (V39)@{b}

   SYNOPSIS@{ub}
        @{"SetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 588}(  vp,  n,   r,    g,    b)
                   a0  d0   d1    d2    d3

        void @{"SetRGB32" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 588}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} );@{b}

   INPUTS@{ub}
        vp = viewport
        n = the number of the color register to set.
        r = red level   (32 bit left justified fraction)
        g = green level (32 bit left justified fraction)
        b = blue level  (32 bit left justified fraction)@{b}

   RESULT@{ub}
        If there is a ColorMap for this viewport, then the value will
        be stored in the ColorMap.
        The selected color register is changed to match your specs.
        If the color value is unused then nothing will happen.@{b}

   BUGS@{ub}@{b}

   NOTES@{ub}
        Lower order bits of the palette specification will be discarded,
        depending on the color palette resolution of the target graphics
        device. Use 0xffffffff for the full value, 0x7fffffff for 50%,
        etc. You can find out the palette range for your screen by
        querying the graphics data base.@{b}

   SEE ALSO@{ub}
        @{"GetColorMap()" LINK "GetColorMap"} @{"GetRGB32()" LINK "GetRGB32"} @{"SetRGB32CM()" LINK "SetRGB32CM"} @{"LoadRGB32()" LINK "LoadRGB32"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "SetRGB32CM" "graphics.library/SetRGB32CM()"
@{b}

   NAME@{ub}
       @{"SetRGB32CM" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 681} -- Set one color register for this ColorMap. (V39)@{b}

   SYNOPSIS@{ub}
       @{"SetRGB32CM" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 681}(  cm,  n,   r,    g,    b)
                    a0  d0   d1    d2    d3

       void @{"SetRGB4CM" LINK "SetRGB4CM"}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} , @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   INPUTS@{ub}
        cm = colormap
       n = the number of the color register to set. Must not exceed the numbe
        r of colors
                    allocated for the colormap.
               r = red level (32 bit unsigned left justified fraction)
               g = green level
               b = blue level@{b}

   RESULT@{ub}
        Store the (r,g,b) triplet at index n of the ColorMap structure.
       This function can be used to set up a ColorMap before before
        linking it into a viewport.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"GetColorMap()" LINK "GetColorMap"} @{"GetRGB32()" LINK "GetRGB32"} @{"SetRGB32()" LINK "SetRGB32"} @{"SetRGB4CM()" LINK "SetRGB4CM"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "SetRGB4" "graphics.library/SetRGB4()"
@{b}

    NAME@{ub}
       @{"SetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 225} -- Set one color register for this viewport.@{b}

    SYNOPSIS@{ub}
       @{"SetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 225}(  vp, n,   r,    g,    b)
                 a0  d0  d1:4  d2:4  d3:4

        void @{"SetRGB4" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 225}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

    FUNCTION@{ub}
        Change the color look up table so that this viewport displays
        the color (r,g,b) for pen number n.@{b}

    INPUTS@{ub}
        vp - pointer to  viewport structure
       n - the color number (range from 0 to 31)
       r - red level (0-15)
       g - green level (0-15)
       b - blue level (0-15)@{b}

    RESULT@{ub}
        If there is a ColorMap for this viewport, then the value will
        be stored in the ColorMap.
       The selected color register is changed to match your specs.
        If the color value is unused then nothing will happen.@{b}

    BUGS@{ub}

        NOTE: Under V36 and up, it is not safe to call this function
        from an interrupt, due to semaphore protection of graphics
        copper lists.@{b}

    SEE ALSO@{ub}
       @{"LoadRGB4()" LINK "LoadRGB4"} @{"GetRGB4()" LINK "GetRGB4"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "SetRGB4CM" "graphics.library/SetRGB4CM()"
@{b}

   NAME@{ub}
       @{"SetRGB4CM" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 453} -- Set one color register for this ColorMap.@{b}

   SYNOPSIS@{ub}
       @{"SetRGB4CM" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 453}(  cm,  n,   r,    g,    b)
                   a0  d0  d1:4  d2:4  d3:4

       void @{"SetRGB4CM" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 453}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} );@{b}

   INPUTS@{ub}
        cm = colormap
       n = the number of the color register to set. Ranges from 0 to 31
            on current Amiga displays.
       r = red level (0-15)
       g = green level (0-15)
       b = blue level (0-15)@{b}

   RESULT@{ub}
        Store the (r,g,b) triplet at index n of the ColorMap structure.
       This function can be used to set up a ColorMap before before
        linking it into a viewport.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"GetColorMap()" LINK "GetColorMap"} @{"GetRGB4()" LINK "GetRGB4"} @{"SetRGB4()" LINK "SetRGB4"} @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}

@ENDNODE
@NODE "SetRPAttrA" "graphics.library/SetRPAttrA()"
@{b}

   NAME@{ub}
       SetRPAttrA -- modify rastport settings via a tag list
        @{"SetRPAttrs" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 720}  -- varargs stub for SetRPAttrA@{b}

   SYNOPSIS@{ub}
       SetRPAttrA(rp,tags)
                  a0   a1

        void SetRPAttrA(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        @{"SetRPAttrs" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 720}(rp,tag,...);@{b}

   FUNCTION@{ub}
       Modify settings of a rastport, based on the taglist passed.
        currently available tags are:

                @{"RPTAG_Font" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 11}              Font for @{"Text()" LINK "Text"}
                RPTAG_SoftStyle         style for text (see @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"})
                @{"RPTAG_APen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 12}              Primary rendering pen
                @{"RPTAG_BPen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 13}              Secondary rendering pen
                @{"RPTAG_DrMd" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 14}              Drawing mode (see @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"})
                @{"RPTAG_OutLinePen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 15}        Area Outline pen
                @{"RPTAG_WriteMask" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 17}         Bit Mask for writing.
                @{"RPTAG_MaxPen" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File" 18}            Maximum pen to render (see @{"SetMaxPen()" LINK "SetMaxPen"})@{b}

   INPUTS@{ub}
        rp - pointer to the RastPort to modify.
        tags - a standard tag list@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"SetFont()" LINK "SetFont"} @{"SetSoftStyle()" LINK "SetSoftStyle"} @{"SetAPen()" LINK "SetAPen"} @{"SetBPen()" LINK "SetBPen"} @{"SetDrMd()" LINK "SetDrMd"} SetOutLinePen()
        @{"SetWriteMask()" LINK "SetWriteMask"} @{"SetMaxPen()" LINK "SetMaxPen"} @{"GetRPAttrA()" LINK "GetRPAttrA"} @{"graphics/rpattr.h" LINK "gg:doc/NDK/Guide/Include/graphics/rpattr.h/File"}

@ENDNODE
@NODE "SetSoftStyle" "graphics.library/SetSoftStyle()"
@{b}

   NAME@{ub}
        @{"SetSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 93} -- Set the soft style of the current font.@{b}

   SYNOPSIS@{ub}
        newStyle = @{"SetSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 93}(rp, style, enable)
        D0                      A1  D0     D1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"SetSoftStyle" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 93}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}, @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52});@{b}

   FUNCTION@{ub}
        This function alters the soft style of the current font.  Only
        those bits that are also set in enable are affected.  The
        resulting style is returned, since some style request changes
        will not be honored when the implicit style of the font
        precludes changing them.@{b}

   INPUTS@{ub}
        rp     - the RastPort from which the font and style
                 are extracted.
        style  - the new font style to set, subject to enable.
        enable - those bits in style to be changed.  Any set bits here
                 that would not be set as a result of @{"AskSoftStyle" LINK "AskSoftStyle"} will
                 be ignored, and the newStyle result will not be as
                 expected.@{b}

   RESULTS@{ub}
        newStyle - the resulting style, both as a result of previous
                   soft style selection, the effect of this function,
                   and the style inherent in the set font.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"AskSoftStyle()" LINK "AskSoftStyle"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}

@ENDNODE
@NODE "SetWriteMask" "graphics.library/SetWriteMask()"
@{b}

   NAME@{ub}
       @{"SetWriteMask" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 673} -- Set the pixel write mask value for a RastPort (V39).@{b}


   SYNOPSIS@{ub}
       success=@{"SetWriteMask" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 673} ( rp, msk )
          d0                   a0   d0

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"SetWriteMask" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 673}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *,@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52})@{b}

   FUNCTION@{ub}
        Set the current value of the bit write mask for the rastport.
        bits of the pixel with zeros in their mask will not be modified by
        subsequent drawing operations.@{b}

   INPUTS@{ub}
       rp  =  a pointer to a valid RastPort structure.
        msk =  a longword mask value.

        Graphics devices which do not support per-bit masking will
        return 0 (failure).@{b}
   BUGS@{ub}@{b}

   NOTES@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/gfxmacros.h" LINK "gg:doc/NDK/Guide/Include/graphics/gfxmacros.h/File"}

@ENDNODE
@NODE "SortGList" "graphics.library/SortGList()"
@{b}

   NAME@{ub}
        @{"SortGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 133} -- Sort the current gel list, ordering its y,x coordinates.@{b}

   SYNOPSIS@{ub}
        @{"SortGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 133}(rp)
                  A1

        void @{"SortGList" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 133}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        Sorts the current gel list according to the gels' y,x coordinates.
        This sorting is essential before calls to @{"DrawGList" LINK "DrawGList"} or @{"DoCollision" LINK "DoCollision"}.@{b}

   INPUTS@{ub}
        rp = pointer to the RastPort structure containing the GelsInfo@{b}

   RESULT@{ub}@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"InitGels()" LINK "InitGels"}  @{"DoCollision()" LINK "DoCollision"}  @{"DrawGList()" LINK "DrawGList"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "StripFont" "graphics.library/StripFont()"
@{b}

   NAME@{ub}
        @{"StripFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 567} -- remove the @{"tf_Extension" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 113} from a font (V36)@{b}

   SYNOPSIS@{ub}
        @{"StripFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 567}(font)
                  A0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"StripFont" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 567}(@{"struct TextFont" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 89} *);

@ENDNODE
@NODE "SyncSBitMap" "graphics.library/SyncSBitMap()"
@{b}

   NAME@{ub}
       @{"SyncSBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 329} --   Syncronize Super BitMap with whatever is
                        in the standard Layer bounds.@{b}

   SYNOPSIS@{ub}
       @{"SyncSBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 329}( layer )
                      a0

        void @{"SyncSBitMap" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 329}( @{"struct Layer" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File" 27} * );@{b}

   FUNCTION@{ub}
       Copy all bits from ClipRects in Layer into Super BitMap
        BitMap.  This is used for those functions that do not
        want to deal with the ClipRect structures but do want
        to be able to work with a SuperBitMap Layer.@{b}

   INPUTS@{ub}
        layer - pointer to a Layer that has a SuperBitMap
                The Layer should already be locked by the caller.@{b}

   RESULT@{ub}
        After calling this function, the programmer can manipulate
        the bits in the superbitmap associated with the layer.
        Afterwards, the programmer should call @{"CopySBitMap" LINK "CopySBitMap"} to
        copy the bits back into the onscreen layer.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"CopySBitMap()" LINK "CopySBitMap"} @{"graphics/clip.h" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File"}

@ENDNODE
@NODE "Text" "graphics.library/Text()"
@{b}

   NAME@{ub}
        Text -- Write text characters (no formatting).@{b}

   SYNOPSIS@{ub}
        Text(rp, string, length)
             A1  A0      D0-0:16

        void Text(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        This graphics function writes printable text characters to the
        specified RastPort at the current position.  No control meaning
        is applied to any of the characters, thus only text on the
        current line is output.

        The current position in the RastPort is updated to the next
        character position.
        If the characters displayed run past the RastPort boundary,
        the current position is truncated to the boundary, and
        thus does not equal the old position plus the text length.@{b}

   INPUTS@{ub}
        rp     - a pointer to the RastPort which describes where the
                 text is to be output
        string - the address of string to output
        length - the number of characters in the string.
                 If zero, there are no characters to be output.@{b}

   NOTES@{ub}
        o   This function may use the blitter.
        o   Changing the text direction with RastPort->TxSpacing is
            not supported.@{b}

   BUGS@{ub}
        For V34 and earlier:
        o   The maximum string length (in pixels) is limited to
            (1024 - 16 = 1008) pixels wide.
        o   A text string whose last character(s) have a
            tf_CharLoc size component that extends to the right of
            the rightmost of the initial and final CP positions
            will be (inappropriately) clipped.@{b}

   SEE ALSO@{ub}
        @{"Move()" LINK "Move"}  @{"TextLength()" LINK "TextLength"}  @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "TextExtent" "graphics.library/TextExtent()"
@{b}

   NAME@{ub}
        @{"TextExtent" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 485} -- Determine raster extent of text data. (V36)@{b}

   SYNOPSIS@{ub}
        @{"TextExtent" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 485}(rp, string, count, textExtent)
                   A1  A0      D0:16  A2

        void textExtent(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54},
             @{"struct TextExtent" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 164} *);@{b}

   FUNCTION@{ub}
        This function determines a more complete metric of the space
        that a text string would render into than the @{"TextLength()" LINK "TextLength"}
        function.@{b}

   INPUTS@{ub}
        rp     - a pointer to the RastPort which describes where the
                 text attributes reside.
        string - the address of the string to determine the length of.
        count  - the number of characters in the string.
                If zero, there are no characters in the string.
        textExtent - a structure to hold the result.@{b}

   RESULTS@{ub}
        textExtent is filled in as follows:
            te_Width  - same as @{"TextLength()" LINK "TextLength"} result: the rp_cp_x
                        advance that rendering this text would cause.
            te_Height - same as tf_YSize.  The height of the
                        font.
            te_Extent.MinX - the offset to the left side of the
                        rectangle this would render into.  Often zero.
            te_Extent.MinY - same as -tf_Baseline.  The offset
                        from the baseline to the top of the rectangle
                        this would render into.
            te_Extent.MaxX - the offset of the left side of the
                        rectangle this would render into.  Often the
                        same as te_Width-1.
            te_Extent.MaxY - same as tf_YSize-tf_Baseline-1.
                        The offset from the baseline to the bottom of
                        the rectangle this would render into.@{b}

   SEE ALSO@{ub}
        @{"TextLength()" LINK "TextLength"}  @{"Text()" LINK "Text"}  @{"TextFit()" LINK "TextFit"}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "TextFit" "graphics.library/TextFit()"
@{b}

   NAME@{ub}
        @{"TextFit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 489} - count characters that will fit in a given extent (V36)@{b}

   SYNOPSIS@{ub}
        chars = @{"TextFit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 489}(rastport, string, strLen, @{"textExtent" LINK "TextExtent"},
        D0              A1        A0      D0      A2
                constrainingExtent, strDirection,
                A3                  D1
                constrainingBitWidth, constrainingBitHeight)
                D2                    D3

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"TextFit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 489}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55},
            @{"struct TextExtent" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 164} *, @{"struct TextExtent" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 164} *, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55});@{b}

   FUNCTION@{ub}
        This function determines how many of the characters of the
        provided string will fit into the space described by the
        constraining parameters.  It also returns the extent of
        that number of characters.@{b}

   INPUTS@{ub}
        rp     - a pointer to the RastPort which describes where the
                 text attributes reside.
        string - the address of string to determine the constraint of
        strLen - The number of characters in the string.
                 If zero, there are no characters in the string.
        @{"textExtent" LINK "TextExtent"} - a structure to hold the extent result.
        constrainingExtent - the extent that the text must fit in.
            This can be @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, indicating only the constrainingBit
            dimensions will describe the constraint.
        strDirection - the offset to add to the string pointer to
            get to the next character in the string.  Usually 1.
            Set to -1 and the string to the end of the string to
            perform a @{"TextFit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 489}() anchored at the end.  No other value
            is valid.
        constrainingBitWidth - an alternative way to specify the
            rendering box constraint width that is independent of
            the rendering origin.  Range 0..32767.
        constrainingBitHeight - an alternative way to specify the
            rendering box constraint height that is independent of
            the rendering origin.  Range 0..32767.@{b}

   RESULTS@{ub}
        chars - the number of characters from the origin of the
                given string that will fit in both the constraining
                extent (which specifies a CP bound and a rendering
                box relative to the origin) and in the rendering width
                and height specified.@{b}

   NOTES@{ub}
        The result is zero chars and an empty @{"textExtent" LINK "TextExtent"} when the fit
        cannot be performed.  This occurs not only when no text will
        fit in the provided constraints, but also when:
        -   the RastPort rp's rp_TxSpacing sign and magnitude is so
            great it reverses the path of the text.
        -   the constrainingExtent does not include x = 0.@{b}


   BUGS@{ub}
        Under V37, @{"TextFit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 489}() would return one too few characters if the
        font was proportional. This can be worked around by passing
        (constrainingBitWidth + 1) for proportional fonts. This is fixed
        for V39.@{b}

   SEE ALSO@{ub}
        @{"TextExtent()" LINK "TextExtent"}  @{"TextLength()" LINK "TextLength"}  @{"Text()" LINK "Text"}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "TextLength" "graphics.library/TextLength()"
@{b}

   NAME@{ub}
        @{"TextLength" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 69} -- Determine raster length of text data.@{b}

   SYNOPSIS@{ub}
        length = @{"TextLength" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 69}(rp, string, count)
        D0                  A1  A0      D0:16

        @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} @{"TextLength" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 69}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}, @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54});@{b}

   FUNCTION@{ub}
        This graphics function determines the length that text data
        would occupy if output to the specified RastPort with the
        current attributes.  The length is specified as the number of
        raster dots: to determine what the current position would be
        after a @{"Text()" LINK "Text"} using this string, add the length to cp_x
        (cp_y is unchanged by @{"Text()" LINK "Text"}).  Use the newer @{"TextExtent()" LINK "TextExtent"} to
        get more information.@{b}

   INPUTS@{ub}
        rp     - a pointer to the RastPort which describes where the
                 text attributes reside.
        string - the address of string to determine the length of
        count  - the string length.  If zero, there are no characters
                 in the string.@{b}

   RESULTS@{ub}
        length - the number of pixels in x this text would occupy, not
                 including any negative kerning that may take place at
                 the beginning of the text string, nor taking into
                 account the effects of any clipping that may take
                 place.@{b}

   NOTES@{ub}
        Prior to V36, the result length occupied only the low word of
        d0 and was not sign extended into the high word.@{b}

   BUGS@{ub}
        A length that would overflow single word arithmetic is not
        calculated correctly.@{b}

   SEE ALSO@{ub}
        @{"TextExtent()" LINK "TextExtent"}  @{"Text()" LINK "Text"}  @{"TextFit()" LINK "TextFit"}
        @{"graphics/text.h" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "UnlockLayerRom" "graphics.library/UnlockLayerRom()"
@{b}

   NAME@{ub}
        @{"UnlockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 325} -- Unlock Layer structure by ROM(gfx lib) code.@{b}

   SYNOPSIS@{ub}
        @{"UnlockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 325}( layer )
                         a5

        void @{"UnlockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 325}( @{"struct Layer" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Release the lock on this layer. If the same task has called
        @{"LockLayerRom" LINK "LockLayerRom"} more than once than the same number of calls to
        @{"UnlockLayerRom" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 325} must happen before the layer is actually freed
        so that other tasks may use it.
        This call does destroy scratch registers.
        This call is identical to @{"UnlockLayer" LINK "gg:doc/NDK/Guide/layers/UnlockLayer"} (@{"layers.library" LINK "gg:doc/NDK/Guide/layers/MAIN"}).@{b}

   INPUTS@{ub}
        layer - pointer to Layer structure@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"LockLayerRom()" LINK "LockLayerRom"} @{"layers.library/UnlockLayer()" LINK "gg:doc/NDK/Guide/layers/UnlockLayer"} @{"graphics/clip.h" LINK "gg:doc/NDK/Guide/Include/graphics/clip.h/File"}

@ENDNODE
@NODE "VBeamPos" "graphics.library/VBeamPos()"
@{b}

   NAME@{ub}
        @{"VBeamPos" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 289} -- Get vertical beam position at this instant.@{b}

   SYNOPSIS@{ub}
        pos = @{"VBeamPos" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 289}()
         d0

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"VBeamPos" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 289}( void );@{b}

   FUNCTION@{ub}
        Get the vertical beam position from the hardware.@{b}

   INPUTS@{ub}
        none@{b}

   RESULT@{ub}
        interrogates hardware for beam position and returns value.
        valid results in are the range of 0-511.
        Because of multitasking, the actual value returned may have
        no use. If you are the highest priority task then the value
        returned should be close, within 1 line.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}

@ENDNODE
@NODE "VideoControl" "graphics.library/VideoControl()"
@{b}

   NAME@{ub}
        @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497} -- Modify the operation of a ViewPort's ColorMap (V36)
        @{"VideoControlTags" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 502} -- varargs stub for @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497} (V36)@{b}
   SYNOPSIS@{ub}
        error = @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}( cm , tags )
        d0                    a0   a1

        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}( @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} *, @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} * );

        error= @{"VideoControlTags" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 502}(cm, tags,...);@{b}


   FUNCTION@{ub}
        Process the commands in the @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497} command TagItem buffer
        using cm as the target, with respect to its "attached" ViewPort.

        viewport commands:

        VTAG_ATTACH_CM     [_SET        | _GET] -- set/get attached viewport
        VTAG_VIEWPORTEXTRA [_SET        | _GET] -- set/get attached vp_extra
        VTAG_NORMAL_DISP   [_SET        | _GET] -- set/get @{"DisplayInfoHandle" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 35}
                                                   (natural mode)
        VTAG_COERCE_DISP   [_SET        | _GET] -- set/get @{"DisplayInfoHandle" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 35}
                                                   (coerced mode)
        VTAG_PF1_BASE      [_SET        | _GET] -- set/get color base for
                                                   first playfield. (V39)
        VTAG_PF2_BASE      [_SET        | _GET] -- set/get color base for
                                                   second playfield. (V39)
        VTAG_SPODD_BASE    [_SET        | _GET] -- set/get color base for odd
                                                   sprites. (V39)
        VTAG_SPEVEN_BASE   [_SET        | _GET] -- set/get color base for even
                                                   sprites. (V39)
        VTAG_BORDERSPRITE  [_SET        | _GET] -- on/off/inquire sprites in
                                                   borders. (V39)
        VTAG_SPRITERESN    [_SET        | _GET] -- set/get sprite resolution
                      (legal values are @{"SPRITERESN_ECS" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 192}/_140NS/_70NS/_35NS.
                       see @{"graphics/view.h" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File"}) (V39)
        VTAG_PF1_TO_SPRITEPRI [_SET     | _GET] -- set/get playfield1 priority
                                                  with respect to sprites (V3 9)
        VTAG_PF2_TO_SPRITEPRI [_SET     | _GET] -- set/get playfield2 priority
                                                  with respect to sprites (V3 9)
        (These two require that the ColorMap is attached to a ViewPort to be
         effective).

        genlock commands:

        VTAG_BORDERBLANK   [_SET | _CLR | _GET] -- on/off/inquire blanking
        VTAG_BORDERNOTRANS [_SET | _CLR | _GET] -- on/off/inquire notransparency
        VTAG_CHROMAKEY     [_SET | _CLR | _GET] -- on/off/inquire chroma mode
        VTAG_BITPLANEKEY   [_SET | _CLR | _GET] -- on/off/inquire bitplane mode
        VTAG_CHROMA_PEN    [_SET | _CLR | _GET] -- set/clr/get chromakey pen #
        VTAG_CHROMA_PLANE  [_SET |      | _GET] -- set/get bitplanekey plane #

        control commands:

        @{"VTAG_IMMEDIATE" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 79}  - normally, @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497} changes do not occur until the
                next @{"MakeVPort" LINK "MakeVPort"}. Using this tag, some changes can be made to
                happen immediately. The tag data is a pointer to a longword
                flag variable which will be cleared if all changes happened
                immediately. See the example. (V39)

        VTAG_FULLPALETTE   [_SET | _CLR | _GET] -- enable/disable loading of all
                colors in the copper list.
                Normally, graphics will only load the color which are necessary
                for the viewport, based upon the screen depth and mode. In order
                to use the color palette banking features, you may need to use
                this tag to tell graphics to load ALL colors, regardless of
                screen depth. (V39)

        @{"VC_IntermediateCLUpdate" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 94}
        @{"VC_IntermediateCLUpdate_Query" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 98}
                 When set, graphics will update the intermediate copper
                 lists on colour changes. When @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, graphics won't update
                 the intermediate copperlists, so @{"ScrollVPort()" LINK "ScrollVPort"},
                 @{"ChangeVPBitMap()" LINK "ChangeVPBitMap"} and colour loading functions will be faster.
                 This value is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} by default. (V40)

        @{"VC_NoColorPaletteLoad" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 100}
        @{"VC_NoColorPaletteLoad_Query" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 107}
                 When set, only colour 0 will be loaded for this ViewPort,
                 hence the inter-ViewPort gap will be smaller. The colours for
                 this ViewPort are inherited from the next higher ViewPort. The
                 results are undefined if this is the first or only ViewPort in
                 the display, and undefined when used in conjunction with
                 VTAG_FULLPALETTE (!?!).
                 This value is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} by default. (V40)

        @{"VC_DUALPF_Disable" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 109}
        @{"VC_DUALPF_Disable_Query" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 120}
                When set, disables the setting of the dual-playfield
                bit in bplcon0. When used with a dual-playfield mode
                screen, this allows using separate scroll and bitmaps
                for the odd and even bitplanes, without going through
                the normal dual-playfield priority and palette selection.
                With appropriate palette setup, this can be used for
                transparency effects, etc.

        copper commands

        VTAG_USERCLIP      [_SET | _CLR | _GET] -- on/off/inquire clipping of
                                                   UserCopperList at bottom
                                                   edge of ColorMap->cm_vp
                                                   (defaults to off)

        buffer commands:

        @{"VTAG_NEXTBUF_CM" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 34}                         -- link to more VTAG commands
        @{"VTAG_END_CM" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 21}                             -- terminate command buffer

        batch mode commands:

        (if you want your videocontrol taglist to be processed in "batch"
         mode, that is, at the next @{"MakeVPort()" LINK "MakeVPort"} for the ColorMap->cm_vp;
         you may install a static list of videocontrol TagItems into the
         ColorMap with the BATCH_ITEMS_SET command; and then enable/disable
         batch mode processing of those items via the BATCH_CM control
         command)

        VTAG_BATCH_CM      [_SET | _CLR | _GET] -- on/off/inquire batch mode
        VTAG_BATCH_ITEMS   [_SET | _ADD | _GET] -- set/add/get batched TagLists

        private commands (used internally by intuition -- do not call):

        VTAG_VPMODEID      [_SET | _CLR | _GET] -- force @{"GetVPModeID()" LINK "GetVPModeID"} return@{b}


   INPUTS@{ub}
        cm   = pointer to @{"struct ColorMap" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 143} obtained via @{"GetColorMap()" LINK "GetColorMap"}.
        tags = pointer to a table of videocontrol tagitems.@{b}

   RESULT@{ub}
        error = @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if no error occurred in the control operation.
        (non-@{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} if bad colormap pointer, no tagitems or bad tag)

        The operating characteristics of the ColorMap and its attached
        ViewPort are modified. The result will be incorporated into the
        ViewPort when its copper lists are reassembled via @{"MakeVPort()" LINK "MakeVPort"}.

        Note that you must @{"NOT" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 1500} change colors in the viewport (via @{"SetRGB4()" LINK "SetRGB4"},
        @{"LoadRGB4()" LINK "LoadRGB4"}, @{"SetRGB4()" LINK "SetRGB4"}, etc.) after changing any of the color palette
        offsets (VTAG_PF1_BASE, etc), without first remaking the ViewPort.@{b}

   NOTES@{ub}
        Sprite resolutions is controlled by two sets of tags, SPRITERESN
        and DEFSPRITERESN. If you don't set the sprite resolution, it will
        follow the intuition-controlled "default" sprite resolution. Setting
        the sprite resolution to one of the SPRITERESN_ values will allow the
        application to override intuition's control of it.

        This function will modify the contents of the TagList you pass to it by
        changing _GET tags to the corresponding _SET or _CLR tag. The
        exceptions to this rule are documented as such above (such as
        @{"VTAG_IMMEDIATE" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 79}).

        The new tags added for V40 have the prefix VC_ instead of VTAG_. These
        tags work in the same manner as all other tags in the system, and will
        not be modified by @{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}().@{b}

   EXAMPLE@{ub}
        must_remake=-1;
        error=@{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}(myvp->ColorMap,@{"VTAG_BORDERBLANK_SET" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 27},-1,
                           (GFXBase->lib_Version>=39)?@{"VTAG_IMMEDIATE" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 79}:@{"TAG_IGNORE" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 41},
                                                &must_remake);
        if (must_remake) { @{"MakeVPort" LINK "MakeVPort"}(myview,myvp); @{"MrgCop" LINK "MrgCop"}(myview); }@{b}

   EXAMPLE@{ub}
        @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} VCTags[] =
        {
            {@{"VTAG_BORDERBLANK_GET" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 45}, @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}},
            {@{"VTAG_SPRITERESN_SET" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 73}, @{"SPRITERESN_35NS" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 196}},
            {@{"TAG_DONE" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 39}, @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}},
        };
        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} bblank = @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98};

        if (@{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}(cm, VCTags) == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101})
        {
            bblank = (VCTags[0].ti_Tag == @{"VTAG_BORDERBLANK_SET" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 27});
        }@{b}

   EXAMPLE@{ub}
        @{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} VCTags[] =
        {
            {@{"VC_NoColorPaletteLoad_Query" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File" 107}, @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}},
            {@{"TAG_DONE" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 39}},
        };
        @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} query;

        VCTags[0].ti_Data = (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52})&query;
        if (@{"VideoControl" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 497}(cm, VCTags) == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101})
        {
            @{"printf" LINK "gg:doc/NDK/Guide/amiga_lib/printf"}("Palette loading is %s\\n", (query ? "off" : "on"));
        }@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"graphics/videocontrol.h" LINK "gg:doc/NDK/Guide/Include/graphics/videocontrol.h/File"}, @{"GetColorMap()" LINK "GetColorMap"}, @{"FreeColorMap()" LINK "FreeColorMap"}

@ENDNODE
@NODE "WaitBlit" "graphics.library/WaitBlit()"
@{b}

   NAME@{ub}
       @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} -- Wait for the blitter to be finished before proceeding
                   with anything else.@{b}

   SYNOPSIS@{ub}
       @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185}()

        void @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185}( void );@{b}

   FUNCTION@{ub}
        @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} returns when the blitter is idle. This function should
        normally only be used when dealing with the blitter in a
        synchronous manner, such as when using @{"OwnBlitter" LINK "OwnBlitter"} and @{"DisownBlitter" LINK "DisownBlitter"}.
        @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} does not wait for all blits queued up using @{"QBlit" LINK "QBlit"} or
        @{"QBSBlit" LINK "QBSBlit"}. You should call @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} if you are just about to modify or
        free some memory that the blitter may be using.@{b}

   INPUTS@{ub}
       none@{b}

   RESULT@{ub}
       Your program waits until the blitter is finished.
        This routine does not use any the CPU registers.
        do/d1/a0/a1 are preserved by this routine.
        It may change the condition codes though.@{b}

   BUGS@{ub}
        When examining bits with the CPU right after a blit, or when freeing
        temporary memory used by the blitter, a @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185}() may be required.

        Note that many graphics calls fire up the blitter, and let it run.
        The CPU does not need to wait for the blitter to finish before
        returning.

        Because of a bug in Agnus (prior to all revisions of fat Agnus)
        this code may return too soon when the blitter has, in fact, not
        started the blit yet, even though BltSize has been written.

        This most often occurs in a heavily loaded system with extended memory,
        @{"HIRES" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 134}, and 4 bitplanes.

        @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} currently tries to avoid this Agnus problem by testing
        the BUSY bit multiple times to make sure the blitter has started.
        If the blitter is BUSY at first check, this function busy waits.

        This initial hardware bug was fixed as of the first "Fat Agnus" chip,
        as used in all A500 and A2000 computers.

        Because of a different bug in Agnus (currently all revisions thru ECS)
        this code may return too soon when the blitter has, in fact, not
        stopped the blit yet, even though blitter busy has been cleared.

        This most often occurs in a heavily loaded system with extended memory,
        in PRODUCTIVITY mode, and 2 bitplanes.

        @{"WaitBlit" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 185} currently tries to avoid this Agnus problem by testing
        the BUSY bit multiple times to make sure the blitter has really
        written its final word of destination data.@{b}

   SEE ALSO@{ub}
        @{"OwnBlitter()" LINK "OwnBlitter"} @{"DisownBlitter()" LINK "DisownBlitter"} @{"hardware/blit.h" LINK "gg:doc/NDK/Guide/Include/hardware/blit.h/File"}

@ENDNODE
@NODE "WaitBOVP" "graphics.library/WaitBOVP()"
@{b}

   NAME@{ub}
        @{"WaitBOVP" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 301} -- Wait till vertical beam reached bottom of
                    this viewport.@{b}

   SYNOPSIS@{ub}
        @{"WaitBOVP" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 301}( vp )
                  a0

        void @{"WaitBOVP" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 301}( @{"struct ViewPort" LINK "gg:doc/NDK/Guide/Include/graphics/view.h/File" 47} * );@{b}

   FUNCTION@{ub}
        Returns when the vertical beam has reached the bottom of this viewport@{b}

   INPUTS@{ub}
        vp - pointer to ViewPort structure@{b}

   RESULT@{ub}
        This function will return sometime after the beam gets beyond
        the bottom of the viewport.  Depending on the multitasking load
        of the system, the actual beam position may be different than
        what would be expected in a lightly loaded system.@{b}

   BUGS@{ub}
        Horrors! This function currently busy waits waiting for the
        beam to get to the right place.  It should use the copper
        interrupt to trigger and send signals like @{"WaitTOF" LINK "WaitTOF"} does.@{b}

   SEE ALSO@{ub}
        @{"WaitTOF()" LINK "WaitTOF"} @{"VBeamPos()" LINK "VBeamPos"}

@ENDNODE
@NODE "WaitTOF" "graphics.library/WaitTOF()"
@{b}

   NAME@{ub}
       @{"WaitTOF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 213} -- Wait for the top of the next video frame.@{b}

   SYNOPSIS@{ub}
       @{"WaitTOF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 213}()

        void @{"WaitTOF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 213}( void );@{b}

   FUNCTION@{ub}
       Wait  for vertical blank to occur and all vertical blank
       interrupt routines to complete before returning to caller.@{b}

   INPUTS@{ub}
       none@{b}

   RESULT@{ub}
        Places this task on the TOF wait queue. When the vertical blank
        interrupt comes around, the interrupt service routine will fire off
        signals to all the tasks doing @{"WaitTOF" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 213}. The highest priority task
        ready will get to run then.@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"exec.library/Wait()" LINK "gg:doc/NDK/Guide/exec/Wait"} @{"exec.library/Signal()" LINK "gg:doc/NDK/Guide/exec/Signal"}

@ENDNODE
@NODE "WriteChunkyPixels" "graphics.library/WriteChunkyPixels()"
@{b}

   NAME@{ub}
        @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742} -- write the pen number value of a rectangular array
        of pixels starting at a specified x,y location and continuing
        through to another x,y location within a certain RastPort. (V40)@{b}

   SYNOPSIS@{ub}
        @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742}(rp,xstart,ystart,xstop,ystop,array,bytesperrow)
                          A0 D0     D1     D2    D3    A2     D4

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742}(@{"struct  RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51}, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51},
             @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51}, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51});@{b}

   FUNCTION@{ub}
        For each pixel in a rectangular region, decode the pen number selector
        from a linear array of pen numbers into the bit-planes used to describe
        a particular rastport.@{b}

   INPUTS@{ub}
        rp     -  pointer to a RastPort structure
        (xstart,ystart) -  starting point in the RastPort
        (xstop,ystop)   -  stopping point in the RastPort
        array  - pointer to an array of UBYTEs from which to fetch the
                 pixel data.
        bytesperrow - The number of bytes per row in the source array.
                This should be at least as large as the number of pixels
                being written per line.@{b}

   RESULT@{ub}@{b}

   NOTE@{ub}
        xstop must be >= xstart
        ystop must be >= ystart
        The source array can be in fast RAM.

        ===chunky-to-planar conversion HW:

        GfxBase->@{"ChunkyToPlanarPtr" LINK "gg:doc/NDK/Guide/Include/graphics/gfxbase.h/File" 130} is either @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, or a pointer to a HW
        register used to aid in the process of converting 8-bit chunky
        pixel data into the bit-plane format used by the Amiga custom
        display chips. If @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, then such hardware is not present.

        If an expansion device provides hardware which operates compatibly,
        than it can install the HW address into this pointer at boot time,
        and the system will use it.

        This pointer may be used for direct access to the chunky-to-planar
        conversion HW, if more is desired than the straight chunky-pixel
        copy that is performed by @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742}().

        If using the hardware directly, it should only be accessed when
        the task using it has control of the blitter (via @{"OwnBlitter()" LINK "OwnBlitter"}),
        since this is the locking used to arbitrate usage of this device.

        The hardware may be viewed as a device which accepts 32 8-bit
        chunky pixels and outputs 8 longwords of bitplane data.

        For proper operation, exactly 8 longwords (containing 32 pixels)
        of chunky data should be written to *(GfxBase->@{"ChunkyToPlanarPtr" LINK "gg:doc/NDK/Guide/Include/graphics/gfxbase.h/File" 130}).
        After the data is written, bitplane data (starting with plane 0)
        can be read back a longword at a time. There is no need to read
        back all 8 longwords if the high-order bitplanes are not needed.

        Since @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742} is not (currently) particularly fast on
        systems without the chunky-to-planar hardware, time critical
        applications (games, etc) may want to use their own custom conversion
        routine if GfxBase->@{"ChunkyToPlanarPtr" LINK "gg:doc/NDK/Guide/Include/graphics/gfxbase.h/File" 130} is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, and call
        @{"WriteChunkyPixels" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 742}() otherwise.

        This pointer is only present in GfxBase in versions of graphics.library
        >= 40, so this should be checked before the pointer is read.@{b}

   BUGS@{ub}
        Not very fast on systems without chunky-to-planar conversion
        hardware.@{b}

   SEE ALSO@{ub}
        @{"WritePixel()" LINK "WritePixel"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "WritePixel" "graphics.library/WritePixel()"
@{b}

   NAME@{ub}
       @{"WritePixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 249} -- Change the pen num of one specific pixel in a
                     specified RastPort.@{b}

   SYNOPSIS@{ub}
       error = @{"WritePixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 249}(  rp, x,  y)
         d0                 a1 D0  D1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"WritePixel" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 249}( @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81}, @{"SHORT" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 81} );@{b}

   FUNCTION@{ub}
       Changes the pen number of the selected pixel in the specified
       RastPort to that currently specified by PenA, the primary
       drawing pen. Obeys minterms in RastPort.@{b}

   INPUTS@{ub}
       rp - a pointer to the RastPort structure
       (x,y) - point within the RastPort at which the selected
           pixel is located.@{b}

   RESULT@{ub}
       error = 0 if pixel succesfully changed
              = -1 if (x,y) is outside the RastPort@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
       @{"ReadPixel()" LINK "ReadPixel"} @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "WritePixelArray8" "graphics.library/WritePixelArray8()"
@{b}

   NAME@{ub}
        @{"WritePixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 542} -- write the pen number value of a rectangular array
        of pixels starting at a specified x,y location and continuing
        through to another x,y location within a certain RastPort. (V36)@{b}

   SYNOPSIS@{ub}
        count = @{"WritePixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 542}(rp,xstart,ystart,xstop,ystop,array,temprp)
        D0                       A0 D0:16  D1:16  D2:16 D3:16  A2   A1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"WritePixelArray8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 542}(@{"struct  RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55},
             @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"struct  RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        For each pixel in a rectangular region, decode the pen number selector
        from a linear array of pen numbers into the bit-planes used to describe
        a particular rastport.@{b}

   INPUTS@{ub}
        rp     -  pointer to a RastPort structure
        (xstart,ystart) -  starting point in the RastPort
        (xstop,ystop)   -  stopping point in the RastPort
        array  - pointer to an array of UBYTEs from which to fetch the
                 pixel data. Allocate at least
                 ((((width+15)>>4)<<4)*(ystop-ystart+1)) bytes.
        temprp - temporary rastport (copy of rp with Layer set == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101},
                 temporary memory allocated for
                 temprp->BitMap with Rows set == 1,
                 temprp->BitMap with BytesPerRow == (((width+15)>>4)<<1),
                 and temporary memory allocated for
                 temprp->BitMap->Planes[])@{b}

   RESULT@{ub}
        count will be set to the number of pixels plotted.@{b}

   NOTE@{ub}
        xstop must be >= xstart
        ystop must be >= ystart@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"WritePixel()" LINK "WritePixel"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "WritePixelLine8" "graphics.library/WritePixelLine8()"
@{b}

   NAME@{ub}
        @{"WritePixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 534} -- write the pen number value of a horizontal line
        of pixels starting at a specified x,y location and continuing
        right for count pixels. (V36)@{b}

   SYNOPSIS@{ub}
        count = @{"WritePixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 534}(rp,xstart,ystart,width,array,temprp)
        D0                      A0 D0:16  D1:16  D2    A2    A1

        @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51} @{"WritePixelLine8" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 534}(@{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55},
             @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}, @{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *, @{"struct RastPort" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 52} *);@{b}

   FUNCTION@{ub}
        For each pixel in a horizontal region, decode the pen number selector
        from a linear array of pen numbers into the bit-planes used to describe
        a particular rastport.@{b}

   INPUTS@{ub}
        rp    -  pointer to a RastPort structure
        (x,y) - a point in the RastPort
        width - count of horizontal pixels to write
        array - pointer to an array of UBYTEs from which to fetch the pixel data
                allocate at least (((width+15)>>4)<<4) bytes.
        temprp - temporary rastport (copy of rp with Layer set == @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101},
                 temporary memory allocated for
                 temprp->BitMap with Rows set == 1,
                 temprp->BitMap BytesPerRow == (((width+15)>>4)<<1),
                 and temporary memory allocated for
                 temprp->BitMap->Planes[])@{b}

   RESULT@{ub}
        Count will be set to the number of pixels plotted@{b}

   NOTE@{ub}
        width must be non negative@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"WritePixel()" LINK "WritePixel"}  @{"graphics/rastport.h" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File"}

@ENDNODE
@NODE "XorRectRegion" "graphics.library/XorRectRegion()"
@{b}

   NAME@{ub}
       @{"XorRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 405} -- Perform 2d XOR operation of rectangle
                       with region, leaving result in region@{b}

   SYNOPSIS@{ub}
       status = @{"XorRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 405}(region,rectangle)
         d0                     a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"XorRectRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 405}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Rectangle" LINK "gg:doc/NDK/Guide/Include/graphics/gfx.h/File" 27} * );@{b}

   FUNCTION@{ub}
        Add portions of rectangle to region if they are not in
        the region.
        Remove portions of rectangle from region if they are
        in the region.@{b}

   INPUTS@{ub}
       region - pointer to Region structure
       rectangle - pointer to Rectangle structure@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}@{b}

   SEE ALSO@{ub}
        @{"OrRegionRegion()" LINK "OrRegionRegion"} @{"AndRegionRegion()" LINK "AndRegionRegion"} @{"graphics/regions.h" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File"}

@ENDNODE
@NODE "XorRegionRegion" "graphics.library/XorRegionRegion()"
@{b}

   NAME@{ub}
       @{"XorRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 445} -- Perform 2d XOR operation of one region
                       with second region, leaving result in second region@{b}

   SYNOPSIS@{ub}
       status = @{"XorRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 445}(region1,region2)
         d0                        a0      a1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"XorRegionRegion" LINK "gg:doc/NDK/Guide/Include/inline/graphics.h/File" 445}( @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} *, @{"struct Region" LINK "gg:doc/NDK/Guide/Include/graphics/regions.h/File" 25} * );@{b}

   FUNCTION@{ub}
        Join the regions together. If any part of region1 overlaps
        region2 then remove that from the new region.@{b}

   INPUTS@{ub}
       region1      = pointer to Region structure
       region2      = pointer to Region structure@{b}

   RESULTS@{ub}
        status - return @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if successful operation
                 return @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if ran out of memory@{b}

   BUGS@{ub}

@ENDNODE
