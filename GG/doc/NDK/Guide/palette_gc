@DATABASE "gg:doc/NDK/Guide/palette_gc"
@MASTER   "gg:doc/NDK/AutoDocs/palette_gc.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:24:34
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "palette_gc.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                                 @{b}palette_gc@{ub}

@{"datasheet" LINK "datasheet"}
@{"styleguide" LINK "styleguide"}

@{"PALETTE_GetClass()" LINK "PALETTE_GetClass"}

@ENDNODE
@NODE "datasheet" "palette_gc/datasheet (information)"


     NAME
        @{"palette_gc" LINK "gg:doc/NDK/Guide/palette_gc/MAIN"} -- A colour selection gadget.

     SUPERCLASS
        gadgetclass

     REQUIRES
        bevel.image

     DESCRIPTION
        This gadget is equivalent to the GadTools @{"PALETTE_KIND" LINK "gg:doc/NDK/Guide/Include/libraries/gadtools.h/File" 45} gadget, but
        implemented as a BOOPSI gadget class.  It displays a palette of
        colours, allowing the user to select one.

        Advantages that this class provides:

         > Right mouse button cancels gadget action.

         > Supports window relativity.

         > Supports gadget interconnections.

         > Supports OS 3.0 BOOPSI Gadget HelpTest.

     METHODS
        @{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63} -- Create the palette gadget, calls @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}.  Passed to
            superclass.

        @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65} -- Set object attributes.  Passed to superclass first.

        @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66} -- Get object attributes.  Passed to superclass first.

        @{"OM_NOTIFY" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 69} -- Notify connections of attribute changes.

        @{"OM_DISPOSE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 64} -- Frees resources and calls superclass.

        @{"OM_UPDATE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 70} -- Calls @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}.

        @{"GM_DOMAIN" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 323} -- Return the maximum and minimum size of the gadget.

        @{"GM_RENDER" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 302} -- Renders the gadget imagry.  Overrides the superclass.

        GM_KEYACTIVE -- Handles keyboard activation.

        GM_KEYINACTIVE -- Handles keyboard de-activation.

        @{"GM_GOACTIVE" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 305} -- Activate the gadget on mouse click only.  Overrides
            the superclass.

        @{"GM_HANDLEINPUT" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 308} -- Handles input events once active.  Overrides the
            superclass.

        @{"GM_GOINACTIVE" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 311} -- Deactivates the gadget.  Passed to the superclass.

        All other methods are passed to the superclass.

     ATTRIBUTES
        @{"GA_Disabled" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 78} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})
            Set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to disable gadget, @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} otherwise

            Defaults to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"PALETTE_Colour" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 29} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62})
                Initially selected color of the palette. This number is a pen
                number, and not the ordinal color number within the palette
                gadget itself.  The exception is when @{"PALETTE_ColourTable" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 36} is
                used, in which case you will get an index into your table.

            Defaults to 0.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"PALETTE_ColourOffset" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 32} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62})
            First color to use in screen palette.

                Defaults to 0.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"PALETTE_ColourTable" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 36} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *)
            Pointer to a table of pen numbers indicating  which colors
            should be used and edited by the palette gadget. This array
            must contain as many entries as there are colors displayed in
            the palette gadget PLUS the colour offset.  The array provided
            with this tag must remain valid for the life of the gadget or
            until a new table is provided.

            Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, which causes a 1-to-1 mapping of pen numbers.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"PALETTE_NumColours" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 39} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55})
            Number of colors to display in the palette gadget.  Currently
            this MUST be a power of

                Defaults to 2.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

@ENDNODE
@NODE "styleguide" "palette_gc/styleguide (information)"


    PALETTE STYLE
        The usability of a palette gadget decreases as the number of
        colours it is displaying increases.  If there is a possibility
        that your palette gadget may be displaying a large number of
        colours (say, greater than 16), you should provide a means to
        simplify the colour selection.  Two possibilities would be to
        divide your colour palette into banks, and have users switch
        banks via a chooser gadget for example.  Another possibility
        would be to attach a scroller gadget to allow the user to
        scroll through the available colours, displaying just a
        subset of them on screen at a time.  You may also want to sort
        your colours in some logical manner and use @{"PALETTE_ColourTable" LINK "gg:doc/NDK/Guide/Include/gadgets/palette.h/File" 36}.

@ENDNODE
@NODE "PALETTE_GetClass" "palette_gc/PALETTE_GetClass()"
@{b}

    NAME@{ub}
        @{"PALETTE_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/palette.h/File" 23} -- Gets the pointer to the palette class.@{b}

    SYNOPSIS@{ub}
        palette_class = @{"PALETTE_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/palette.h/File" 23}();
        D0

        Class * @{"PALETTE_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/palette.h/File" 23}(@{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24});@{b}

    FUNCTION@{ub}
        Obtains the pointer to the Palette gadget class for use with
        @{"NewObject()" LINK "gg:doc/NDK/Guide/intuition/NewObject"}.  This function always returns a valid pointer so
        you do not need to check it.  The reason is that if the library
        opens fine, then the pointer returned is already setup.  (Of course
        this implies that if opening the library fails, you shouldn't be
        calling this.)

        Note that this function does not create the class, that is done
        when the class library is opened.@{b}

    INPUTS@{ub}
        Nothing.@{b}

    RESULT@{ub}
        palette_class - Pointer to the Palette gadget class.@{b}

    SEE ALSO@{ub}

@ENDNODE
