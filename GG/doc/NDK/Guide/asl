@DATABASE "gg:doc/NDK/Guide/asl"
@MASTER   "gg:doc/NDK/AutoDocs/asl.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:19:12
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "asl.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                                 @{b}asl.library@{ub}

@{"AbortAslRequest()" LINK "AbortAslRequest"}      @{"ActivateAslRequest()" LINK "ActivateAslRequest"}  @{"AllocAslRequest()" LINK "AllocAslRequest"}
@{"AllocAslRequestTags()" LINK "AllocAslRequest"}  @{"AllocFileRequest()" LINK "AllocFileRequest"}    @{"AslRequest()" LINK "AslRequest"}
@{"AslRequestTags()" LINK "AslRequest"}       @{"FreeAslRequest()" LINK "FreeAslRequest"}      @{"FreeFileRequest()" LINK "FreeFileRequest"}
@{"RequestFile()" LINK "RequestFile"}          

@ENDNODE
@NODE "AbortAslRequest" "asl.library/AbortAslRequest()"
@{b}

   NAME@{ub}
        @{"AbortAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 60} -- attempt to abort an asl requester. (V44)@{b}

   SYNOPSIS@{ub}
        @{"AbortAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 60}(requester);
                           A0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"AbortAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 60}(@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} requester);@{b}

   FUNCTION@{ub}
        The different types of asl requesters are modal (as of V44). Yet at
        times it may be necessary to abort a modal requester from a different
        task context. @{"AbortAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 60}() will attempt to tell the modal asl
        requester provided to shut down and perform as if the user had clicked
        on the requester's window close gadget. This may take some time to
        complete; be prepared to handle this.@{b}

   INPUTS@{ub}
        requester - requester obtained from @{"AllocAslRequest()" LINK "AllocAslRequest"}, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} in
                    which case this function does nothing.@{b}

   SEE ALSO@{ub}
        @{"AslRequest()" LINK "AslRequest"}

@ENDNODE
@NODE "ActivateAslRequest" "asl.library/ActivateAslRequest()"
@{b}

   NAME@{ub}
        @{"ActivateAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 64} -- attempt to activate an asl requester. (V44)@{b}

   SYNOPSIS@{ub}
        @{"ActivateAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 64}(requester);
                              A0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"ActivateAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 64}(@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} requester);@{b}

   FUNCTION@{ub}
        The different types of asl requesters are modal (as of V44). Yet at
        times it may be necessary to activate a modal requester from a different
        task context.@{b}

   INPUTS@{ub}
        requester - requester obtained from @{"AllocAslRequest()" LINK "AllocAslRequest"}, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} in
                    which case this function does nothing.@{b}

   SEE ALSO@{ub}
        @{"AslRequest()" LINK "AslRequest"}

@ENDNODE
@NODE "AllocAslRequest" "asl.library/AllocAslRequest()"
@{b}

   NAME@{ub}
        @{"AllocAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 38} -- allocate an ASL requester. (V36)
        @{"AllocAslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 43} -- varargs stub for @{"AllocAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 38}(). (V36)@{b}

   SYNOPSIS@{ub}
        requester = @{"AllocAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 38}(reqType,tagList);
        D0                          D0      A0

        @{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} @{"AllocAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 38}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},@{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        requester = @{"AllocAslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 43}(reqType,firstTag, ...);

        @{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} @{"AllocAslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 43}(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52},Tag,...);@{b}

   FUNCTION@{ub}
        Allocates an ASL requester data structure of the specified type.@{b}

   INPUTS@{ub}
        type - type of requester to allocate. Currently defined types
               are @{"ASL_FileRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 57}, @{"ASL_FontRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 58} and @{"ASL_ScreenModeRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 59}.
               @{"ASL_ScreenModeRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 59} is available starting with asl.library
               V38 only.
        tags - pointer to an optional tag list specifying how to initialize the
               data structure returned by this function. See the
               documentation for @{"AslRequest()" LINK "AslRequest"} for an explanation of
               how to use the currently defined tags.@{b}

   RESULT@{ub}
        requester - an initialized requester data structure, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} on
                    failure. The data structure returned corresponds to the
                    requested type;
                        @{"ASL_FileRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 57}       -> (@{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *)
                        @{"ASL_FontRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 58}       -> (@{"struct FontRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 193} *)
                        @{"ASL_ScreenModeRequest" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 59} -> (@{"struct ScreenModeRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 297} *)@{b}

   WARNING@{ub}
        All ASL requester data structures are READ-ONLY and can only be
        modified by using tags!

        You must not specify requester options by using both pre-V38
        tags like @{"ASLFR_Flags1" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 123} and V38 tags like @{"ASLFR_DoSaveMode" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 125} as
        this can lead to unexpected results.

        Take care when attaching an ASL requester to a simple refresh
        window or a window that already shares its IDCMP with other
        windows. In such cases you must ask for the ASL requester to
        allocate a private IDCMP (with the @{"ASLFR_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 95},
        @{"ASLFO_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 215} and @{"ASLSM_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 343} tags) or your window
        will lose @{"IDCMP_REFRESHWINDOW" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 834} events and other messages.@{b}

   BUGS@{ub}
        Under V38 and V39, the filtering functions for the file requester
        were only asked to filter drawer selections if the @{"ASLFR_FilterDrawers" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 135}
        tag was set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. The compatible behavior, reestablished in V40,
        is to always pass drawers for filtering.@{b}

   NOTES@{ub}
        As of V38-V40 the ASL requester uses @{"gadtools.library" LINK "gg:doc/NDK/Guide/gadtools/MAIN"} routines to
        create and maintain its user interface. Every IntuiMessage the library
        passes through the functions specified with @{"ASLSM_IntuiMsgFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 344} and
        ASLSM_HookFunc will have been filtered through the
        @{"gadtools.library/GT_GetIMsg()" LINK "gg:doc/NDK/Guide/gadtools/GT_GetIMsg"} routine.@{b}

   SEE ALSO@{ub}
        @{"AslRequest()" LINK "AslRequest"}, @{"FreeAslRequest()" LINK "FreeAslRequest"}

@ENDNODE
@NODE "AllocFileRequest" "asl.library/AllocFileRequest()"
@{b}

   NAME@{ub}
        @{"AllocFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 26} -- allocates an ASL FileRequester structure. (V36)@{b}

   SYNOPSIS@{ub}
        requester = @{"AllocFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 26}()
        D0

        @{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *@{"AllocFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 26}(@{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24});@{b}

   NOTES@{ub}
        This function is obsolete, please use @{"AllocAslRequest()" LINK "AllocAslRequest"} instead.@{b}

   SEE ALSO@{ub}
        @{"AllocAslRequest()" LINK "AllocAslRequest"}

@ENDNODE
@NODE "AslRequest" "asl.library/AslRequest()"
@{b}

   NAME@{ub}
        @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51} -- get input from user using an ASL requester. (V36)
        @{"AslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 56} -- varargs stub for @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(). (V36)@{b}

   SYNOPSIS@{ub}
        result = @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(requester,tags);
        D0                  A0        A1

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49},@{"struct TagItem" LINK "gg:doc/NDK/Guide/Include/utility/tagitem.h/File" 32} *);

        result = @{"AslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 56}(requester,firstTag,...);

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"AslRequestTags" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 56}(@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49},Tag,...);@{b}

   FUNCTION@{ub}
        Prompts the user for input, based on the specific type of
        requester and modifying tags. The actions and results
        are specific to the type of requester, but in general the action is
        to open a requesting window prompting the user for a specific input.
        If the user cancels or the system aborts the request, @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}
        is returned, otherwise the requester's data structure reflects the
        user input.

        Note that tag values stay in effect for each use of the
        requester until they are cleared or modified by passing the
        same tag with a new value.@{b}

   INPUTS@{ub}
        requester - requester structure allocated with @{"AllocAslRequest()" LINK "AllocAslRequest"}. If
                    this parameter is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, this function will always return
                    @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} with a @{"dos.library/IoErr()" LINK "gg:doc/NDK/Guide/dos/IoErr"} result of
                    @{"ERROR_NO_FREE_STORE" LINK "gg:doc/NDK/Guide/Include/dos/dos.h/File" 181}.
        tags - pointer to an optional tag list which may be used to
               control features of the requester.@{b}

   TAGS@{ub}
        Tags for use with the file requester:

        @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92} (@{"struct Window" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 909} *) - Parent window of requester. If no
                        @{"ASLFR_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 93} tag is specified, the window structure
                        is used to determine on which screen to open the
                        requesting window. (V36)

        @{"ASLFR_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 94} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Name of a public screen to open on.
                        This overrides the screen used by @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92}. (V38)

        @{"ASLFR_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 93} (@{"struct Screen" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 133} *) - Screen on which to open the requester.
                        This overrides the screen used by @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92} or by
                        @{"ASLFR_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 94}. (V38)

        @{"ASLFR_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 95} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                        allocate a new IDCMP port for the requesting window.
                        If not specified or set to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, and if @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92}
                        is provided, the requesting window will share
                        @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92}'s IDCMP port. (V38)

        @{"ASLFR_IntuiMsgFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 96} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call whenever an
                        unknown Intuition message arrives at the message port
                        being used by the requesting window. The function
                        receives the following parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *)
                                A2 - (@{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *)
                        (V38)

        @{"ASLFR_SleepWindow" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 97} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tag will cause the
                        window specified by @{"ASLFR_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 92} to be "put to
                        sleep". That is, a busy pointer will be displayed in
                        the parent window, and no gadget or menu activity will
                        be allowed. This is done by opening an invisible
                        Intuition Requester in the parent window. (V38)

        @{"ASLFR_UserData" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 98} (@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49}) - A 32-bit value that is simply copied in the
                        fr_UserData field of the requester structure. (V38)

        @{"ASLFR_TextAttr" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 106} (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *) - Font to be used for the requesting
                        window's gadgets and menus. If this tag is not provided
                        or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the default font of the screen
                        on which the requesting window opens will be used.
                        This font must already be in memory as ASL calls
                        @{"OpenFont()" LINK "gg:doc/NDK/Guide/graphics/OpenFont"} and not @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}. As of V38, the font
                        used in the file requester's file list must be
                        monospaced. If the font provided with this tag is
                        not monospaced, then the file list will use the current
                        system default font as chosen in preferences. (V38)

        @{"ASLFR_Locale" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 107} (@{"struct Locale" LINK "gg:doc/NDK/Guide/Include/libraries/locale.h/File" 118} *) - Locale to use for the requesting
                        window. This determines the language used for the
                        requester's gadgets and menus. If this tag is not
                        provided or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the system's current
                        default locale will be used. As of V38, items such as
                        the date format always uses the system default locale.
                        (V38)

        @{"ASLFR_TitleText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 108} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Title to use for the requesting window.
                        Default is no title. (V36)

        @{"ASLFR_PositiveText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 109} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the positive gadget in the
                        requester. English default is "OK". Prior to V38, this
                        string could not be longer than 6 characters. (V36)

        @{"ASLFR_NegativeText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 110} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the negative gadget in the
                        requester. English default is "Cancel". Prior to V38,
                        this string could not be longer than 6 characters.
                        (V36)

        @{"ASLFR_InitialLeftEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 113} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested left edge of requesting
                        window. (V36)

        @{"ASLFR_InitialTopEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 114} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested top edge of requesting
                        window. (V36)

        @{"ASLFR_InitialWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 115} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested width of requesting window. (V36)

        @{"ASLFR_InitialHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 116} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested height of requesting window.
                        (V36)

        @{"ASLFR_InitialFile" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 117} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Initial contents of the file requester's
                        File text gadget. Default is empty. (V36)

        @{"ASLFR_InitialDrawer" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 118} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Initial contents of the file
                        requester's Drawer text gadget. Default is empty. (V36)

        @{"ASLFR_InitialPattern" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 119} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Initial contents of the file
                        requester's Pattern text gadget. Default is #?. (V36)

        @{"ASLFR_InitialShowVolumes" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 120} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When opening the file requester,
                        read the list of active volumes, do not read the contents
                        of the specified drawer. (V44)

        @{"ASLFR_Flags1" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 123} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Bulk initialization of many of the requester's
                        options. See <@{"libraries/asl.h" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File"}> for the possible
                        flag values. Default is 0. (V36)

        @{"ASLFR_Flags2" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 124} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Bulk initialization of many of the requester's
                        options. See <@{"libraries/asl.h" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File"}> for the possible
                        flag values. Default is 0. (V36)

        @{"ASLFR_DoSaveMode" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 125} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} when the file requester
                        is being used for saving. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_DoMultiSelect" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 126} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to let the user
                        select multiple files at once. This tag excludes
                        save mode. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_DoPatterns" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 127} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause a pattern
                        gadget to be displayed. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_DrawersOnly" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 130} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to only display drawers, and have no File gadget. This
                        is useful to let the user choose a destination
                        directory. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_FilterFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 131} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call for each file
                        encountered. If the function returns @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the file is
                        included in the file list, otherwise it is rejected
                        and not displayed. The function receives the following
                        parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"struct AnchorPath" LINK "gg:doc/NDK/Guide/Include/dos/dosasl.h/File" 52} *)
                                A2 - (@{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *)
                        (V38)

        @{"ASLFR_RejectIcons" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 132} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        not to display Workbench icons. This flag should
                        normally be set. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_RejectPattern" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 133} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *) - Specifies an AmigaDOS pattern that is
                        used to reject files. That is, any files with names
                        matching this pattern are not included in the file
                        list. Note that the pattern must have been
                        parsed by @{"dos.library/ParsePatternNoCase()" LINK "gg:doc/NDK/Guide/dos/ParsePatternNoCase"}. Default
                        is ~(#?) which matches nothing. (V38)

        @{"ASLFR_AcceptPattern" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 134} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *) - Specifies an AmigaDOS pattern that is
                        used to accept files. That is, only files with names
                        matching this pattern are included in the file list.
                        Note that the pattern must have been parsed by
                        @{"dos.library/ParsePatternNoCase()" LINK "gg:doc/NDK/Guide/dos/ParsePatternNoCase"}. Default is #? which
                        matches everything. (V38)

        @{"ASLFR_FilterDrawers" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 135} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} if you want the
                        @{"ASLFR_RejectPattern" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 133}, @{"ASLFR_AcceptPattern" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 134}, and the
                        Pattern text gadget to also apply to drawer names.
                        Normally, drawers are always displayed. Default is
                        @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFR_HookFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 136} (@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49}) - A function that is called if the
                        @{"FRF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 154} or @{"FRF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 155} flags are specified in
                        the @{"ASLFR_Flags1" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 123} tag. The function will be called like
                        so, with the arguments on the stack using the standard
                        C pushing order:

                                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} function(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} mask, @{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} object,
                                               @{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *fr)

                        The Mask value is a copy of the specific @{"ASLFR_Flags1" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 123}
                        value the callback is for, either @{"FRF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 154} or
                        @{"FRF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 155}; object is a pointer to a data object
                        (a (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *) for @{"FRF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 155}, and a
                        (@{"struct AnchorPath" LINK "gg:doc/NDK/Guide/Include/dos/dosasl.h/File" 52} *) for @{"FRF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 154}).

                        For @{"FRF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 154}, the function should return 0 if the
                        file should be added to the list, non-zero if it
                        shouldn't. For @{"FRF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 155}, the function should
                        return the original (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *) it was
                        passed.

        @{"ASLFR_SetSortBy" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 139} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) -- Criteria for sorting the file list.
                this must be one of @{"ASLFRSORTBY_Name" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 171} (sort by name),
                @{"ASLFRSORTBY_Date" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 172} (sort by date) or @{"ASLFRSORTBY_Size" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 173}
                (sort by size).
                Default is @{"ASLFRSORTBY_Name" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 171}. (V44)

        @{"ASLFR_GetSortBy" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 140} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} *) -- Before leaving @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(), the
                file requester will store its current sort criteria
                (@{"ASLFRSORTBY_Name" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 171}, @{"ASLFRSORTBY_Date" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 172} or @{"ASLFRSORTBY_Size" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 173})
                in the @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} pointed to by this tag.
                Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, i.e. no data is stored. (V44)

        @{"ASLFR_SetSortDrawers" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 141} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) -- Determines where drawers
                are placed in the sorted list; this must be one of
                @{"ASLFRSORTDRAWERS_First" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 176} (drawers appear before all
                other files), @{"ASLFRSORTDRAWERS_Mix" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 177} (drawers are sorted
                along with the the other files) or @{"ASLFRSORTDRAWERS_Last" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 178}
                (drawers appear after all other files).
                Default is @{"ASLFRSORTDRAWERS_First" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 176}. (V44)

        @{"ASLFR_GetSortDrawers" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 142} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} *) -- Before leaving @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(),
                the file requester will store its current drawer list
                placement (@{"ASLFRSORTDRAWERS_First" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 176}, @{"ASLFRSORTDRAWERS_Mix" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 177}
                or @{"ASLFRSORTDRAWERS_Last" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 178}) in the @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} pointed to by
                this tag.
                Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, i.e. no data is stored. (V44)

        @{"ASLFR_SetSortOrder" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 143} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) -- Selects the order in which
                the list is sorted; this must be one of @{"ASLFRSORTORDER_Ascend" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 181}
                (smallest/oldest files first, largest/newest files last)
                or @{"ASLFRSORTORDER_Descend" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 182} (largest/newest files first,
                smallest/oldest files last).
                Default is @{"ASLFRSORTORDER_Ascend" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 181}. (V44)

        @{"ASLFR_GetSortOrder" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 144} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} *) -- Before leaving @{"AslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 51}(),
                the file requester will store its current sort order
                (@{"ASLFRSORTORDER_Ascend" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 181}, @{"ASLFRSORTORDER_Descend" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 182}) in the
                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} pointed to by this tag.
                Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, i.e. no data is stored. (V44)

        @{"ASLFR_PopToFront" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 99} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                bring the screen to the front the requester has opened on.
                The position of the screen will be adjusted, too, to bring
                the area into view the window has opened in. The screen
                brought to the front will be pushed into the background
                again when the requester window closes.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V44)

        @{"ASLFR_Activate" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 102} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the requester window
                will open in active state.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V45)

        Tags used for the font requester:

        @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212} (@{"struct Window" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 909} *) - Parent window of requester. If no
                        @{"ASLFO_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 213} tag is specified, the window structure
                        is used to determine on which screen to open the
                        requesting window. (V36)

        @{"ASLFO_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 214} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Name of a public screen to open on.
                        This overrides the screen used by @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212}. (V38)

        @{"ASLFO_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 213} (@{"struct Screen" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 133} *) - Screen on which to open the requester.
                        This overrides the screen used by @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212} or by
                        @{"ASLFO_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 214}. (V38)

        @{"ASLFO_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 215} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                        allocate a new IDCMP port for the requesting window.
                        If not specified or set to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, and if @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212}
                        is provided, the requesting window will share
                        @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212}'s IDCMP port. (V38)

        @{"ASLFO_IntuiMsgFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 216} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call whenever an
                        unknown Intuition message arrives at the message port
                        being used by the requesting window. The function
                        receives the following parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *)
                                A2 - (@{"struct FontRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 193} *)
                        (V38)

        @{"ASLFO_SleepWindow" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 217} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tag will cause the
                        window specified by @{"ASLFO_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 212} to be "put to
                        sleep". That is, a busy pointer will be displayed in
                        the parent window, and no gadget or menu activity will
                        be allowed. This is done by opening an invisible
                        Intuition Requester in the parent window. (V38)

        @{"ASLFO_UserData" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 218} (@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49}) - A 32-bit value that is simply copied in the
                        fo_UserData field of the requester structure. (V38)

        @{"ASLFO_TextAttr" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 227} (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *) - Font to be used for the requesting
                        window's gadgets and menus. If this tag is not provided
                        or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the default font of the screen
                        on which the requesting window opens will be used.
                        This font must already be in memory as ASL calls
                        @{"OpenFont()" LINK "gg:doc/NDK/Guide/graphics/OpenFont"} and not @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}.
                        (V38)

        @{"ASLFO_Locale" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 228} (@{"struct Locale" LINK "gg:doc/NDK/Guide/Include/libraries/locale.h/File" 118} *) - Locale to use for the requesting
                        window. This determines the language used for the
                        requester's gadgets and menus. If this tag is not
                        provided or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the system's current
                        default locale will be used. (V38)

        @{"ASLFO_TitleText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 229} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Title to use for the requesting window.
                        Default is no title. (V36)

        @{"ASLFO_PositiveText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 230} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the positive gadget in the
                        requester. English default is "OK". Prior to V38, this
                        string could not be longer than 6 characters. (V36)

        @{"ASLFO_NegativeText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 231} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the negative gadget in the
                        requester. English default is "Cancel". Prior to V38,
                        this string could not be longer than 6 characters.
                        (V36)

        @{"ASLFO_InitialLeftEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 234} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested left edge of requesting
                        window. (V36)

        @{"ASLFO_InitialTopEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 235} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested top edge of requesting
                        window. (V36)

        @{"ASLFO_InitialWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 236} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested width of requesting window. (V38)

        @{"ASLFO_InitialHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 237} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested height of requesting window.
                        (V38)

        @{"ASLFO_InitialName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 238} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Initial contents of the font requester's
                        Name text gadget. Default is none. (V36)

        @{"ASLFO_InitialSize" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 239} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - Initial contents of the font requester's
                        Size numeric gadget (fo_Attr.ta_YSize). Default is 8.
                        (V36)

        @{"ASLFO_InitialStyle" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 240} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Initial setting of the font requester's
                        Style gadget (fo_Attr.ta_Style). Default is @{"FS_NORMAL" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 26}.
                        (V36)

        @{"ASLFO_InitialFlags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 241} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Initial setting of the font requester's
                        fo_Flags field (fo_Attr.ta_Flags). Default is
                        @{"FPF_ROMFONT" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 43}. (V36)

        @{"ASLFO_InitialFrontPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 242} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Initial setting of the font requester's
                        Front Color gadget (fo_FrontPen). This value also
                        determines the color used to render the text in the
                        sample font area. Default is 1. (V36)

        @{"ASLFO_InitialBackPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 243} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Initial setting of the font requester's
                        Back Color gadget (fo_BackPen). This value also
                        determines the color used to render the background of
                        the sample font area. Default is 0. (V36)

        @{"ASLFO_InitialDrawMode" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 244} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - Initial setting of the font
                        requester's Mode gadget (fo_DrawMode). Default is
                        @{"JAM1" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 90}. (V38)

        @{"ASLFO_Flags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 247} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Bulk initialization of many of the requester's
                        options. See <@{"libraries/asl.h" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File"}> for the possible
                        flag values. Default is 0. (V36)

        @{"ASLFO_DoFrontPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 248} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Front Color selection gadget. Default is
                        @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFO_DoBackPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 249} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Back Color selection gadget. Default
                        is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFO_DoStyle" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 250} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Style checkboxes. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLFO_DoDrawMode" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 251} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Mode cycle gadget. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLFO_SampleText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 252} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - The sample text to print in the font
                preview area. This can either be a nul-terminated string
                which should not be longer than 80 characters, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}
                will tell ASL to use the default sample text.
                Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}. (V45)

        @{"ASLFO_FixedWidthOnly" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 255} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the
                        requester to only display fixed-width fonts. Default
                        is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLFO_MinHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 256} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - The minimum font height to let the user
                        select. Default is 5. (V36)

        @{"ASLFO_MaxHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 257} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - The maximum font height to let the user
                        select. Default is 24. (V36)

        @{"ASLFO_FilterFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 258} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call for each font
                        encountered. If the function returns @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the font is
                        included in the font list, otherwise it is rejected
                        and not displayed. The function receives the following
                        parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *)
                                A2 - (@{"struct FontRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 193} *)
                        (V38)

        @{"ASLFO_HookFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 259} (@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49}) - A function that is called if the
                        @{"FOF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 285} or @{"FOF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 284} flags are specified in
                        the @{"ASLFO_Flags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 247} tag. The function will be called like
                        so, with the arguments on the stack using the standard
                        C pushing order:

                                @{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} function(@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52} mask, @{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49} object,
                                               @{"struct FontRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 193} *fr)

                        The Mask value is a copy of the specific @{"ASLFO_Flags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 247}
                        value the callback is for, either @{"FOF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 285} or
                        @{"FOF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 284}; object is a pointer to a data object
                        (a (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *) for @{"FOF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 284}, and a
                        (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *) for @{"FOF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 285}).

                        For @{"FOF_FILTERFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 285}, the function should return 1 if the
                        font should be added to the list, 0 if it
                        shouldn't. For @{"FOF_INTUIFUNC" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 284}, the function should
                        return the original (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *) it was
                        passed.

        @{"ASLFO_MaxFrontPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 260} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - The maximum number of pens that are offered
                        to the user when the @{"ASLFO_DoFrontPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 248} tag is specified.
                        Default is 255. (V40)

        @{"ASLFO_MaxBackPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 261} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62}) - The maximum number of pens that are offered
                        to the user when the @{"ASLFO_DoBackPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 249} tag is specified.
                        Default is 255. (V40)

        @{"ASLFO_ModeList" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 264} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67} *) - Replacement list of modes to display in
                        Mode cycle gadget. The first string pointer is used
                        as the name of the gadgets, and following strings are
                        used as the different labels of the cycle gadget,
                        until a @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} string pointer which denotes the end of
                        the string pointer array. The first string in this
                        array corresponds to @{"JAM1" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 90}, the second is @{"JAM2" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 91}, the
                        third to @{"COMPLEMENT" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 92}. The array can be early terminated
                        by not providing all entries. English default for
                        the array is "Text" "Text+Field" and "Complement".
                        (V36)

        @{"ASLFO_FrontPens" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 265} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *) - Pointer to a table of pen numbers
                        indicating  which colors should be used and edited
                        by the palette gadget when the @{"ASLFO_DoFrontPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 248} tag is
                        specified. This array must contain as many entries as
                        there are colors displayed in the palette gadget. The
                        Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, which causes a 1-to-1 mapping of pen
                        numbers. (V40)

        @{"ASLFO_BackPens" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 266} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62} *) - Pointer to a table of pen numbers
                        indicating  which colors should be used and edited
                        by the palette gadget when the @{"ASLFO_DoBackPen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 249} tag is
                        specified. This array must contain as many entries as
                        there are colors displayed in the palette gadget. The
                        Default is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, which causes a 1-to-1 mapping of pen
                        numbers. (V40)

        @{"ASLFO_PopToFront" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 219} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                bring the screen to the front the requester has opened on.
                The position of the screen will be adjusted, too, to bring
                the area into view the window has opened in. The screen
                brought to the front will be pushed into the background
                again when the requester window closes.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V44)

        @{"ASLFO_Activate" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 222} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the requester window
                will open in active state.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V45)

        Tags used for the screen mode requester (V38):

        @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340} (@{"struct Window" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 909} *) - Parent window of requester. If no
                        @{"ASLSM_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 341} tag is specified, the window structure
                        is used to determine on which screen to open the
                        requesting window. (V38)

        @{"ASLSM_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 342} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Name of a public screen to open on.
                        This overrides the screen used by @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340}. (V38)

        @{"ASLSM_Screen" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 341} (@{"struct Screen" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 133} *) - Screen on which to open the requester.
                        This overrides the screen used by @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340} or by
                        @{"ASLSM_PubScreenName" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 342}. (V38)

        @{"ASLSM_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 343} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                        allocate a new IDCMP port for the requesting window.
                        If not specified or set to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, and if @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340}
                        is provided, the requesting window will share
                        @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340}'s IDCMP port. (V38)

        @{"ASLSM_IntuiMsgFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 344} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call whenever an
                        unknown Intuition message arrives at the message port
                        being used by the requesting window. The function
                        receives the following parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"struct IntuiMessage" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 763} *)
                                A2 - (@{"struct ScreenModeRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 297} *)
                        (V38)

        @{"ASLSM_SleepWindow" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 345} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - When set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tag will cause the
                        window specified by @{"ASLSM_Window" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 340} to be "put to
                        sleep". That is, a busy pointer will be displayed in
                        the parent window, and no gadget or menu activity will
                        be allowed. This is done by opening an invisible
                        Intuition Requester in the parent window. (V38)

        @{"ASLSM_UserData" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 346} (@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49}) - A 32-bit value that is simply copied in the
                        sm_UserData field of the requester structure. (V38)

        @{"ASLSM_TextAttr" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 355} (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *) - Font to be used for the requesting
                        window's gadgets and menus. If this tag is not provided
                        or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the default font of the screen
                        on which the requesting window opens will be used.
                        This font must already be in memory as ASL calls
                        @{"OpenFont()" LINK "gg:doc/NDK/Guide/graphics/OpenFont"} and not @{"OpenDiskFont()" LINK "gg:doc/NDK/Guide/diskfont/OpenDiskFont"}.
                        (V38)

        @{"ASLSM_Locale" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 356} (@{"struct Locale" LINK "gg:doc/NDK/Guide/Include/libraries/locale.h/File" 118} *) - Locale to use for the requesting
                        window. This determines the language used for the
                        requester's gadgets and menus. If this tag is not
                        provided or its value is @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}, the system's current
                        default locale will be used. (V38)

        @{"ASLSM_TitleText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 357} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Title to use for the requesting window.
                        Default is no title. (V38)

        @{"ASLSM_PositiveText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 358} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the positive gadget in the
                        requester. English default is "OK". Prior to V38, this
                        string could not be longer than 6 characters. (V36)

        @{"ASLSM_NegativeText" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 359} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67}) - Label of the negative gadget in the
                        requester. English default is "Cancel". Prior to V38,
                        this string could not be longer than 6 characters.
                        (V36)

        @{"ASLSM_InitialLeftEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 362} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested left edge of requesting
                        window. (V38)

        @{"ASLSM_InitialTopEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 363} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested top edge of requesting
                        window. (V38)

        @{"ASLSM_InitialWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 364} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested width of requesting window. (V38)

        @{"ASLSM_InitialHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 365} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Suggested height of requesting window.
                        (V38)

        @{"ASLSM_InitialDisplayID" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 366} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Initial setting of the Mode list view
                        gadget (sm_DisplayID). Default is 0 (@{"LORES_KEY" LINK "gg:doc/NDK/Guide/Include/graphics/modeid.h/File" 48}). (V38)

        @{"ASLSM_InitialDisplayWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 367} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Initial setting of the Width gadget
                        (sm_DisplayWidth). Default is 640. (V38)

        @{"ASLSM_InitialDisplayHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 368} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - Initial setting of the Height
                        gadget (sm_DisplayHeight). Default is 200. (V38)

        @{"ASLSM_InitialDisplayDepth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 369} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - Initial setting of the Colors
                        gadget (sm_DisplayDepth). Default is 2. (V38)

        @{"ASLSM_InitialOverscanType" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 370} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - Initial setting of the
                        Overscan Type cycle gadget (sm_OverscanType).
                        These values are the OSCAN_#? values from
                        <@{"intuition/screens.h" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File"}>.

                        User-space naming of overscan types is different than
                        programmer-space:

                        Under V38:
                             0               -->  "Regular Size"
                             @{"OSCAN_TEXT" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 531}      -->  "Text Size"
                             @{"OSCAN_STANDARD" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 532}  -->  "Graphics Size"
                             OSCAN_MAXIMUM   -->  "Maximum Size"

                        Under V39 and above:
                             @{"OSCAN_TEXT" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 531}      -->  "Text Size"
                             @{"OSCAN_STANDARD" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 532}  -->  "Graphics Size"
                             OSCAN_MAXIMUM   -->  "Extreme Size"
                             @{"OSCAN_VIDEO" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 534}     -->  "Maximum Size"

                        @{"OSCAN_VIDEO" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 534} is not supported prior to V39. The value
                        0 corresponds to "Regular Size" under V38. Since this
                        value isn't supported by Intuition's @{"SA_Overscan" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 297}
                        tag, it was removed from ASL in V39. Asking for 0
                        under V39 will yield @{"OSCAN_TEXT" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 531}.

                        Default is @{"OSCAN_TEXT" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 531}. (V38)

        @{"ASLSM_InitialAutoScroll" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 371} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Initial setting of the AutoScroll
                        cycle gadget (sm_AutoScroll). Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V38)

        @{"ASLSM_InitialInfoOpened" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 372} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Whether to open the property
                        information window automatically. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLSM_InitialInfoLeftEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 373} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Initial left edge of information
                        window. (V38)

        @{"ASLSM_InitialInfoTopEdge" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 374} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}) - Initial top edge of information
                        window. (V38)

        @{"ASLSM_DoWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 377} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Width numeric gadget. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLSM_DoHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 378} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the Height numeric gadget. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLSM_DoDepth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 379} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester to
                        display the Colors slider gadget. Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.
                        (V38)

        @{"ASLSM_DoOverscanType" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 380} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the
                        requester to display the Overscan Type cycle gadget.
                        Default is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLSM_DoAutoScroll" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 381} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) - Set this tag to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to cause the requester
                        to display the AutoScroll checkbox gadget. Default is
                        @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}. (V38)

        @{"ASLSM_PropertyFlags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 384} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - A mode must have these property flags to
                        appear in the list of modes. Only the bits set in
                        @{"ASLSM_PropertyMask" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 385} are considered. See the description
                        of @{"ASLSM_PropertyMask" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 385} for more info. Default is
                        @{"DIPF_IS_WB" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 98}. (V38)

        @{"ASLSM_PropertyMask" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 385} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - A mask to apply to @{"ASLSM_PropertyFlags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 384}
                        to determine which bits to consider. The use of these
                        two values is identical in concept to how Exec uses
                        the two flag paraneters in @{"exec.library/SetSignal()" LINK "gg:doc/NDK/Guide/exec/SetSignal"}.
                        Default is @{"DIPF_IS_WB" LINK "gg:doc/NDK/Guide/Include/graphics/displayinfo.h/File" 98}. This is how this tag and the
                        @{"ASLSM_PropertyFlags" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 384} tag interact:

                        if ((displayInfo.PropertyFlags & propertyMask) ==
                           (propertyFlags & propertyMask))
                        {
                            /* Mode accepted */
                        }
                        else
                        {
                            /* Mode rejected */
                        }

                        where "displayInfo" is a filled in DisplayInfo
                        structure for a given mode. (V38)

        @{"ASLSM_MinWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 386} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - The minimum display width to let the user
                        choose. Default is 16. (V38)

        @{"ASLSM_MaxWidth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 387} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - The maximum display width to let the user
                        choose. Default is 16368. (V38)

        @{"ASLSM_MinHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 388} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - The minimum display height to let the user
                        choose. Default is 16. (V38)

        @{"ASLSM_MaxHeight" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 389} (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) - The maximum display height to let the user
                        choose. Default is 16384. (V38)

        @{"ASLSM_MinDepth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 390} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - The minimum display depth to let the user
                        choose. Default is 1. (V38)

        @{"ASLSM_MaxDepth" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 391} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55}) - The maximum display depth to let the user
                        choose. Default is 24. (V38)

        @{"ASLSM_FilterFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 392} (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *) - A function to call for each mode
                        encountered. If the function returns @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the mode is
                        included in the file list, otherwise it is rejected
                        and not displayed. The function receives the following
                        parameters:
                                A0 - (@{"struct Hook" LINK "gg:doc/NDK/Guide/Include/utility/hooks.h/File" 28} *)
                                A1 - (@{"ULONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 52}) mode id
                                A2 - (@{"struct ScreenModeRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 297} *)
                        (V38)

        @{"ASLSM_CustomSMList" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 395} (@{"struct List" LINK "gg:doc/NDK/Guide/Include/exec/lists.h/File" 20} *) - Custom list of modes to let
                        the user choose from. This is a list of DisplayNode
                        nodes, see <@{"libraries/asl.h" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File"}> for the definition. (V38)

        @{"ASLSM_PopToFront" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 347} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, this tells ASL to
                bring the screen to the front the requester has opened on.
                The position of the screen will be adjusted, too, to bring
                the area into view the window has opened in. The screen
                brought to the front will be pushed into the background
                again when the requester window closes.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V44)

        @{"ASLSM_Activate" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 350} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91}) -- If set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, the requester window
                will open in active state.
                Default is @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. (V45)@{b}

   RESULT@{ub}
        result - @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} if the user cancelled the requester or if something
                 prevented the requester from opening. If @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}, values in the
                 requester structure will be set depending on the particular
                 type of requester.

                 Under V38, If the return value is @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}, you can look
                 at the result from the @{"dos.library/IoErr()" LINK "gg:doc/NDK/Guide/dos/IoErr"} function to
                 determine whether the requester was cancelled or simply
                 failed to open. If @{"dos.library/IoErr()" LINK "gg:doc/NDK/Guide/dos/IoErr"} returns 0, then the
                 requester was cancelled, any other value indicates a failure
                 to open. Current possible failure codes are
                 @{"ERROR_NO_FREE_STORE" LINK "gg:doc/NDK/Guide/Include/dos/dos.h/File" 181} which indicates there was not enough
                 memory, and @{"ERROR_NO_MORE_ENTRIES" LINK "gg:doc/NDK/Guide/Include/dos/dos.h/File" 217} which indicates no modes
                 were available for display in the screen mode requester
                 (usually because the application filter hook filtered them
                 all away)@{b}

   WARNING@{ub}
        All ASL requester data structures are READ-ONLY and can only be
        modified by using tags!

        You must not specify requester options by using both pre-V38
        tags like @{"ASLFR_Flags1" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 123} and V38 tags like @{"ASLFR_DoSaveMode" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 125} as
        this can lead to unexpected results.

        Take care when attaching an ASL requester to a simple refresh
        window or a window that already shares its IDCMP with other
        windows. In such cases you must ask for the ASL requester to
        allocate a private IDCMP (with the @{"ASLFR_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 95},
        @{"ASLFO_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 215} and @{"ASLSM_PrivateIDCMP" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 343} tags) or your window
        will lose @{"IDCMP_REFRESHWINDOW" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 834} events and other messages.@{b}

   BUGS@{ub}
        Under V38 and V39, the filtering functions for the file requester
        were only asked to filter drawer selections if the @{"ASLFR_FilterDrawers" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 135}
        tag was set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}. The compatible behavior, reestablished in V40,
        is to always pass drawers for filtering.@{b}

   NOTES@{ub}
        As of V38-V40 the ASL requester uses @{"gadtools.library" LINK "gg:doc/NDK/Guide/gadtools/MAIN"} routines to
        create and maintain its user interface. Every IntuiMessage the library
        passes through the functions specified with @{"ASLSM_IntuiMsgFunc" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 344} and
        ASLSM_HookFunc will have been filtered through the
        @{"gadtools.library/GT_GetIMsg()" LINK "gg:doc/NDK/Guide/gadtools/GT_GetIMsg"} routine.@{b}

   SEE ALSO@{ub}
        @{"AllocAslRequest()" LINK "AllocAslRequest"}, @{"FreeAslRequest()" LINK "FreeAslRequest"}

@ENDNODE
@NODE "FreeAslRequest" "asl.library/FreeAslRequest()"
@{b}

   NAME@{ub}
        @{"FreeAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 47} -- frees resources allocated by @{"AllocAslRequest()" LINK "AllocAslRequest"}. (V36)@{b}

   SYNOPSIS@{ub}
        @{"FreeAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 47}(requester);
                       A0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"FreeAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 47}(@{"APTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 49});@{b}

   FUNCTION@{ub}
        @{"FreeAslRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 47}() frees any system resources allocated by
        @{"AllocAslRequest()" LINK "AllocAslRequest"}. Once a requester has been freed, it can no
        longer be used with other calls to @{"AslRequest()" LINK "AslRequest"}.@{b}

   INPUTS@{ub}
        requester - requester obtained from @{"AllocAslRequest()" LINK "AllocAslRequest"}, or @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101} in
                    which case this function does nothing.@{b}

   SEE ALSO@{ub}
        @{"AllocAslRequest()" LINK "AllocAslRequest"}

@ENDNODE
@NODE "FreeFileRequest" "asl.library/FreeFileRequest()"
@{b}

   NAME@{ub}
        @{"FreeFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 30} -- free file requester structure. (V36)@{b}

   SYNOPSIS@{ub}
        @{"FreeFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 30}(requester);
                        A0

        @{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24} @{"FreeFileRequest" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 30}(@{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *);@{b}

   NOTES@{ub}
        This function is obsolete, please use @{"AllocAslRequest()" LINK "AllocAslRequest"} instead.@{b}

   SEE ALSO@{ub}
        @{"FreeAslRequest()" LINK "FreeAslRequest"}

@ENDNODE
@NODE "RequestFile" "asl.library/RequestFile()"
@{b}

   NAME@{ub}
        @{"RequestFile" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 34} -- request user to select files. (V36)@{b}

   SYNOPSIS@{ub}
        result = @{"RequestFile" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 34}(requester);
        D0                   A0

        @{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91} @{"RequestFile" LINK "gg:doc/NDK/Guide/Include/inline/asl.h/File" 34}(@{"struct FileRequester" LINK "gg:doc/NDK/Guide/Include/libraries/asl.h/File" 71} *);@{b}

   NOTES@{ub}
        This function is obsolete, please use @{"AslRequest()" LINK "AslRequest"} instead.@{b}

   SEE ALSO@{ub}
        @{"AslRequest()" LINK "AslRequest"}

@ENDNODE
