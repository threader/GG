@DATABASE "gg:doc/NDK/Guide/integer_gc"
@MASTER   "gg:doc/NDK/AutoDocs/integer_gc.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:22:58
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "integer_gc.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                                 @{b}integer_gc@{ub}

@{"datasheet" LINK "datasheet"}

@{"INTEGER_GetClass()" LINK "INTEGER_GetClass"}

@ENDNODE
@NODE "datasheet" "integer_gc/datasheet (information)"


     NAME
        @{"integer_gc" LINK "gg:doc/NDK/Guide/integer_gc/MAIN"} -- A numeric entry gadget with arrows.

     SUPERCLASS
        gadgetclass

    REQUIRES
        bevel.image

     DESCRIPTION
        This gadget is a special form of a strggclass gadget for entering
        positive or negative long integers.  The user can type in the number
        or use the arrows to adjust the number up or down.

        Advantages that this class provides:

         > Supports window relativity.

         > Optionally creates arrows for adjusting the value.

         > Definable minimum and maximum values as well as maximum number
           of characters.

         > Supports OS 3.0 BOOPSI Gadget HelpTest.

     METHODS
        @{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63} -- Create the integer gadget.  Passed to superclass.

        @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65} -- Set object attributes.  Passed to superclass first.

        @{"OM_DISPOSE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 64} -- Cancels the integer menu before being passed onto
            superclass.

        @{"OM_UPDATE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 70} -- Set object notification attributes.  Passed to superclass
            first.

        @{"GM_RENDER" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 302} -- Renders the gadget imagry.  Overrides the superclass.

        @{"GM_GOACTIVE" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 305} -- Handles activation, pops up the integer menu.  Overrides
            the superclass.

        @{"GM_HANDLEINPUT" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 308} -- Handles input events once active.  Overrides the
            superclass.

        @{"GM_GOINACTIVE" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 311} -- Closes the integer menu.  Overrides the superclass.

        All other methods are passed to the superclass.

     ATTRIBUTES
        @{"GA_Disabled" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 78} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})
            Set to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95} to disable gadget, @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98} otherwise

            Defaults to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"GA_TextAttr" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 185} (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *)
            Font to use for the gadget text.

            Defaults to the screen's font.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"INTEGER_Number" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 29} (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})
            The value for the integer gadget.  The class will adjust this
            number to fit into the range you define.  The current value for
            @{"INTEGER_Number" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 29} will be returned in IntuiMessage.Code on
            @{"IDCMP_GADGETUP" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 838} as a @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54}.  Note that since the gadget handles
            values of size @{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51}, the value returned in IntuiMessage.Code
            will only be valid if the ranges you are allowing fit into a
            @{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54} value.  Otherwise you will have to use @{"GetAttr()" LINK "gg:doc/NDK/Guide/intuition/GetAttr"}.

            Defaults to 0.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}, @{"OM_NOTIFY" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 69})

        @{"INTEGER_MaxChars" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 32} (@{"WORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 54})
            Maximum number of characters you are allowing the number to be.
            This is including an optional negative sign (-).

            Defaults to 10.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}, @{"OM_NOTIFY" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 69})

        @{"INTEGER_Minimum" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 36} (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})
            Minimum value the number can have.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}, @{"OM_NOTIFY" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 69})

        @{"INTEGER_Maximum" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 39} (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})
            Maximum value the number can have.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}, @{"OM_NOTIFY" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 69})

        @{"INTEGER_Arrows" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 42} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})
            Should arrows be displayed for adjusting the number?  The class
            is smart enough to not render arrows if the gadget becomes too
            small.

            Defaults to @{"TRUE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 95}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"INTEGER_MinVisible" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 45} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55})
            If set, this tag specifies the minimum number of numeric
            characters which should be visible at the gadgets minimum
            domain size. This tag effects @{"GM_DOMAIN" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 323}'s result to tweak
            the display in a pleasing way.

            Defaults to 0.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65})

        All strggclass tags can be used with @{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65} and @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}
        except for @{"STRINGA_LongVal" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 262} and @{"STRINGA_MaxChars" LINK "gg:doc/NDK/Guide/Include/intuition/gadgetclass.h/File" 235}.  Use @{"INTEGER_Number" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 29}
        and @{"INTEGER_MaxChars" LINK "gg:doc/NDK/Guide/Include/gadgets/integer.h/File" 32} instead.

@ENDNODE
@NODE "INTEGER_GetClass" "integer_gc/INTEGER_GetClass()"
@{b}

    NAME@{ub}
        @{"INTEGER_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/integer.h/File" 23} -- Gets the pointer to the integer class.@{b}

    SYNOPSIS@{ub}
        integer_class = @{"INTEGER_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/integer.h/File" 23}();
        D0

        Class * @{"INTEGER_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/integer.h/File" 23}(@{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24});@{b}

    FUNCTION@{ub}
        Obtains the pointer to the Integer gadget class for use with
        @{"NewObject()" LINK "gg:doc/NDK/Guide/intuition/NewObject"}.  This function always returns a valid pointer so
        you do not need to check it.  The reason is that if the library
        opens fine, then the pointer returned is already setup.  (Of course
        this implies that if opening the library fails, you shouldn't be
        calling this.)

        Note that this function does not create the class, that is done
        when the class library is opened.@{b}

    INPUTS@{ub}
        Nothing.@{b}

    RESULT@{ub}
        integer_class - Pointer to the Integer gadget class.@{b}

    SEE ALSO@{ub}

@ENDNODE
