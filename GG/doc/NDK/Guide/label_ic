@DATABASE "gg:doc/NDK/Guide/label_ic"
@MASTER   "gg:doc/NDK/AutoDocs/label_ic.doc"
@REMARK   This file was created by ADtoHT 2.1 on 06-Jan-13 19:23:48
@REMARK   Do not edit
@REMARK   ADtoHT is © 1993-1995 Christian Stieber

@NODE MAIN "label_ic.doc"
@TOC "gg:doc/NDK/Guide/TOC/MAIN"

                                  @{b}label_ic@{ub}

@{"datasheet" LINK "datasheet"}

@{"LABEL_GetClass()" LINK "LABEL_GetClass"}

@ENDNODE
@NODE "datasheet" "label_ic/datasheet (information)"


    NAME
        @{"label_ic" LINK "gg:doc/NDK/Guide/label_ic/MAIN"} -- Image class for creating idenification labels

    SUPERCLASS
        imageclass

    REQUIRES
        None.

    DESCRIPTION
        Label is an image class suitable for creating identification labels,
        such as might appear next to or inside a gadget, or as some kind of
        title in a window.  A label consists of text and graphics mixed in
        a variety of ways.

    METHODS
        @{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}--Create the label image.  Passed to superclass, then @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}.

        @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66}--Get object attributes.  Passed to superclass first.

        @{"OM_SET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 65}--Set object attributes.  Passed to superclass first.

        @{"OM_UPDATE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 70}--Set object notification attributes.  Passed to superclass
            first.

        @{"IM_DRAW" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 183}--Renders the images.  Overrides the superclass.

        All other methods are passed to the superclass, including @{"OM_DISPOSE" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 64}.

    ATTRIBUTES
        @{"IA_Font" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 64} (@{"struct TextAttr" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 65} *)
            Font to use for any label text.  Only affects text passed AFTER
            this tag is given.  If you did not use the @{"LABEL_DrawInfo" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 49} tag
            then you MUST use this tag to specify what font to use.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"IA_FGPen" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 38} (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})
            Foreground pen for any label text.  Only affects text passed AFTER
            this tag is given.

            Defaults to pen 1.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"IA_BGPen" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 40} (@{"LONG" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 51})
            Background pen for any label text.  Only affects text passed AFTER
            this tag is given.

            Defaults to pen 0.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"IA_Mode" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 63} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62})
            Draw mode for rendering text.  Only affects text passed AFTER
            this tag is given.

            Defaults to @{"JAM1" LINK "gg:doc/NDK/Guide/Include/graphics/rastport.h/File" 90}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"LABEL_DrawInfo" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 49} (@{"struct DrawInfo" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 66} *)
            Contains important pen information.  This is used to get default
            font information.  If you do not use @{"IA_Font" LINK "gg:doc/NDK/Guide/Include/intuition/imageclass.h/File" 64} to specify fonts
            then you MUST pass the screen DrawInfo before you give the
            @{"LABEL_Text" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 51} tag.

            Defaults to @{"NULL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 101}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"LABEL_Text" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 51} (@{"STRPTR" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 67})
            A text string to render.  A '\\n' in the string will cause the
            text position to move to a new line.  The first underscore ('_')
            in the string will be used to underline the proceeding character
            in the string.  Any number of text strings and images can be
            provided in a label, mixed at will.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"LABEL_SoftStyle" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 72} (@{"UBYTE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 62})
            SoftStyle for any label text.  Only affects text passed AFTER
            this tag is given.

            Defaults to @{"FS_NORMAL" LINK "gg:doc/NDK/Guide/Include/graphics/text.h/File" 26}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})


        @{"LABEL_Image" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 54} (@{"struct Image" LINK "gg:doc/NDK/Guide/Include/intuition/intuition.h/File" 705} *)
            An image to render.  Any number of text strings and images can
            be provided in a label, mixed at will.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"LABEL_DisposeImage" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 69} (@{"BOOL" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 91})
            Indicates that you want the NEXT image passed via @{"LABEL_Image" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 54}
            to be disposed automatically when the label instance is
            disposed.  This implies that the next image passed MUST be a
            BOOPSI image (since static images cannot be disposed).  This
            will only affect the next image passed, therefore you must
            specify this tag for every image you wish to have disposed of
            automatically.

            Defaults to @{"FALSE" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 98}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"LABEL_Mapping" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 57} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} *)
            Pen mapping array, affects the NEXT image passed via
            @{"LABEL_Image" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 54} ONLY.  You should pass a mapping for non-BOOPSI
            images only, it is expected that BOOPSI images would be
            correctly pen-mapped.

            The mapping array must have at least as many elements as the
            image has bit planes (i.e. 1 << Image.Depth).  For element
            i in the array you put in the new pen that you want pen i in
            your image to be mapped to.  For a typical 4-colour image, you
            might use the following mapping:

                @{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55} mapping[4];
                @{"struct DrawInfo" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 66} *drinfo;

                /* Before hand we'll have obtained the screen's DrawInfo.
                 */
                mapping[0] = drinfo->dri_Pens[@{"BACKGROUNDPEN" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 101}];
                mapping[1] = drinfo->dri_Pens[@{"SHADOWPEN" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 98}];
                mapping[2] = drinfo->dri_Pens[@{"SHINEPEN" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 97}];
                mapping[3] = drinfo->dri_Pens[@{"FILLPEN" LINK "gg:doc/NDK/Guide/Include/intuition/screens.h/File" 99}];

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63})

        @{"LABEL_Justification" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 60} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55})
            Justification for entire label.  This can be either @{"LJ_LEFT" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 28}
            (the default), @{"LJ_CENTRE" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 29} or @{"LJ_RIGHT" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 30}.

            Applicability is (@{"OM_NEW" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 63}, @{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

        @{"LABEL_Key" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 63} (@{"UWORD" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 55})
            For getting the key that was underscored in text passed with
            @{"LABEL_Text" LINK "gg:doc/NDK/Guide/Include/images/label.h/File" 51}.

            Applicability is (@{"OM_GET" LINK "gg:doc/NDK/Guide/Include/intuition/classusr.h/File" 66})

    SEE ALSO
        @{"intuition.library/GetScreenDrawInfo()" LINK "gg:doc/NDK/Guide/intuition/GetScreenDrawInfo"}

@ENDNODE
@NODE "LABEL_GetClass" "label_ic/LABEL_GetClass()"
@{b}

    NAME@{ub}
        @{"LABEL_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/label.h/File" 23} -- Gets the pointer to the label image class.@{b}

    SYNOPSIS@{ub}
        label_class = @{"LABEL_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/label.h/File" 23}();
        D0

        Class * @{"LABEL_GetClass" LINK "gg:doc/NDK/Guide/Include/inline/label.h/File" 23}(@{"VOID" LINK "gg:doc/NDK/Guide/Include/exec/types.h/File" 24});@{b}

    FUNCTION@{ub}
        Obtains the pointer to the Label gadget class for use with
        @{"NewObject()" LINK "gg:doc/NDK/Guide/intuition/NewObject"}.  This function always returns a valid pointer so
        you do not need to check it.  The reason is that if the library
        opens fine, then the pointer returned is already setup.  (Of course
        this implies that if opening the library fails, you shouldn't be
        calling this.)

        Note that this function does not create the class, that is done
        when the class library is opened.@{b}

    INPUTS@{ub}
        Nothing.@{b}

    RESULT@{ub}
        label_class - Pointer to the Label gadget class.@{b}

    SEE ALSO@{ub}

@ENDNODE
